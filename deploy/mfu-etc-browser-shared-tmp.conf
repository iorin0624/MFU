[Service]
# The ETC browser runs as a separate long-lived process and exposes Xvfb on
# /tmp/.X11-unix/X97.  The web application must share that mount namespace to
# inspect and activate the browser from the administration screen.
PrivateTmp=no
