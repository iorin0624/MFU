# Include this inside both mfu.iori0624.jp TLS VirtualHost blocks.
# The parent vhost uses ProxyErrorOverride On for maintenance responses, but
# Vue clients must receive Flask's structured 4xx JSON responses unchanged.
<Location /album/api/>
    ProxyErrorOverride Off
</Location>
