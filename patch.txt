diff --git a/chat/__init__.py b/chat/__init__.py
index 76cecd381a1186ad0b7feb45f9019bd5364ac87b..1f6594c68a585d912380fbd82f1c9eb90b0df46e 100644
--- a/chat/__init__.py
+++ b/chat/__init__.py
@@ -1273,184 +1273,50 @@ def _parse_read_room_key(room_key: str | None) -> dict[str, Any]:
     if value.startswith("dm:"):
         dm_uuid = value.split(":", 1)[1].strip()
         if dm_uuid:
             result.update({"mode": "dm", "dm_uuid": dm_uuid, "room_key": _build_read_room_key(dm_uuid=dm_uuid)})
         return result
 
     parts = value.split(":")
     if len(parts) == 4 and parts[0] == "event" and parts[2] == "room":
         try:
             event_id = int(parts[1])
         except Exception:
             event_id = 0
         room_id = parts[3].strip()
         if event_id > 0 and room_id:
             result.update(
                 {
                     "mode": "event",
                     "event_id": event_id,
                     "room_id": room_id,
                     "room_key": _build_read_room_key(event_id=event_id, room_id=room_id),
                 }
             )
     return result
 
 
-def _debug_list_recent_event_rooms(limit: int = 50) -> list[dict[str, Any]]:
-    max_rows = max(1, min(int(limit or 50), 200))
-    if not _ensure_chat_rooms_schema() or not _ensure_chat_messages_room_schema():
-        return []
-    db = get_db()
-    cur = db.cursor(dictionary=True)
-    try:
-        cur.execute(
-            """
-            SELECT r.event_id,
-                   r.room_id,
-                   r.room_name,
-                   latest.latest_message_id,
-                   latest.latest_message_at
-              FROM chat_rooms r
-              JOIN (
-                    SELECT m.event_id,
-                           m.room_id,
-                           MAX(m.id) AS latest_message_id,
-                           MAX(m.created_at) AS latest_message_at
-                      FROM chat_messages m
-                     GROUP BY m.event_id, m.room_id
-              ) latest
-                ON latest.event_id = r.event_id
-               AND latest.room_id = r.room_id
-             WHERE IFNULL(r.is_archived, 0) = 0
-             ORDER BY latest.latest_message_at DESC, latest.latest_message_id DESC
-             LIMIT %s
-            """,
-            (max_rows,),
-        )
-        rows = cur.fetchall() or []
-    finally:
-        cur.close()
-        db.close()
-
-    items: list[dict[str, Any]] = []
-    for row in rows:
-        event_id = int(row.get("event_id") or 0)
-        room_id = str(row.get("room_id") or "").strip()
-        if event_id <= 0 or not room_id:
-            continue
-        room_name = str(row.get("room_name") or "").strip() or room_id
-        items.append(
-            {
-                "event_id": event_id,
-                "room_id": room_id,
-                "room_name": room_name,
-                "latest_message_id": int(row.get("latest_message_id") or 0),
-                "latest_message_at": row.get("latest_message_at"),
-                "room_key": _build_read_room_key(event_id=event_id, room_id=room_id),
-            }
-        )
-    return items
-
-
-def _debug_list_recent_dm_rooms(limit: int = 50) -> list[dict[str, Any]]:
-    max_rows = max(1, min(int(limit or 50), 200))
-    if not _ensure_chat_dm_schema():
-        return []
-
-    actor = get_chat_actor() or {}
-    me_actor_key = _canonical_read_actor_key(get_chat_actor_key(actor))
-
-    db = get_db()
-    cur = db.cursor(dictionary=True)
-    try:
-        cur.execute(
-            """
-            SELECT id, uuid, dm_type, pair_key, last_message_id, last_message_at
-              FROM chat_dm_conversations
-             ORDER BY last_message_at DESC, id DESC
-             LIMIT %s
-            """,
-            (max_rows,),
-        )
-        conversations = cur.fetchall() or []
-        conversation_ids = [int(row.get("id") or 0) for row in conversations if int(row.get("id") or 0) > 0]
-
-        participant_map: dict[int, list[dict[str, Any]]] = {}
-        if conversation_ids:
-            placeholders = ",".join(["%s"] * len(conversation_ids))
-            cur.execute(
-                f"""
-                SELECT conversation_id, actor_key, display_name_cache
-                  FROM chat_dm_participants
-                 WHERE conversation_id IN ({placeholders})
-                """,
-                tuple(conversation_ids),
-            )
-            for row in cur.fetchall() or []:
-                cid = int(row.get("conversation_id") or 0)
-                participant_map.setdefault(cid, []).append(row)
-    finally:
-        cur.close()
-        db.close()
-
-    items: list[dict[str, Any]] = []
-    for conv in conversations:
-        conversation_id = int(conv.get("id") or 0)
-        dm_uuid = str(conv.get("uuid") or "").strip()
-        if conversation_id <= 0 or not dm_uuid:
-            continue
-
-        participants = participant_map.get(conversation_id) or []
-        peer_row = None
-        for participant in participants:
-            actor_key = _canonical_read_actor_key(str(participant.get("actor_key") or ""))
-            if actor_key and actor_key != me_actor_key:
-                peer_row = participant
-                break
-        if peer_row is None and participants:
-            peer_row = participants[0]
-
-        peer_actor_key = _canonical_read_actor_key(str((peer_row or {}).get("actor_key") or ""))
-        peer_display_name = str((peer_row or {}).get("display_name_cache") or "").strip()
-        if not peer_display_name and peer_actor_key:
-            peer_display_name = _actor_key_to_display_name(peer_actor_key)
-
-        items.append(
-            {
-                "dm_uuid": dm_uuid,
-                "conversation_id": conversation_id,
-                "dm_type": str(conv.get("dm_type") or ""),
-                "pair_key": str(conv.get("pair_key") or ""),
-                "latest_message_id": int(conv.get("last_message_id") or 0),
-                "latest_message_at": conv.get("last_message_at"),
-                "room_key": _build_read_room_key(dm_uuid=dm_uuid),
-                "peer_actor_key": peer_actor_key,
-                "peer_display_name": peer_display_name,
-            }
-        )
-    return items
-
 
 def _ensure_chat_read_state_v2_schema() -> bool:
     global CHAT_READ_STATE_V2_SCHEMA_READY
     if CHAT_READ_STATE_V2_SCHEMA_READY is not None:
         return CHAT_READ_STATE_V2_SCHEMA_READY
 
     with CHAT_READ_STATE_V2_SCHEMA_CHECK_LOCK:
         if CHAT_READ_STATE_V2_SCHEMA_READY is not None:
             return CHAT_READ_STATE_V2_SCHEMA_READY
 
         db = get_db()
         cur = db.cursor(dictionary=True)
         now = datetime.utcnow()
         try:
             cur.execute(
                 """
                 CREATE TABLE IF NOT EXISTS chat_read_state_v2 (
                   id BIGINT AUTO_INCREMENT PRIMARY KEY,
                   room_key VARCHAR(191) NOT NULL,
                   actor_key VARCHAR(64) NOT NULL,
                   last_read_message_id BIGINT NOT NULL DEFAULT 0,
                   created_at DATETIME NOT NULL,
                   updated_at DATETIME NOT NULL,
                   UNIQUE KEY uq_chat_read_state_v2_room_actor (room_key, actor_key),
                   KEY idx_chat_read_state_v2_room_last (room_key, last_read_message_id),
@@ -1583,329 +1449,50 @@ def _load_chat_read_state_v2_snapshot(room_key: str) -> list[dict[str, Any]]:
         db.close()
 
     merged: dict[str, int] = {}
     for row in rows:
         canonical_key = _canonical_read_actor_key(str(row.get("actor_key") or ""))
         if not canonical_key:
             continue
         read_id = int(row.get("last_read_message_id") or 0)
         merged[canonical_key] = max(merged.get(canonical_key, 0), read_id)
 
     snapshot: list[dict[str, Any]] = []
     for actor_key, last_read in merged.items():
         actor_type, actor_id = _split_read_actor_key(actor_key)
         snapshot.append(
             {
                 "actor_key": actor_key,
                 "actor_type": actor_type,
                 "actor_id": actor_id,
                 "display_name": _actor_key_to_display_name(actor_key),
                 "last_read_message_id": int(last_read),
             }
         )
     return snapshot
 
 
-def _debug_fetch_chat_read_state_v2_rows(room_key: str) -> list[dict[str, Any]]:
-    room_key = str(room_key or "").strip()
-    if not room_key or not _ensure_chat_read_state_v2_schema():
-        return []
-    db = get_db()
-    cur = db.cursor(dictionary=True)
-    try:
-        cur.execute(
-            """
-            SELECT actor_key, last_read_message_id, updated_at
-              FROM chat_read_state_v2
-             WHERE room_key=%s
-             ORDER BY updated_at DESC, actor_key ASC
-            """,
-            (room_key,),
-        )
-        rows = cur.fetchall() or []
-    finally:
-        cur.close()
-        db.close()
-
-    normalized: list[dict[str, Any]] = []
-    for row in rows:
-        raw_actor_key = str(row.get("actor_key") or "").strip()
-        canonical_actor_key = _canonical_read_actor_key(raw_actor_key)
-        actor_type, actor_id = _split_read_actor_key(raw_actor_key)
-        normalized.append(
-            {
-                "actor_key": raw_actor_key,
-                "canonical_actor_key": canonical_actor_key,
-                "actor_type": actor_type,
-                "actor_id": actor_id,
-                "last_read_message_id": int(row.get("last_read_message_id") or 0),
-                "updated_at": row.get("updated_at"),
-            }
-        )
-    return normalized
-
-
-def _debug_fetch_legacy_event_read_rows(event_id: int | None, room_id: str | None) -> list[dict[str, Any]]:
-    if not event_id or int(event_id) <= 0 or not str(room_id or "").strip() or not _ensure_chat_read_state_room_schema():
-        return []
-    db = get_db()
-    cur = db.cursor(dictionary=True)
-    try:
-        cur.execute(
-            """
-            SELECT actor_type, actor_id, last_read_message_id, updated_at
-              FROM chat_read_state
-             WHERE event_id=%s AND room_id=%s
-             ORDER BY updated_at DESC, actor_type ASC, actor_id ASC
-            """,
-            (int(event_id), str(room_id).strip()),
-        )
-        rows = cur.fetchall() or []
-    finally:
-        cur.close()
-        db.close()
-
-    result: list[dict[str, Any]] = []
-    for row in rows:
-        actor_type = str(row.get("actor_type") or "").strip()
-        actor_id = str(row.get("actor_id") or "").strip()
-        legacy_actor_key = f"{actor_type}:{actor_id}" if actor_type and actor_id else ""
-        result.append(
-            {
-                "actor_type": actor_type,
-                "actor_id": actor_id,
-                "legacy_actor_key": legacy_actor_key,
-                "canonical_legacy_actor_key": _canonical_read_actor_key(legacy_actor_key),
-                "last_read_message_id": int(row.get("last_read_message_id") or 0),
-                "updated_at": row.get("updated_at"),
-            }
-        )
-    return result
-
-
-def _debug_fetch_legacy_dm_read_rows(dm_uuid: str | None) -> tuple[list[dict[str, Any]], dict[str, Any]]:
-    uuid_value = str(dm_uuid or "").strip()
-    if not uuid_value or not _ensure_chat_dm_schema():
-        return [], {}
-
-    db = get_db()
-    cur = db.cursor(dictionary=True)
-    try:
-        cur.execute("SELECT id, uuid FROM chat_dm_conversations WHERE uuid=%s LIMIT 1", (uuid_value,))
-        conversation = cur.fetchone() or {}
-        conversation_id = int(conversation.get("id") or 0)
-        if conversation_id <= 0:
-            return [], {}
-
-        cur.execute("SHOW COLUMNS FROM chat_dm_participants LIKE 'updated_at'")
-        has_updated_at = bool(cur.fetchone())
-        updated_at_select = "updated_at" if has_updated_at else "NULL AS updated_at"
-        cur.execute(
-            f"""
-            SELECT actor_key,
-                   display_name_cache,
-                   last_read_message_id,
-                   created_at,
-                   {updated_at_select}
-              FROM chat_dm_participants
-             WHERE conversation_id=%s
-             ORDER BY created_at DESC, actor_key ASC
-            """,
-            (conversation_id,),
-        )
-        participant_rows = cur.fetchall() or []
-    finally:
-        cur.close()
-        db.close()
-
-    rows: list[dict[str, Any]] = []
-    for row in participant_rows:
-        actor_key = str(row.get("actor_key") or "").strip()
-        rows.append(
-            {
-                "actor_key": actor_key,
-                "canonical_actor_key": _canonical_read_actor_key(actor_key),
-                "display_name_cache": str(row.get("display_name_cache") or "").strip(),
-                "last_read_message_id": int(row.get("last_read_message_id") or 0),
-                "created_at": row.get("created_at"),
-                "updated_at": row.get("updated_at"),
-            }
-        )
-    return rows, {"id": int(conversation.get("id") or 0), "uuid": str(conversation.get("uuid") or "")}
-
-
-def _debug_fetch_presence_rows(actor_key: str, room_key: str) -> list[dict[str, Any]]:
-    actor_type, actor_id = _split_read_actor_key(actor_key)
-    room_key = str(room_key or "").strip()
-    if not actor_type or not actor_id or not room_key or not _ensure_chat_active_view_schema():
-        return []
-    db = get_db()
-    cur = db.cursor(dictionary=True)
-    try:
-        cur.execute(
-            """
-            SELECT actor_type, actor_id, room_id, client_id, is_visible,
-                   entered_at, last_ping_at, updated_at
-              FROM chat_active_views
-             WHERE actor_type=%s AND actor_id=%s AND room_id=%s
-             ORDER BY updated_at DESC
-            """,
-            (actor_type, actor_id, room_key),
-        )
-        return cur.fetchall() or []
-    finally:
-        cur.close()
-        db.close()
-
-
-def _debug_build_read_v2_checks(ctx: dict[str, Any]) -> list[dict[str, str]]:
-    checks: list[dict[str, str]] = []
-    room_key = str(ctx.get("room_key") or "")
-    effective_actor_key = str(ctx.get("effective_actor_key") or "")
-    aliases = [str(v) for v in (ctx.get("aliases") or []) if str(v)]
-    alias_canonical_set = {_canonical_read_actor_key(value) for value in aliases if _canonical_read_actor_key(value)}
-    v2_rows = list(ctx.get("v2_rows") or [])
-    snapshot_rows = list(ctx.get("snapshot_rows") or [])
-    legacy_rows = list(ctx.get("legacy_rows") or [])
-
-    if not room_key:
-        checks.append({"level": "error", "message": "room_key が空です。"})
-    if not effective_actor_key:
-        checks.append({"level": "error", "message": "effective_actor_key が空です。"})
-
-    if not v2_rows:
-        checks.append({"level": "warning", "message": "V2 に room_key 対象行がありません。"})
-
-    v2_alias_rows = [
-        row
-        for row in v2_rows
-        if _canonical_read_actor_key(str(row.get("actor_key") or "")) in alias_canonical_set
-    ]
-    if effective_actor_key and not v2_alias_rows:
-        checks.append({"level": "warning", "message": "effective_actor_key の alias に一致する V2 行がありません。"})
-
-    snapshot_effective_rows = [
-        row
-        for row in snapshot_rows
-        if _canonical_read_actor_key(str(row.get("actor_key") or "")) == effective_actor_key
-    ]
-    if v2_alias_rows and effective_actor_key and not snapshot_effective_rows:
-        checks.append({"level": "warning", "message": "V2 にはあるが snapshot には effective_actor_key がありません。"})
-
-    snapshot_mismatch_rows = [
-        row
-        for row in snapshot_rows
-        if str(row.get("actor_key") or "")
-        and _canonical_read_actor_key(str(row.get("actor_key") or "")) != str(row.get("actor_key") or "")
-    ]
-    if snapshot_mismatch_rows:
-        checks.append({"level": "warning", "message": "snapshot に canonical 後の actor_key がズレている行があります。"})
-
-    legacy_effective_rows = [
-        row
-        for row in legacy_rows
-        if _canonical_read_actor_key(str(row.get("canonical_legacy_actor_key") or row.get("canonical_actor_key") or "")) == effective_actor_key
-    ]
-    if legacy_effective_rows and not v2_alias_rows:
-        checks.append({"level": "warning", "message": "旧テーブルにはあるが V2 に存在しません。"})
-
-    if legacy_effective_rows and v2_alias_rows:
-        legacy_max = max(int(row.get("last_read_message_id") or 0) for row in legacy_effective_rows)
-        v2_max = max(int(row.get("last_read_message_id") or 0) for row in v2_alias_rows)
-        if legacy_max != v2_max:
-            checks.append({"level": "warning", "message": f"旧テーブル({legacy_max}) と V2({v2_max}) の last_read_message_id が不一致です。"})
-        if v2_max < legacy_max:
-            checks.append({"level": "warning", "message": f"V2 の last_read_message_id ({v2_max}) が期待値 ({legacy_max}) より小さいです。"})
-
-    if snapshot_effective_rows and v2_alias_rows:
-        snapshot_max = max(int(row.get("last_read_message_id") or 0) for row in snapshot_effective_rows)
-        v2_max = max(int(row.get("last_read_message_id") or 0) for row in v2_alias_rows)
-        if snapshot_max < v2_max:
-            checks.append({"level": "warning", "message": f"snapshot の last_read_message_id ({snapshot_max}) が V2 ({v2_max}) より小さいです。"})
-
-    if not checks:
-        checks.append({"level": "ok", "message": "目立った不整合は検出されませんでした。"})
-    return checks
-
-
-def _debug_collect_read_v2_context(
-    event_id: int | None = None,
-    room_id: str | None = None,
-    dm_uuid: str | None = None,
-    actor_key: str | None = None,
-) -> dict[str, Any]:
-    actor = get_chat_actor()
-    dm_uuid_value = str(dm_uuid or "").strip()
-    mode = "dm" if dm_uuid_value else "event"
-    parsed_event_id = int(event_id or 0) if mode == "event" else None
-    room_id_value = str(room_id or "").strip() if mode == "event" else ""
-    requested_actor_key = str(actor_key or "").strip()
-
-    effective_actor_key = _canonical_read_actor_key(requested_actor_key) if requested_actor_key else _canonical_read_actor_key_from_actor(actor)
-    room_key = _build_read_room_key(
-        event_id=parsed_event_id if mode == "event" else None,
-        room_id=room_id_value if mode == "event" else None,
-        dm_uuid=dm_uuid_value if mode == "dm" else None,
-    )
-    aliases = _read_actor_key_aliases(effective_actor_key)
-    split_type, split_id = _split_read_actor_key(effective_actor_key)
-
-    v2_rows = _debug_fetch_chat_read_state_v2_rows(room_key)
-    snapshot_rows = _load_chat_read_state_v2_snapshot(room_key)
-    conversation_meta: dict[str, Any] = {}
-    if mode == "dm":
-        legacy_rows, conversation_meta = _debug_fetch_legacy_dm_read_rows(dm_uuid_value)
-    else:
-        legacy_rows = _debug_fetch_legacy_event_read_rows(parsed_event_id, room_id_value)
-    presence_rows = _debug_fetch_presence_rows(effective_actor_key, room_key)
-
-    ctx: dict[str, Any] = {
-        "mode": mode,
-        "event_id": parsed_event_id,
-        "room_id": room_id_value,
-        "dm_uuid": dm_uuid_value,
-        "room_key": room_key,
-        "requested_actor_key": requested_actor_key,
-        "raw_actor": actor,
-        "effective_actor_key": effective_actor_key,
-        "effective_actor_type": split_type,
-        "effective_actor_id": split_id,
-        "split_actor_key": {"actor_type": split_type, "actor_id": split_id},
-        "aliases": aliases,
-        "v2_rows": v2_rows,
-        "v2_effective_rows": [row for row in v2_rows if _canonical_read_actor_key(str(row.get("actor_key") or "")) == effective_actor_key],
-        "snapshot_rows": snapshot_rows,
-        "snapshot_effective_rows": [
-            row for row in snapshot_rows if _canonical_read_actor_key(str(row.get("actor_key") or "")) == effective_actor_key
-        ],
-        "legacy_rows": legacy_rows,
-        "conversation_meta": conversation_meta,
-        "presence_rows": presence_rows,
-    }
-    ctx["checks"] = _debug_build_read_v2_checks(ctx)
-    return ctx
-
 
 def _log_auto_migration_error(name: str, sql: str, event_id: int | None = None) -> None:
     current_app.logger.warning(
         "chat auto-migration failed name=%s event_id=%s sql=%s",
         name,
         event_id,
         sql,
         exc_info=True,
     )
 
 
 def _ensure_chat_rooms_schema() -> bool:
     global CHAT_ROOMS_SCHEMA_READY
     if CHAT_ROOMS_SCHEMA_READY is not None:
         return CHAT_ROOMS_SCHEMA_READY
     with CHAT_ROOMS_SCHEMA_CHECK_LOCK:
         if CHAT_ROOMS_SCHEMA_READY is not None:
             return CHAT_ROOMS_SCHEMA_READY
         db = get_db()
         cur = db.cursor()
         try:
             sql = """
             CREATE TABLE IF NOT EXISTS chat_rooms (
               room_id VARCHAR(36) PRIMARY KEY,
               event_id BIGINT NOT NULL,
@@ -5781,171 +5368,61 @@ def _build_admin_dm_inbox_items(actor_key: str) -> list[dict[str, Any]]:
         nickname = str(user.get("nickname") or "").strip() or peer_actor_key
         rows_by_peer[peer_actor_key] = {
             "id": None,
             "uuid": None,
             "dm_type": "admin_user",
             "last_message_at": None,
             "last_read_message_id": None,
             "last_message": "",
             "unread_count": 0,
             "peer_actor_key": peer_actor_key,
             "peer_display_name": nickname,
         }
 
     items = list(rows_by_peer.values())
     items.sort(
         key=lambda row: (
             0 if int(row.get("unread_count") or 0) > 0 else 1,
             -int(row.get("unread_count") or 0),
             -(int(row["last_message_at"].timestamp()) if isinstance(row.get("last_message_at"), datetime) else 0),
             str(row.get("peer_display_name") or ""),
         )
     )
     return items
 
 
-def _parse_debug_int(value: Any) -> int | None:
-    text = str(value or "").strip()
-    if not text:
-        return None
-    try:
-        parsed = int(text)
-    except Exception:
-        return None
-    return parsed if parsed > 0 else None
-
 
 @chat_bp.before_request
 def _require_any_login():
     if request.endpoint in {"chat.manifest", "chat.sw", "chat.static"}:
         return None
     actor = get_chat_actor()
     if not actor:
         return jsonify({"ok": False, "error": "ログインが必要です"}), 403
     return None
 
 
-@chat_bp.get("/debug/read-v2")
-def debug_read_v2():
-    actor = get_chat_actor()
-    if not _is_admin_actor(actor):
-        abort(403)
-
-    event_id = _parse_debug_int(request.args.get("event_id"))
-    room_id = (request.args.get("room_id") or "").strip()
-    dm_uuid = (request.args.get("dm_uuid") or "").strip()
-    room_key = (request.args.get("room_key") or "").strip()
-    requested_actor_key = (request.args.get("actor_key") or "").strip()
-
-    warnings: list[str] = []
-    room_key_parse = _parse_read_room_key(room_key)
-    if room_key and room_key_parse.get("mode") == "":
-        warnings.append("room_key を解析できませんでした。event / dm の入力または候補一覧から選択してください。")
-    if room_key_parse.get("mode") == "dm":
-        dm_uuid = str(room_key_parse.get("dm_uuid") or "")
-        event_id = None
-        room_id = ""
-    elif room_key_parse.get("mode") == "event":
-        event_id = int(room_key_parse.get("event_id") or 0) or None
-        room_id = str(room_key_parse.get("room_id") or "")
-        dm_uuid = ""
-
-    recent_event_rooms: list[dict[str, Any]] = []
-    recent_dm_rooms: list[dict[str, Any]] = []
-    try:
-        recent_event_rooms = _debug_list_recent_event_rooms(limit=50)
-    except Exception:
-        current_app.logger.warning("debug read-v2 failed to load recent event rooms", exc_info=True)
-        warnings.append("最近のイベントルーム取得に失敗しました。")
-    try:
-        recent_dm_rooms = _debug_list_recent_dm_rooms(limit=50)
-    except Exception:
-        current_app.logger.warning("debug read-v2 failed to load recent DM rooms", exc_info=True)
-        warnings.append("最近のDM取得に失敗しました。")
-
-    ctx = _debug_collect_read_v2_context(
-        event_id=event_id,
-        room_id=room_id,
-        dm_uuid=dm_uuid,
-        actor_key=requested_actor_key,
-    )
-    return render_template(
-        "chat/debug_read_v2.html",
-        actor=actor,
-        ctx=ctx,
-        mode=ctx.get("mode"),
-        event_id=ctx.get("event_id") or "",
-        room_id=ctx.get("room_id") or "",
-        dm_uuid=ctx.get("dm_uuid") or "",
-        room_key=room_key,
-        requested_actor_key=ctx.get("requested_actor_key") or "",
-        recent_event_rooms=recent_event_rooms,
-        recent_dm_rooms=recent_dm_rooms,
-        warnings=warnings,
-        test_message_id=(request.args.get("test_message_id") or "").strip(),
-    )
-
-
-@chat_bp.post("/debug/read-v2/upsert")
-def debug_read_v2_upsert():
-    actor = get_chat_actor()
-    if not _is_admin_actor(actor):
-        abort(403)
-
-    event_id = _parse_debug_int(request.form.get("event_id"))
-    room_id = (request.form.get("room_id") or "").strip()
-    dm_uuid = (request.form.get("dm_uuid") or "").strip()
-    message_id = _parse_debug_int(request.form.get("message_id"))
-    raw_actor_key = (request.form.get("actor_key") or "").strip()
-
-    effective_actor_key = _canonical_read_actor_key(raw_actor_key) if raw_actor_key else _canonical_read_actor_key_from_actor(actor)
-    room_key = _build_read_room_key(
-        event_id=event_id if not dm_uuid else None,
-        room_id=room_id if not dm_uuid else None,
-        dm_uuid=dm_uuid or None,
-    )
-
-    saved_value = 0
-    if room_key and effective_actor_key and message_id:
-        saved_value = _upsert_chat_read_state_v2(room_key, effective_actor_key, int(message_id))
-        if saved_value > 0:
-            flash(f"V2へ保存テストを実行しました。saved={saved_value}", "success")
-        else:
-            flash("V2へ保存テストを実行しましたが保存値を取得できませんでした。", "warning")
-    else:
-        flash("入力が不足しています（room_key / actor_key / message_id）。", "danger")
-
-    params = {
-        "event_id": event_id or "",
-        "room_id": room_id,
-        "dm_uuid": dm_uuid,
-        "actor_key": effective_actor_key,
-        "test_message_id": message_id or "",
-        "saved_value": saved_value,
-    }
-    return redirect(url_for("chat.debug_read_v2", **params))
-
 
 @chat_bp.route("/")
 def index():
     actor = get_chat_actor()
     if not actor:
         abort(403)
     events = _accessible_events(actor)
     dm_inbox_items: list[dict[str, Any]] = []
     if _is_chat_admin_actor(actor) and _ensure_chat_dm_schema():
         actor_key = get_chat_actor_key(actor) or "admin:1"
         dm_inbox_items = _build_admin_dm_inbox_items(actor_key)
     notif_ctx = _build_chat_notification_context(actor)
     return render_template(
         "chat/index.html",
         actor=actor,
         events=events,
         csrf_token=_chat_csrf(),
         nav_mode="chat",
         dm_inbox_items=dm_inbox_items,
         dm_user_user_enabled=_chat_dm_enable_user_user(),
         vapid_public_key=os.getenv("CHAT_VAPID_PUBLIC_KEY", ""),
         **notif_ctx,
     )
 
 
@@ -6016,57 +5493,55 @@ def dm_room(uuid: str):
     if not can_access_dm(uuid, actor_key):
         abort(403)
 
     conversation = _get_dm_conversation_by_uuid(uuid)
     if not conversation:
         abort(404)
 
     db = get_db()
     cur = db.cursor(dictionary=True)
     try:
         cur.execute(
             "SELECT actor_key FROM chat_dm_participants WHERE conversation_id=%s AND actor_key<>%s LIMIT 1",
             (conversation["id"], actor_key),
         )
         peer = cur.fetchone() or {}
     finally:
         cur.close()
         db.close()
 
     messages = _load_dm_messages(int(conversation["id"]), actor_key, uuid)
     peer_name = _actor_key_to_display_name(str(peer.get("actor_key") or ""))
     notif_ctx = _build_chat_notification_context(actor)
     return render_template(
         "chat/room.html",
         actor=actor,
-        chat_read_front_debug_enabled=_is_chat_admin_actor(actor),
         current_user_id=_canonical_read_actor_key_from_actor(actor),
         event={"id": 0, "title": "DM"},
         messages=messages,
         vapid_public_key=os.getenv("CHAT_VAPID_PUBLIC_KEY", ""),
         csrf_token=_chat_csrf(),
-        can_broadcast=False,
         can_manage_rooms=False,
         accessible_rooms=[],
         active_room={"room_id": f"dm:{uuid}", "room_name": peer_name, "is_main": 1},
         default_avatar_url=_default_avatar_url(),
         nav_mode="chat",
         dm_mode=True,
         dm_uuid=uuid,
         dm_room_id=f"dm:{uuid}",
         peer_display_name=peer_name,
         **notif_ctx,
     )
 
 
 @chat_bp.post("/dm/api/send")
 def dm_api_send():
     actor = get_chat_actor()
     actor_key = _get_dm_actor_key(actor)
     if not actor or not actor_key:
         return jsonify({"ok": False, "error": "forbidden"}), 403
     payload = request.get_json(silent=True) or {}
     if (payload.get("csrf_token") or "").strip() != session.get("chat_csrf"):
         return jsonify({"ok": False, "error": "csrf"}), 400
     dm_uuid = str(payload.get("dm_uuid") or "").strip()
     if not dm_uuid:
         return jsonify({"ok": False, "error": "dm_uuid_required"}), 400
@@ -6528,64 +6003,61 @@ def room(event_id: int):
     _ensure_chat_read_state_v2_schema()
     _ensure_chat_delete_schema()
     _ensure_chat_edit_schema()
 
     requested_room_id = (request.args.get("room_id") or "").strip() or None
     allowed, effective_room_id, active_room = _can_access_room(event_id, requested_room_id, actor)
     if not allowed or not effective_room_id or not active_room:
         abort(403)
 
     event = _get_event(event_id)
     if not event:
         abort(404)
     avatar_cache: dict[str, str] = {}
     raw_messages = _load_messages(event_id, effective_room_id)
     message_ids = [int(m.get("id") or 0) for m in raw_messages if int(m.get("id") or 0) > 0]
     reaction_summary_by_message = _load_reactions_by_message_ids(message_ids)
     my_reaction_by_message = _load_my_reactions_by_message_ids(message_ids, actor)
     images_by_message = _load_message_images_by_message_ids(message_ids)
     messages = []
     for message in raw_messages:
         message_id = int(message.get("id") or 0)
         message["reactions_summary"] = reaction_summary_by_message.get(message_id, [])
         message["my_reaction"] = my_reaction_by_message.get(message_id)
         message["images"] = images_by_message.get(message_id) or _fallback_images_from_message(message)
         messages.append(_present_message(message, actor, avatar_cache=avatar_cache))
-    can_broadcast = actor["actor_type"] in {"admin", "acl"}
     accessible_rooms = _list_accessible_rooms(event_id, actor)
     current_user_id = _canonical_read_actor_key_from_actor(actor)
     notif_ctx = _build_chat_notification_context(actor)
     return render_template(
         "chat/room.html",
         actor=actor,
-        chat_read_front_debug_enabled=_is_chat_admin_actor(actor),
         current_user_id=current_user_id,
         event=event,
         messages=messages,
         vapid_public_key=os.getenv("CHAT_VAPID_PUBLIC_KEY", ""),
         csrf_token=_chat_csrf(),
-        can_broadcast=can_broadcast,
         can_manage_rooms=_can_manage_rooms(event_id, actor),
         accessible_rooms=accessible_rooms,
         active_room=active_room,
         default_avatar_url=_default_avatar_url(),
         nav_mode="chat",
         dm_mode=False,
         dm_uuid="",
         dm_room_id="",
         peer_display_name="",
         **notif_ctx,
     )
 
 
 @chat_bp.get("/events/<int:event_id>/images/<path:name>")
 def chat_image(event_id: int, name: str):
     actor = get_chat_actor()
     if not actor:
         abort(403)
     if not _can_access_event(event_id, actor):
         abort(403)
     if "/" in name or "\\" in name or name.startswith("."):
         abort(404)
     directory = os.path.join(CHAT_UPLOAD_DIR, str(event_id))
     return send_from_directory(directory, name)
 
@@ -8047,104 +7519,50 @@ def push_unsubscribe():
             if source_ext_user_id:
                 cur.execute(
                     """
                     DELETE FROM chat_push_subscriptions
                      WHERE actor_type='line'
                        AND actor_id=%s
                        AND endpoint_hash=%s
                     """,
                     (source_ext_user_id, endpoint_hash),
                 )
                 alias_cleanup_deleted = int(cur.rowcount or 0)
         current_app.logger.info(
             "chat push unsubscribe actor_type=%s actor_id=%s endpoint_hash_prefix=%s alias_cleanup_deleted=%s",
             actor.get("actor_type"),
             actor.get("actor_id"),
             (endpoint_hash or "")[:10],
             alias_cleanup_deleted,
         )
         db.commit()
     finally:
         cur.close()
         db.close()
     return jsonify({"ok": True})
 
 
-@chat_bp.post("/api/events/<int:event_id>/broadcast")
-def broadcast_push(event_id: int):
-    actor = get_chat_actor()
-    if not actor or actor["actor_type"] == "line":
-        abort(403)
-    if not _can_access_event(event_id, actor):
-        abort(403)
-    token = (request.form.get("csrf_token") or "").strip()
-    if token != session.get("chat_csrf"):
-        abort(400)
-
-    msg = _validate_body(request.form.get("body") or "")
-    db = get_db()
-    cur = db.cursor(dictionary=True)
-    sent_count = 0
-    try:
-        cur.execute("SELECT user_id FROM mfu_event_member WHERE event_id=%s", (event_id,))
-        for row in cur.fetchall() or []:
-            sent_count += _send_push_to_actor(
-                "line",
-                str(row["user_id"]),
-                {
-                    "title": "イベント通知",
-                    "body": msg,
-                    "event_id": event_id,
-                    "url": f"/chat/events/{event_id}?{urlencode({'event_id': event_id, 'room_id': effective_room_id})}",
-                },
-            )
-    finally:
-        cur.close()
-        db.close()
-    db = get_db()
-    cur = db.cursor(dictionary=True)
-    try:
-        cur.execute("SELECT user_id FROM mfu_event_member WHERE event_id=%s", (event_id,))
-        for row in cur.fetchall() or []:
-            recipient = int(row["user_id"])
-            digest = hashlib.sha256(msg.encode("utf-8")).hexdigest()[:16]
-            _create_external_chat_notification(
-                recipient_user_id=recipient,
-                kind="chat_broadcast",
-                title="イベント通知",
-                body=msg,
-                event_id=event_id,
-                room_id=None,
-                room_name=None,
-                dedup_key=f"chat:broadcast:{event_id}:{digest}:{recipient}",
-            )
-    finally:
-        cur.close()
-        db.close()
-    _log_notification(event_id, "broadcast", {"body": msg}, sent_count)
-    return jsonify({"ok": True, "sent_count": sent_count})
-
 
 @chat_bp.get("/manifest.json")
 def manifest():
     return jsonify(
         {
             "name": "MFU Event Chat",
             "short_name": "MFU Chat",
             "start_url": "/chat/",
             "display": "standalone",
             "background_color": "#ffffff",
             "theme_color": "#0d6efd",
             "icons": [],
         }
     )
 
 
 
 @socketio.on("connect")
 def chat_connect():
     actor = get_chat_actor()
     _audit_log("room_join", actor=_actor_log_id(actor), result="connect")
     if not actor:
         current_app.logger.info("chat socket connect denied: no actor")
         return False
     ext_user_joined = False
diff --git a/chat/templates/chat/debug_read_v2.html b/chat/templates/chat/debug_read_v2.html
deleted file mode 100644
index 0a9061dd1e03919e93ccc2578f93af32d2be6f7c..0000000000000000000000000000000000000000
--- a/chat/templates/chat/debug_read_v2.html
+++ /dev/null
@@ -1,276 +0,0 @@
-{% extends "chat/base_chat.html" %}
-{% block content %}
-<div class="container py-4">
-  <style>
-    .debug-target-row.active { background: #fff3cd; }
-  </style>
-  <h1 class="mb-3">既読V2 デバッグ</h1>
-
-  <div class="card mb-4 border-2">
-    <div class="card-body">
-      <div class="row g-3">
-        <div class="col-md-4">
-          <div class="text-muted">mode</div>
-          <div class="fs-5 fw-bold">{{ ctx.mode }}</div>
-        </div>
-        <div class="col-md-4">
-          <div class="text-muted">room_key</div>
-          <div class="fs-5 fw-bold text-break">{{ ctx.room_key or '-' }}</div>
-        </div>
-        <div class="col-md-4">
-          <div class="text-muted">effective actor_key</div>
-          <div class="fs-5 fw-bold text-break">{{ ctx.effective_actor_key or '-' }}</div>
-        </div>
-      </div>
-      {% if request.args.get('saved_value') %}
-      <div class="alert alert-success mt-3 mb-0">actual saved value: {{ request.args.get('saved_value') }}</div>
-      {% endif %}
-    </div>
-  </div>
-
-  {% with flashes = get_flashed_messages(with_categories=true) %}
-  {% if flashes %}
-    {% for category, message in flashes %}
-      <div class="alert alert-{{ 'danger' if category in ['error', 'danger'] else ('warning' if category == 'warning' else 'success') }}">{{ message }}</div>
-    {% endfor %}
-  {% endif %}
-  {% endwith %}
-
-  {% for warning in warnings or [] %}
-  <div class="alert alert-warning">{{ warning }}</div>
-  {% endfor %}
-
-  <div class="card mb-4">
-    <div class="card-header fw-bold">デバッグ対象選択</div>
-    <div class="card-body">
-      <div class="row g-3 mb-3">
-        <div class="col-lg-6">
-          <form method="get" action="{{ url_for('chat.debug_read_v2') }}" class="border rounded p-3">
-            <div class="fw-bold mb-2">イベント既読</div>
-            <div class="mb-2"><label class="form-label">event_id</label><input class="form-control" name="event_id" value="{{ event_id }}"></div>
-            <div class="mb-2"><label class="form-label">room_id</label><input class="form-control" name="room_id" value="{{ room_id }}"></div>
-            <div class="mb-2"><label class="form-label">actor_key (任意)</label><input class="form-control" name="actor_key" value="{{ requested_actor_key }}"></div>
-            <button class="btn btn-primary">イベント既読を開く</button>
-          </form>
-        </div>
-        <div class="col-lg-6">
-          <form method="get" action="{{ url_for('chat.debug_read_v2') }}" class="border rounded p-3">
-            <div class="fw-bold mb-2">DM既読</div>
-            <div class="mb-2"><label class="form-label">dm_uuid</label><input class="form-control" name="dm_uuid" value="{{ dm_uuid }}"></div>
-            <div class="mb-2"><label class="form-label">actor_key (任意)</label><input class="form-control" name="actor_key" value="{{ requested_actor_key }}"></div>
-            <button class="btn btn-primary">DM既読を開く</button>
-          </form>
-        </div>
-      </div>
-
-      <div class="mb-3 border rounded p-3">
-        <div class="fw-bold mb-2">room_key 直指定</div>
-        <form method="get" action="{{ url_for('chat.debug_read_v2') }}" class="row g-2 align-items-end">
-          <div class="col-md-8"><label class="form-label">room_key</label><input class="form-control" name="room_key" value="{{ room_key or '' }}" placeholder="event:123:room:main / dm:uuid"></div>
-          <div class="col-md-4"><button class="btn btn-outline-primary w-100">room_keyで開く</button></div>
-        </form>
-      </div>
-
-      <div class="fw-bold mb-2">最近のイベントルーム</div>
-      <div class="table-responsive mb-4">
-        <table class="table table-sm table-striped table-bordered align-middle">
-          <thead><tr><th>event_id</th><th>room_id</th><th>room_name</th><th>latest_message_id</th><th>latest_message_at</th><th>room_key</th><th></th></tr></thead>
-          <tbody>
-            {% for row in recent_event_rooms %}
-            <tr class="debug-target-row {% if row.room_key == ctx.room_key %}active{% endif %}">
-              <td>{{ row.event_id }}</td>
-              <td>{{ row.room_id }}</td>
-              <td>{{ row.room_name }}</td>
-              <td>{{ row.latest_message_id or '-' }}</td>
-              <td>{{ row.latest_message_at or '-' }}</td>
-              <td class="text-break">{{ row.room_key }}</td>
-              <td><a class="btn btn-sm btn-outline-primary" href="{{ url_for('chat.debug_read_v2', event_id=row.event_id, room_id=row.room_id, actor_key=requested_actor_key) }}">開く</a></td>
-            </tr>
-            {% else %}
-            <tr><td colspan="7" class="text-muted">候補なし</td></tr>
-            {% endfor %}
-          </tbody>
-        </table>
-      </div>
-
-      <div class="fw-bold mb-2">最近のDM</div>
-      <div class="table-responsive">
-        <table class="table table-sm table-striped table-bordered align-middle">
-          <thead><tr><th>dm_uuid</th><th>conversation_id</th><th>dm_type</th><th>peer_display_name</th><th>latest_message_id</th><th>latest_message_at</th><th>room_key</th><th></th></tr></thead>
-          <tbody>
-            {% for row in recent_dm_rooms %}
-            <tr class="debug-target-row {% if row.room_key == ctx.room_key %}active{% endif %}">
-              <td class="text-break">{{ row.dm_uuid }}</td>
-              <td>{{ row.conversation_id }}</td>
-              <td>{{ row.dm_type or '-' }}</td>
-              <td>{{ row.peer_display_name or row.peer_actor_key or '-' }}</td>
-              <td>{{ row.latest_message_id or '-' }}</td>
-              <td>{{ row.latest_message_at or '-' }}</td>
-              <td class="text-break">{{ row.room_key }}</td>
-              <td><a class="btn btn-sm btn-outline-primary" href="{{ url_for('chat.debug_read_v2', dm_uuid=row.dm_uuid, actor_key=requested_actor_key) }}">開く</a></td>
-            </tr>
-            {% else %}
-            <tr><td colspan="8" class="text-muted">候補なし</td></tr>
-            {% endfor %}
-          </tbody>
-        </table>
-      </div>
-    </div>
-  </div>
-
-  <div class="card mb-4">
-    <div class="card-header fw-bold">F. 自動判定</div>
-    <div class="card-body">
-      {% for check in ctx.checks %}
-        <div class="alert py-2 mb-2 {% if check.level == 'error' %}alert-danger{% elif check.level == 'warning' %}alert-warning{% else %}alert-success{% endif %}">
-          <strong>[{{ check.level|upper }}]</strong> {{ check.message }}
-        </div>
-      {% endfor %}
-    </div>
-  </div>
-
-  <div class="card mb-4">
-    <div class="card-header fw-bold">A. リクエスト情報</div>
-    <div class="card-body">
-      <form method="get" action="{{ url_for('chat.debug_read_v2') }}" class="row g-2 align-items-end mb-3">
-        <div class="col-md-2"><label class="form-label">event_id</label><input class="form-control" name="event_id" value="{{ event_id }}"></div>
-        <div class="col-md-2"><label class="form-label">room_id</label><input class="form-control" name="room_id" value="{{ room_id }}"></div>
-        <div class="col-md-3"><label class="form-label">dm_uuid</label><input class="form-control" name="dm_uuid" value="{{ dm_uuid }}"></div>
-        <div class="col-md-3"><label class="form-label">actor_key</label><input class="form-control" name="actor_key" value="{{ requested_actor_key }}"></div>
-        <div class="col-md-2"><button class="btn btn-primary w-100">再読込</button></div>
-      </form>
-      <table class="table table-sm table-bordered">
-        <tr><th>mode</th><td>{{ ctx.mode }}</td></tr>
-        <tr><th>event_id</th><td>{{ ctx.event_id or '-' }}</td></tr>
-        <tr><th>room_id</th><td>{{ ctx.room_id or '-' }}</td></tr>
-        <tr><th>dm_uuid</th><td>{{ ctx.dm_uuid or '-' }}</td></tr>
-        <tr><th>requested_actor_key</th><td>{{ ctx.requested_actor_key or '-' }}</td></tr>
-        <tr><th>effective_actor_key</th><td>{{ ctx.effective_actor_key or '-' }}</td></tr>
-        <tr><th>room_key</th><td>{{ ctx.room_key or '-' }}</td></tr>
-      </table>
-    </div>
-  </div>
-
-  <div class="card mb-4">
-    <div class="card-header fw-bold">B. actor 情報</div>
-    <div class="card-body">
-      <table class="table table-sm table-bordered">
-        <tr><th>canonical read actor_key</th><td>{{ ctx.effective_actor_key or '-' }}</td></tr>
-        <tr><th>split result</th><td>{{ ctx.split_actor_key.actor_type }} / {{ ctx.split_actor_key.actor_id }}</td></tr>
-        <tr><th>aliases</th><td>{{ ctx.aliases|join(', ') or '-' }}</td></tr>
-      </table>
-      <div class="fw-bold mb-1">raw actor object</div>
-      <pre class="bg-light p-2 border rounded">{{ ctx.raw_actor|tojson(indent=2) }}</pre>
-    </div>
-  </div>
-
-  <div class="card mb-4">
-    <div class="card-header fw-bold">C. DB生データ（V2）</div>
-    <div class="card-body">
-      <table class="table table-sm table-striped table-bordered">
-        <thead><tr><th>actor_key</th><th>canonical_actor_key</th><th>last_read_message_id</th><th>updated_at</th></tr></thead>
-        <tbody>
-          {% for row in ctx.v2_rows %}
-          <tr class="{% if row.canonical_actor_key == ctx.effective_actor_key %}table-warning{% endif %}">
-            <td>{{ row.actor_key }}</td>
-            <td>{{ row.canonical_actor_key }}</td>
-            <td>{{ row.last_read_message_id }}</td>
-            <td>{{ row.updated_at or '-' }}</td>
-          </tr>
-          {% else %}
-          <tr><td colspan="4" class="text-muted">データなし</td></tr>
-          {% endfor %}
-        </tbody>
-      </table>
-    </div>
-  </div>
-
-  <div class="card mb-4">
-    <div class="card-header fw-bold">D. snapshot 結果</div>
-    <div class="card-body">
-      <table class="table table-sm table-striped table-bordered">
-        <thead><tr><th>actor_key</th><th>actor_type</th><th>actor_id</th><th>display_name</th><th>last_read_message_id</th></tr></thead>
-        <tbody>
-          {% for row in ctx.snapshot_rows %}
-          <tr class="{% if row.actor_key == ctx.effective_actor_key %}table-warning{% endif %}">
-            <td>{{ row.actor_key }}</td><td>{{ row.actor_type }}</td><td>{{ row.actor_id }}</td><td>{{ row.display_name }}</td><td>{{ row.last_read_message_id }}</td>
-          </tr>
-          {% else %}
-          <tr><td colspan="5" class="text-muted">データなし</td></tr>
-          {% endfor %}
-        </tbody>
-      </table>
-    </div>
-  </div>
-
-  <div class="card mb-4">
-    <div class="card-header fw-bold">E. 旧テーブル比較</div>
-    <div class="card-body">
-      {% if ctx.mode == 'event' %}
-      <table class="table table-sm table-striped table-bordered">
-        <thead><tr><th>actor_type</th><th>actor_id</th><th>legacy_actor_key</th><th>canonical_legacy_actor_key</th><th>last_read_message_id</th><th>updated_at</th></tr></thead>
-        <tbody>
-          {% for row in ctx.legacy_rows %}
-          <tr>
-            <td>{{ row.actor_type }}</td><td>{{ row.actor_id }}</td><td>{{ row.legacy_actor_key }}</td><td>{{ row.canonical_legacy_actor_key }}</td><td>{{ row.last_read_message_id }}</td><td>{{ row.updated_at or '-' }}</td>
-          </tr>
-          {% else %}
-          <tr><td colspan="6" class="text-muted">データなし</td></tr>
-          {% endfor %}
-        </tbody>
-      </table>
-      {% else %}
-      <div class="mb-2"><strong>chat_dm_conversations</strong>: uuid={{ ctx.conversation_meta.uuid or '-' }}, id={{ ctx.conversation_meta.id or '-' }}</div>
-      <table class="table table-sm table-striped table-bordered">
-        <thead><tr><th>actor_key</th><th>display_name_cache</th><th>canonical_actor_key</th><th>last_read_message_id</th><th>created_at</th><th>updated_at</th></tr></thead>
-        <tbody>
-          {% for row in ctx.legacy_rows %}
-          <tr>
-            <td>{{ row.actor_key }}</td><td>{{ row.display_name_cache or '-' }}</td><td>{{ row.canonical_actor_key }}</td><td>{{ row.last_read_message_id }}</td><td>{{ row.created_at or '-' }}</td><td>{{ row.updated_at or '-' }}</td>
-          </tr>
-          {% else %}
-          <tr><td colspan="6" class="text-muted">データなし</td></tr>
-          {% endfor %}
-        </tbody>
-      </table>
-      {% endif %}
-    </div>
-  </div>
-
-  <div class="card mb-4">
-    <div class="card-header fw-bold">G. 手動テスト</div>
-    <div class="card-body">
-      <form method="post" action="{{ url_for('chat.debug_read_v2_upsert') }}" class="row g-2 align-items-end">
-        <input type="hidden" name="event_id" value="{{ event_id }}">
-        <input type="hidden" name="room_id" value="{{ room_id }}">
-        <input type="hidden" name="dm_uuid" value="{{ dm_uuid }}">
-        <input type="hidden" name="actor_key" value="{{ ctx.effective_actor_key }}">
-        <div class="col-md-4">
-          <label class="form-label">message_id</label>
-          <input class="form-control" name="message_id" value="{{ test_message_id }}" placeholder="例: 12345">
-        </div>
-        <div class="col-md-3"><button class="btn btn-danger w-100" type="submit">V2へ保存テスト</button></div>
-        <div class="col-md-3"><a class="btn btn-secondary w-100" href="{{ url_for('chat.debug_read_v2', event_id=event_id, room_id=room_id, dm_uuid=dm_uuid, actor_key=ctx.effective_actor_key) }}">再読込</a></div>
-      </form>
-    </div>
-  </div>
-
-  <div class="card mb-4">
-    <div class="card-header fw-bold">Presence (chat_active_views)</div>
-    <div class="card-body">
-      <table class="table table-sm table-striped table-bordered">
-        <thead><tr><th>actor_type</th><th>actor_id</th><th>room_id</th><th>client_id</th><th>is_visible</th><th>entered_at</th><th>last_ping_at</th><th>updated_at</th></tr></thead>
-        <tbody>
-          {% for row in ctx.presence_rows %}
-          <tr>
-            <td>{{ row.actor_type }}</td><td>{{ row.actor_id }}</td><td>{{ row.room_id }}</td><td>{{ row.client_id }}</td><td>{{ row.is_visible }}</td><td>{{ row.entered_at or '-' }}</td><td>{{ row.last_ping_at or '-' }}</td><td>{{ row.updated_at or '-' }}</td>
-          </tr>
-          {% else %}
-          <tr><td colspan="8" class="text-muted">データなし</td></tr>
-          {% endfor %}
-        </tbody>
-      </table>
-    </div>
-  </div>
-</div>
-{% endblock %}
diff --git a/chat/templates/chat/room.html b/chat/templates/chat/room.html
index 27024e2530d35c2f1cdd9869a835e99ed174039b..7a7c8d63040646ee85059552f562d29774cabf9c 100644
--- a/chat/templates/chat/room.html
+++ b/chat/templates/chat/room.html
@@ -1408,159 +1408,84 @@
   top: 6px;
   right: 6px;
   border: 0;
   background: rgba(0,0,0,0.55);
   color: #fff;
   width: 28px;
   height: 28px;
   border-radius: 999px;
   line-height: 1;
 }
 
 .chat-upload-preview-modal .foot {
   padding: 12px 14px;
   border-top: 1px solid #e6e9f4;
   display: flex;
   justify-content: flex-end;
   gap: 8px;
 }
 
 .chat-upload-hint {
   font-size: 0.82rem;
   color: #667;
 }
 
 
-.chat-read-front-debug-toggle.is-on {
-  background: #dff3ff;
-  border-color: #8ec8ff;
-  color: #1d4d87;
-}
-
-.chat-read-front-debug {
-  margin-top: 6px;
-  padding: 6px 8px;
-  border-radius: 8px;
-  background: rgba(226, 236, 245, 0.95);
-  border: 1px solid rgba(68, 98, 128, 0.25);
-  font-size: 0.72rem;
-  color: #2f3f55;
-}
-
-.chat-read-front-debug-row {
-  display: flex;
-  flex-wrap: wrap;
-  gap: 6px;
-  margin-bottom: 4px;
-}
-
-.chat-read-front-debug-badge {
-  display: inline-block;
-  border-radius: 999px;
-  padding: 1px 7px;
-  font-size: 0.68rem;
-  font-weight: 600;
-  border: 1px solid transparent;
-}
-
-.chat-read-front-debug-badge.ok { background: #e8f8ec; color: #1f6b35; border-color: #a8dbb8; }
-.chat-read-front-debug-badge.warning { background: #fff4da; color: #7b5600; border-color: #f0ce7d; }
-.chat-read-front-debug-badge.hidden { background: #ffe8e8; color: #8e2222; border-color: #f0b3b3; }
-
-.chat-read-front-debug-panel {
-  position: fixed;
-  right: 12px;
-  bottom: 12px;
-  width: min(420px, 92vw);
-  max-height: 52vh;
-  overflow: auto;
-  z-index: 1080;
-  background: rgba(23, 28, 38, 0.92);
-  color: #d9e7ff;
-  border-radius: 10px;
-  border: 1px solid rgba(127, 166, 230, 0.35);
-  padding: 10px;
-  font-size: 0.72rem;
-}
-
-.chat-read-front-debug-log {
-  margin-top: 8px;
-  max-height: 220px;
-  overflow: auto;
-  border-top: 1px solid rgba(127, 166, 230, 0.25);
-  padding-top: 6px;
-}
-
-.chat-read-front-debug-json {
-  white-space: pre-wrap;
-  word-break: break-all;
-  background: rgba(255,255,255,0.08);
-  border-radius: 6px;
-  padding: 6px;
-  margin-top: 4px;
-}
-
 </style>
 
 {% macro render_reaction_box(message) -%}
   <div class="chat-reaction-box"
        data-reaction-box="1"
        data-my-reaction="{{ message.my_reaction or '' }}"
        data-reactions-summary='{{ (message.reactions_summary or [])|tojson }}'>
     <button type="button" class="chat-reaction-toggle react-open-btn" aria-label="リアクション" title="リアクション">+</button>
     <div class="chat-reaction-panel d-none"></div>
     <div class="chat-reaction-summary"></div>
   </div>
 {%- endmacro %}
 
 <div class="container py-3 chat-page"
      data-event-id="{{ event.id }}"
      data-room-id="{{ active_room.room_id }}"
      data-csrf="{{ csrf_token }}"
      data-vapid="{{ vapid_public_key }}"
      data-me="{{ current_user_id or me_user_id or '' }}"
      data-display-name="{{ actor.display_name }}"
      data-default-avatar="{{ default_avatar_url }}"
      data-is-admin="{{ 1 if actor.actor_type == 'admin' else 0 }}"
      data-dm-mode="{{ 1 if dm_mode else 0 }}"
      data-dm-uuid="{{ dm_uuid or '' }}"
      data-dm-room-id="{{ dm_room_id or '' }}"
-     data-read-front-debug-enabled="{{ 1 if chat_read_front_debug_enabled else 0 }}"
-     data-room-debug-key="{% if dm_mode %}dm:{{ dm_uuid }}{% else %}event:{{ event.id }}:room:{{ active_room.room_id }}{% endif %}"
      data-title-base="{% if dm_mode %}{{ peer_display_name }}{% else %}{{ event.title }}{% endif %}"
      data-notification-scope="{{ notification_scope|default('external') }}"
      data-unread-api="{{ (notification_api_map.unread_count if notification_api_map else '/external-login/api/notifications/unread-count') }}"
      data-room-read-api="{{ (notification_api_map.room_read if notification_api_map else '/external-login/api/notifications/read-by-room') }}"
      data-dm-room-read-api="{{ (notification_api_map.dm_room_read if notification_api_map else '/external-login/api/notifications/read-dm-room') }}">
   {% include "chat/_chat_nav.html" %}
   <div class="chat-room-main">
     <div class="chat-room-title">{% if dm_mode %}DM / {{ peer_display_name }}{% else %}{{ event.title }}/{{ active_room.room_name }}{% endif %}</div>
     <div class="mb-2 d-flex gap-2 align-items-center flex-wrap">
-      {% if actor.actor_type == 'admin' %}
-      <a class="btn btn-sm btn-outline-dark" target="_blank" rel="noopener" href="{% if dm_mode %}{{ url_for('chat.debug_read_v2', dm_uuid=dm_uuid) }}{% else %}{{ url_for('chat.debug_read_v2', event_id=event.id, room_id=active_room.room_id) }}{% endif %}">既読デバッグ</a>
-      {% endif %}
-      <button type="button" id="chat-read-front-debug-toggle" class="btn btn-sm btn-outline-secondary chat-read-front-debug-toggle d-none" aria-pressed="false">既読UIデバッグ OFF</button>
       <button type="button" id="chat-unread-jump-btn" class="btn btn-sm btn-outline-info d-none" disabled>未読へ</button>
       {% if can_manage_rooms %}
       <button type="button" id="chat-room-create-btn" class="btn btn-sm btn-outline-primary">サブルーム作成</button>
       {% if active_room.is_main|int != 1 %}
       <button type="button" id="chat-room-members-btn" class="btn btn-sm btn-outline-secondary">メンバー編集</button>
       <button type="button" id="chat-room-delete-btn" class="btn btn-sm btn-outline-danger">サブルーム削除</button>
       {% endif %}
       {% endif %}
     </div>
 
     <div class="chat-body-layout">
   <div id="chat-search-row" class="mb-2 d-flex gap-2 align-items-center d-none">
     <input id="chat-search-input" type="text" class="form-control form-control-sm" maxlength="100" placeholder="このルームを検索">
     <button type="button" id="chat-search-btn" class="btn btn-sm btn-outline-secondary" aria-label="検索" title="検索">🔎</button>
     <button type="button" id="chat-search-close" class="btn btn-sm btn-light">閉じる</button>
   </div>
   <div id="chat-search-panel" class="chat-search-panel d-none"></div>
 
   <div id="chatRoomPickerModal" class="chat-room-picker-modal d-none" aria-hidden="true">
     <div class="backdrop" data-room-picker-close="1"></div>
     <div class="panel">
       <div class="d-flex justify-content-between align-items-center mb-2">
         <h6 class="mb-0">サブルーム選択</h6>
         <button type="button" class="btn btn-sm btn-light" data-room-picker-close="1">閉じる</button>
       </div>
@@ -1893,119 +1818,106 @@
       <div class="d-flex justify-content-end gap-2 mt-3">
         <button type="button" id="room-create-cancel" class="btn btn-sm btn-light">キャンセル</button>
         <button type="button" id="room-create-submit" class="btn btn-sm btn-primary">作成</button>
       </div>
     </div>
   </div>
 
   <div id="roomMembersModal" class="room-member-modal" aria-hidden="true">
     <div class="backdrop" data-close="1"></div>
     <div class="panel">
       <h5 class="mb-2">メンバー編集</h5>
       <div class="d-flex gap-2 mb-2">
         <input id="room-members-search" type="text" class="form-control form-control-sm" placeholder="候補検索（名前 / actor_key）">
         <button type="button" id="room-members-select-all" class="btn btn-sm btn-outline-secondary">全選択</button>
         <button type="button" id="room-members-unselect-all" class="btn btn-sm btn-outline-secondary">全解除</button>
       </div>
       <div class="small mb-1">選択中 <span id="room-members-count">0</span>名</div>
       <div id="room-members-candidates" class="room-member-candidate-list"></div>
       <div class="d-flex justify-content-end gap-2 mt-3">
         <button type="button" id="room-members-cancel" class="btn btn-sm btn-light">キャンセル</button>
         <button type="button" id="room-members-submit" class="btn btn-sm btn-primary">保存</button>
       </div>
     </div>
   </div>
 
-  <div class="mt-3 d-flex gap-2 align-items-center">
-    {% if can_broadcast %}
-    <form method="post" action="{{ url_for('chat.broadcast_push', event_id=event.id) }}" class="d-flex gap-2">
-      <input type="hidden" name="csrf_token" value="{{ csrf_token }}">
-      <input name="body" class="form-control form-control-sm" placeholder="一斉通知メッセージ">
-      <button class="btn btn-warning btn-sm" type="submit">運営一斉通知</button>
-    </form>
-    {% endif %}
-  </div>
 </div>
 
 <script defer src="https://cdn.socket.io/4.7.5/socket.io.min.js"></script>
 <script>
 window.addEventListener('DOMContentLoaded', async () => {
   const root = document.querySelector('[data-event-id]');
   if (!root) return;
 
   if (!window.io) {
     return;
   }
 
   const eventId = Number(root.dataset.eventId);
   const roomId = root.dataset.roomId || "";
   const csrfToken = root.dataset.csrf;
   const vapidKey = root.dataset.vapid;
   const meId = root.dataset.me || '';
   const myDisplayName = root.dataset.displayName || '';
   const defaultAvatarUrl = root.dataset.defaultAvatar || '/static/img/avatar_default.png';
   const isAdmin = root.dataset.isAdmin === '1';
-  const canReadFrontDebug = root.dataset.readFrontDebugEnabled === '1';
-  const roomDebugKey = root.dataset.roomDebugKey || '';
-  const readFrontDebugStorageKey = 'chat.readFrontDebug.enabled';
   const sendBtn = document.getElementById('chat-send-btn');
   const dmMode = root.dataset.dmMode === '1';
   const dmUuid = root.dataset.dmUuid || "";
   const dmRoomId = root.dataset.dmRoomId || "";
   const pageTitleBase = String(root.dataset.titleBase || '').trim() || (dmMode ? 'DM' : 'チャット');
   const notificationScope = String(root.dataset.notificationScope || 'external').trim() || 'external';
   const unreadApiUrl = String(root.dataset.unreadApi || '/external-login/api/notifications/unread-count');
   const roomReadApiUrl = String(root.dataset.roomReadApi || '/external-login/api/notifications/read-by-room');
   const dmRoomReadApiUrl = String(root.dataset.dmRoomReadApi || '/external-login/api/notifications/read-dm-room');
   const effectiveDmRoomId = dmRoomId || (dmUuid ? `dm:${dmUuid}` : "");
   if (dmMode && (!dmUuid || !effectiveDmRoomId)) {
     if (sendBtn) sendBtn.disabled = true;
     alert('DM UUIDが取得できないため送信できません。');
     return;
   }
   const isMobile = window.matchMedia('(hover: none)').matches;
   const reactionEmojis = ['💕', '👍', '😆', '😭', '😢', '🫶'];
   const maxUploadImages = 6;
   const maxUploadImageBytes = 20 * 1024 * 1024;
   const DOUBLE_TAP_INTERVAL_MS = 280;
   const DOUBLE_TAP_MAX_DISTANCE = 12;
   const DOUBLE_TAP_MOVE_CANCEL = 8;
 
   const msgBox = document.getElementById('messages');
   const form = document.getElementById('chat-form');
   const bodyInput = document.getElementById('chat-body');
   const maxInputLines = 6;
   const pushBtn = document.getElementById('enable-push');
   const roomSwitch = document.getElementById('chat-room-switch');
   const roomPickerToggle = document.getElementById('chat-room-picker-toggle');
   const roomPickerModal = document.getElementById('chatRoomPickerModal');
   const notificationBadge = document.getElementById('chat-notification-badge');
   const roomCreateBtn = document.getElementById('chat-room-create-btn');
   const roomMembersBtn = document.getElementById('chat-room-members-btn');
   const roomDeleteBtn = document.getElementById('chat-room-delete-btn');
   const unreadJumpBtn = document.getElementById('chat-unread-jump-btn');
-  const readFrontDebugToggleBtn = document.getElementById('chat-read-front-debug-toggle');
   const muteBtn = document.getElementById('chat-mute-btn');
   const muteMenu = document.getElementById('chat-mute-menu');
   const mentionMenu = document.getElementById('chat-mention-menu');
   const roomCreateModal = document.getElementById('roomCreateModal');
   const roomMembersModal = document.getElementById('roomMembersModal');
   const threadModal = document.getElementById('threadModal');
   if (dmMode) {
     document.querySelectorAll('[data-ctx-action="open-thread"], .thread-summary-link').forEach((el) => { el.style.display = 'none'; });
     if (threadModal) threadModal.style.display = 'none';
   }
   const threadModalCloseBtn = document.getElementById('thread-modal-close');
   const threadRootContainer = document.getElementById('thread-root');
   const threadRepliesContainer = document.getElementById('thread-replies');
   const threadForm = document.getElementById('thread-form');
   const threadBodyInput = document.getElementById('thread-body');
   const reactionDetailModal = document.getElementById('reactionDetailModal');
   const reactionDetailCloseBtn = document.getElementById('reaction-detail-close');
   const reactionDetailBody = document.getElementById('reaction-detail-body');
   const imageInput = document.getElementById('chat-image-file');
   const imageBtn = document.getElementById('chat-image-btn');
   const replyPreview = document.getElementById('reply-preview');
   const replySender = document.getElementById('reply-sender');
   const replyText = document.getElementById('reply-text');
   const replyToInput = document.getElementById('reply-to-id');
   const editTargetInput = document.getElementById('edit-target-id');
@@ -2081,58 +1993,53 @@ window.addEventListener('DOMContentLoaded', async () => {
     },
     ctxRow: null,
     ctxBodyPlain: '',
     editTargetMessageId: null,
     typingMembers: new Map(),
     typingDebounceTimer: null,
     typingStopTimer: null,
     typingSent: false,
     threadRootId: null,
     loadingOlder: false,
     hasMoreOlder: true,
     messageIds: new Set(),
     unreadByRoomId: new Map(),
     unreadRefreshTimer: null,
     notificationPollTimer: null,
     presenceHeartbeatTimer: null,
     roomPresenceEntered: false,
     muteUntilByRoomId: new Map(),
     mentionCandidates: [],
     mentionActiveIndex: 0,
     mentionRange: null,
     mentionDebounceTimer: null,
     uploadPreviewFiles: [],
     uploadPreviewUrls: [],
     dragOverlayDepth: 0,
-    readFrontDebugEnabled: false,
-    readFrontDebugLog: [],
-    readFrontDebugSeq: 0,
     latestReadSnapshotCount: 0,
     latestReadUpdate: null,
     latestSendSeenMessageId: null,
-    latestMyMessageChecks: [],
-    readFrontDebugSelectedMessageId: null,
     initialReadSnapshotApplied: false,
     initialJoinEmitted: false,
     initialReadSnapshotTimer: null,
   };
 
   if (ctxReactions && ctxReactions.childElementCount === 0) {
     reactionEmojis.forEach((emoji) => {
       const btn = document.createElement('button');
       btn.type = 'button';
       btn.className = 'chat-reaction-emoji';
       btn.dataset.emoji = emoji;
       btn.textContent = emoji;
       ctxReactions.appendChild(btn);
     });
   }
 
   const socket = window.io({ path: '/socket.io', transports: ['websocket', 'polling'] });
   socket.on('chat_message', (msg) => {
     const wasNearBottom = isNearBottom(msgBox);
     const messageId = Number(msg?.id || 0);
     const threadRootId = Number(msg?.thread_root_id || 0);
     const isThreadReply = threadRootId > 0;
 
     if (isThreadReply) {
       bumpThreadReplyCount(threadRootId);
@@ -2154,151 +2061,123 @@ window.addEventListener('DOMContentLoaded', async () => {
       } else {
         showJumpBtn();
       }
       sendSeenForMessageId(messageId);
     }
     setSendingState(false);
     scheduleUnreadRefresh();
   });
   socket.on('chat_error', (d) => {
     setSendingState(false);
     alert(d.error || '送信失敗');
   });
   socket.on('chat_read_snapshot', (payload) => {
     const readStates = Array.isArray(payload?.read_states) ? payload.read_states : [];
     const debugPayload = {
       dmMode,
       eventId,
       roomId,
       dmUuid,
       payload_event_id: payload?.event_id ?? null,
       payload_room_id: payload?.room_id ?? null,
       payload_dm_uuid: payload?.dm_uuid ?? null,
       read_states_length: readStates.length,
       readStateSize: state.readStateByActor.size,
     };
-    pushReadFrontDebugLog('socket_snapshot_received', debugPayload);
-    if (!isReadSnapshotForCurrentRoom(payload)) {
-      pushReadFrontDebugLog('socket_snapshot_ignored_room_mismatch', debugPayload);
-      return;
-    }
+    if (!isReadSnapshotForCurrentRoom(payload)) return;
     state.latestReadSnapshotCount = Array.isArray(payload?.read_states) ? payload.read_states.length : 0;
     applyReadSnapshot(readStates);
     state.initialReadSnapshotApplied = true;
     if (state.initialReadSnapshotTimer) {
       clearTimeout(state.initialReadSnapshotTimer);
       state.initialReadSnapshotTimer = null;
     }
     refreshReadIndicators();
-    refreshAllReadFrontDebug?.();
-    pushReadFrontDebugLog('socket_snapshot_applied', {
-      ...debugPayload,
-      readStateSize: state.readStateByActor.size,
-    });
     sendSeenForLatestMessage();
   });
   socket.on('chat_read_update', (payload) => {
     state.latestReadUpdate = payload || null;
-    pushReadFrontDebugLog('socket_update_received', {
-      dmMode,
-      eventId,
-      roomId,
-      dmUuid,
-      payload_event_id: payload?.event_id ?? null,
-      payload_room_id: payload?.room_id ?? null,
-      payload_dm_uuid: payload?.dm_uuid ?? null,
-      read_states_length: 0,
-      readStateSize: state.readStateByActor.size,
-      actor_key: payload?.actor_key || `${payload?.actor_type || ''}:${payload?.actor_id || ''}`,
-      message_id: Number(payload?.last_read_message_id || 0),
-    });
     applyReadUpdate(payload);
     refreshReadIndicators();
   });
   socket.on('chat_reaction_update', (payload) => {
     const messageId = Number(payload?.message_id || 0);
     if (!messageId) return;
     const row = document.querySelector(`.chat-row[data-message-id="${messageId}"]`);
     if (row && Number(row.dataset.deletedFlag || 0) === 1) return;
     const prev = state.reactionByMessage.get(String(messageId)) || { reactionsSummary: [], myReaction: null };
     let myReaction = prev.myReaction || null;
     const changed = payload?.changed || {};
     if (canonicalReadActorKeyFromKey(changed.actor_key || `${changed.actor_type || ""}:${changed.actor_id || ""}`) === canonicalMeId) {
       myReaction = changed.emoji || null;
     }
     setReactionState(messageId, payload?.reactions || [], myReaction);
     if (isMessageInOpenThread(messageId)) refreshOpenThread();
   });
   socket.on('chat_delete_update', (payload) => {
     const messageId = Number(payload?.message_id || 0);
     if (!messageId) return;
     applyDeletedState(messageId, payload?.deleted_by_actor_type || '');
     if (isMessageInOpenThread(messageId)) refreshOpenThread();
   });
   socket.on('chat_edit_update', (payload) => {
     const messageId = Number(payload?.message_id || payload?.id || 0);
     if (!messageId) return;
     applyEditedState(messageId, payload);
     if (isMessageInOpenThread(messageId)) refreshOpenThread();
   });
   socket.on('chat_typing_update', (payload) => {
     if (dmMode) return;
     applyTypingUpdate(payload || {});
   });
 
-  initializeReadFrontDebug();
   initReactionUis();
   syncExistingMessageIds();
   refreshReadIndicators();
   emitInitialChatJoin();
   await initializeNotificationState();
   startPresenceHeartbeat();
   scheduleUnreadRefresh();
   startNotificationBadgePolling();
   refreshMuteButton();
   scrollToLatestMessage();
   window.addEventListener('load', () => {
     scrollToLatestMessage();
     sendSeenForLatestMessage();
   }, { once: true });
 
   updateAutoGrow();
   bodyInput?.addEventListener('input', () => {
     updateAutoGrow();
     queueTypingStart();
     handleMentionInput();
   });
   bodyInput?.addEventListener('keydown', handleMentionKeydown);
 
 
 
   msgBox.addEventListener('click', (e) => {
-    const selectedRow = e.target.closest('.chat-row[data-message-id]');
-    if (selectedRow && isReadFrontDebugEnabled()) {
-      state.readFrontDebugSelectedMessageId = Number(selectedRow.dataset.messageId || 0) || null;
-      renderReadFrontDebugPanel();
-    }
     const rowForAction = e.target.closest('.chat-row');
     if (rowForAction && Number(rowForAction.dataset.deletedFlag || 0) === 1) {
       if (e.target.closest('.chat-reply-quote')) {
         jumpToMessage(e.target.closest('.chat-reply-quote').dataset.jumpToId);
       }
       return;
     }
 
     const imageLink = e.target.closest('.chat-image-link');
     if (imageLink) {
       e.preventDefault();
       let imageList = [];
       try {
         imageList = JSON.parse(imageLink.dataset.imageList || '[]');
       } catch (_) {
         imageList = [];
       }
       const imageUrls = imageList.map((item) => item?.url).filter(Boolean);
       const fallbackUrl = imageLink.dataset.fullImageUrl || imageLink.href || '';
       if (!imageUrls.length && fallbackUrl) imageUrls.push(fallbackUrl);
       openImageViewer(imageUrls, Number(imageLink.dataset.imageIndex || 0));
       return;
     }
 
     const readIndicator = e.target.closest('.chat-read-inline, .chat-read-indicator');
@@ -3045,65 +2924,51 @@ window.addEventListener('DOMContentLoaded', async () => {
 
   async function disablePush(registration, subscription) {
     await subscription.unsubscribe();
     const endpoint = subscription.endpoint || (subscription.toJSON()?.endpoint) || '';
     if (endpoint) {
       await fetch('/chat/api/push/unsubscribe', {
         method: 'POST',
         headers: { 'Content-Type': 'application/json' },
         body: JSON.stringify({ csrf_token: csrfToken, endpoint }),
       });
     }
     const nextSubscription = await registration.pushManager.getSubscription();
     updatePushButton(Boolean(nextSubscription));
   }
 
   function setSendingState(isSending) {
     if (sendBtn) sendBtn.disabled = isSending;
     if (imageBtn) imageBtn.disabled = isSending;
   }
 
   function isMyMessage(msg) {
     const senderRaw = String(msg?.sender_id || '');
     const senderCanonical = canonicalReadActorKeyFromKey(senderRaw);
     const byCanonical = senderCanonical && canonicalMeId && senderCanonical === canonicalMeId;
     const byName = (!senderRaw && msg?.sender_display_name && myDisplayName && msg.sender_display_name === myDisplayName);
-    const result = Boolean(byCanonical || byName);
-    if (canReadFrontDebug) {
-      const check = {
-        sender_id_raw: senderRaw,
-        sender_id_canonical: senderCanonical,
-        canonicalMeId,
-        sender_display_name: msg?.sender_display_name || '',
-        myDisplayName,
-        result,
-      };
-      state.latestMyMessageChecks.push(check);
-      if (state.latestMyMessageChecks.length > 50) state.latestMyMessageChecks.shift();
-      pushReadFrontDebugLog('is_my_message', check);
-    }
-    return result;
+    return Boolean(byCanonical || byName);
   }
 
   function renderMessage(msg) {
     const createdAt = msg.created_at_iso ? new Date(msg.created_at_iso) : new Date();
     const dateLabel = Number.isNaN(createdAt.getTime())
       ? (msg.created_at_jst_date_label || '')
       : formatDateLabel(createdAt);
     const timeLabel = Number.isNaN(createdAt.getTime())
       ? (msg.created_at_jst_time_hm || '')
       : formatTimeLabel(createdAt);
 
     appendDateDividerIfNeeded(dateLabel);
 
     const isMe = isMyMessage(msg);
 
     const row = document.createElement('div');
     row.className = `chat-row chat-message ${isMe ? 'me' : 'other'}`;
     row.id = `msg-${msg.id}`;
     row.dataset.messageId = msg.id;
     row.dataset.senderName = msg.sender_display_name || 'Unknown';
     row.dataset.senderAvatarUrl = msg.sender_avatar_url || defaultAvatarUrl;
     const fullPlain = msg.body_plain || msg.body || '';
     row.dataset.bodyPlain = JSON.stringify(msg.body_plain_excerpt || makeExcerpt(msg.body || ''));
     row.dataset.bodyPlainFull = JSON.stringify(fullPlain || msg.body_plain_excerpt || makeExcerpt(msg.body || ''));
     row.dataset.replyToId = msg.reply_to_message_id || '';
@@ -4543,364 +4408,191 @@ function openFulltextModal(row) {
       return ['', ''];
     }
     return [key.slice(0, sepIndex), key.slice(sepIndex + 1)];
   }
 
   function canonicalReadActorKeyFromKey(actorKey) {
     const key = String(actorKey || '').trim();
     if (!key) return '';
     if (key === 'admin' || key.startsWith('admin:')) return 'admin:admin';
     if (key === 'system' || key.startsWith('system:')) return 'system:system';
     const [type, id] = splitActorKey(key);
     if ((type === 'line' || type === 'acl') && id) return `${type}:${id}`;
     return '';
   }
 
   function readDisplayNameFallback(actorKey) {
     const key = canonicalReadActorKeyFromKey(actorKey);
     if (!key) return '';
     if (key === 'admin:admin') return 'admin';
     if (key === 'system:system') return 'System';
     if (key.startsWith('acl:')) return key.slice(4);
     if (key.startsWith('line:')) return `LINE-${key.slice(5)}`;
     return key;
   }
 
-  function getDebugRoomContext() {
-    return { roomDebugKey, dmMode, eventId, roomId, dmUuid, effectiveDmRoomId };
-  }
-
-  function escapeDebugJson(value) {
-    try {
-      return escapeHtml(JSON.stringify(value, null, 2));
-    } catch (_) {
-      return escapeHtml(String(value));
-    }
-  }
-
-  function isReadFrontDebugEnabled() {
-    return Boolean(canReadFrontDebug && state.readFrontDebugEnabled);
-  }
-
-  function setReadFrontDebugEnabled(enabled) {
-    const next = Boolean(enabled && canReadFrontDebug);
-    state.readFrontDebugEnabled = next;
-    if (canReadFrontDebug) {
-      try { localStorage.setItem(readFrontDebugStorageKey, next ? '1' : '0'); } catch (_) {}
-    }
-    if (readFrontDebugToggleBtn) {
-      readFrontDebugToggleBtn.classList.toggle('is-on', next);
-      readFrontDebugToggleBtn.textContent = `既読UIデバッグ ${next ? 'ON' : 'OFF'}`;
-      readFrontDebugToggleBtn.setAttribute('aria-pressed', next ? 'true' : 'false');
-    }
-    refreshAllReadFrontDebug();
-    renderReadFrontDebugPanel();
-  }
-
-  function pushReadFrontDebugLog(type, payload) {
-    state.readFrontDebugSeq += 1;
-    state.readFrontDebugLog.push({ seq: state.readFrontDebugSeq, at: new Date().toISOString(), type, payload });
-    if (state.readFrontDebugLog.length > 100) state.readFrontDebugLog.shift();
-    if (isReadFrontDebugEnabled()) renderReadFrontDebugPanel();
-  }
-
   function computeReadIndicatorState(row) {
     const messageId = Number(row?.dataset?.messageId || 0);
     const senderIdRaw = String(row?.dataset?.senderId || '');
     const senderKey = canonicalReadActorKeyFromKey(senderIdRaw);
     const selfKey = canonicalMeId;
     const indicator = row?.querySelector('.chat-read-inline, .chat-read-indicator');
     const list = row?.querySelector('.chat-read-list');
     const isMeRow = row?.classList?.contains('me') || false;
     const readerEntries = [];
     const readers = [];
 
     for (const entry of state.readStateByActor.values()) {
       const canonicalKey = canonicalReadActorKeyFromKey(entry.actor_key || '');
       const passesMessageThreshold = Number(entry.last_read_message_id || 0) >= messageId;
       const excludedBySender = canonicalKey && canonicalKey === senderKey;
       const excludedBySelf = canonicalKey && canonicalKey === selfKey;
       const excludedBySystem = canonicalKey === 'system:system';
       readerEntries.push({
         actor_key: entry.actor_key || '',
         display_name: entry.display_name || readDisplayNameFallback(canonicalKey),
         last_read_message_id: Number(entry.last_read_message_id || 0),
         canonical_key: canonicalKey,
         passesMessageThreshold,
         excludedBySender,
         excludedBySelf,
         excludedBySystem,
       });
       if (canonicalKey && passesMessageThreshold && !excludedBySender && !excludedBySelf && !excludedBySystem) {
         readers.push(entry.display_name || readDisplayNameFallback(canonicalKey));
       }
     }
     readers.sort((a, b) => a.localeCompare(b, 'ja'));
 
     let hiddenReason = 'visible';
     if (!isMeRow) hiddenReason = 'row_not_me';
     else if (!messageId) hiddenReason = 'message_id_missing';
     else if (!indicator) hiddenReason = 'indicator_missing';
     else if (!list) hiddenReason = 'list_missing';
     else if (!readers.length) hiddenReason = 'no_readers';
 
     return { messageId, senderIdRaw, senderKey, selfKey, indicator, list, isMeRow, readers, readerEntries, hiddenReason };
   }
 
-  function getRowReadDebugInfo(row) {
-    const c = computeReadIndicatorState(row);
-    return {
-      messageId: c.messageId,
-      senderIdRaw: c.senderIdRaw,
-      senderKey: c.senderKey,
-      selfKey: c.selfKey,
-      isMeRow: c.isMeRow,
-      indicatorExists: Boolean(c.indicator),
-      listExists: Boolean(c.list),
-      readers: c.readers,
-      readerEntries: c.readerEntries,
-      hiddenReason: c.hiddenReason,
-      dataset: {
-        messageId: row?.dataset?.messageId || '',
-        senderId: row?.dataset?.senderId || '',
-        senderName: row?.dataset?.senderName || '',
-        deletedFlag: row?.dataset?.deletedFlag || '',
-        className: row?.className || '',
-      },
-    };
-  }
-
-  function renderReadFrontDebugForRow(row) {
-    if (!row) return;
-    const existing = row.querySelector('.chat-read-front-debug');
-    if (!isReadFrontDebugEnabled()) {
-      if (existing) existing.remove();
-      return;
-    }
-    const info = getRowReadDebugInfo(row);
-    const panel = existing || document.createElement('div');
-    panel.className = 'chat-read-front-debug';
-    const badgeClass = info.hiddenReason === 'visible' ? 'ok' : (info.hiddenReason === 'no_readers' ? 'warning' : 'hidden');
-    const readerRows = info.readerEntries.map((entry) => `<li>${escapeHtml(entry.display_name || '')} / ${escapeHtml(entry.actor_key || '')} / last=${entry.last_read_message_id}</li>`).join('');
-    panel.innerHTML = `<div class="chat-read-front-debug-row"><span>msg:${info.messageId || '(none)'}</span><span>sender(raw):${escapeHtml(info.senderIdRaw || '(none)')}</span><span>senderKey:${escapeHtml(info.senderKey || '(none)')}</span><span>selfKey:${escapeHtml(info.selfKey || '(none)')}</span><span>row:${info.isMeRow ? 'me' : 'other'}</span><span>readers:${info.readers.length}</span><span class="chat-read-front-debug-badge ${badgeClass}">${escapeHtml(info.hiddenReason)}</span></div><div>dataset: messageId=${escapeHtml(info.dataset.messageId)} / senderId=${escapeHtml(info.dataset.senderId)} / senderName=${escapeHtml(info.dataset.senderName)} / deleted=${escapeHtml(info.dataset.deletedFlag)} / class=${escapeHtml(info.dataset.className)}</div><div>readers: ${info.readers.length ? escapeHtml(info.readers.join(', ')) : '(none)'}</div><details><summary>影響entry(${info.readerEntries.length})</summary><ul>${readerRows || '<li>(none)</li>'}</ul></details>`;
-    (row.querySelector('.chat-main') || row).appendChild(panel);
-  }
-
-  function refreshAllReadFrontDebug() {
-    msgBox.querySelectorAll('.chat-row[data-message-id]').forEach((row) => renderReadFrontDebugForRow(row));
-  }
-
-  function renderReadFrontDebugPanel() {
-    let panel = document.getElementById('chat-read-front-debug-panel');
-    if (!isReadFrontDebugEnabled()) {
-      if (panel) panel.remove();
-      return;
-    }
-    if (!panel) {
-      panel = document.createElement('div');
-      panel.id = 'chat-read-front-debug-panel';
-      panel.className = 'chat-read-front-debug-panel';
-      document.body.appendChild(panel);
-    }
-    const latestLogs = state.readFrontDebugLog.slice(-20).reverse();
-    const latestRow = [...msgBox.querySelectorAll('.chat-row[data-message-id]')].pop() || null;
-    const latestInfo = latestRow ? getRowReadDebugInfo(latestRow) : null;
-    const selectedRow = state.readFrontDebugSelectedMessageId ? msgBox.querySelector(`.chat-row[data-message-id="${state.readFrontDebugSelectedMessageId}"]`) : null;
-    const selectedInfo = selectedRow ? getRowReadDebugInfo(selectedRow) : null;
-    panel.innerHTML = `<div class="d-flex justify-content-between align-items-center"><strong>既読フロントデバッグ</strong><button type="button" class="btn btn-sm btn-light" data-debug-close="1">閉じる</button></div><div>canonicalMeId: <code>${escapeHtml(canonicalMeId || '(none)')}</code></div><div>dmMode: ${dmMode ? '1' : '0'} / eventId:${eventId} / roomId:${escapeHtml(roomId)} / dmUuid:${escapeHtml(dmUuid)}</div><div>readStateByActor件数: ${state.readStateByActor.size}</div><div>latest snapshot count: ${state.latestReadSnapshotCount} / latest sendSeen: ${state.latestSendSeenMessageId || '(none)'}</div><div>latest update: <pre class="chat-read-front-debug-json">${escapeDebugJson(state.latestReadUpdate || {})}</pre></div><details><summary>readStateByActor JSON</summary><pre class="chat-read-front-debug-json">${escapeDebugJson(Object.fromEntries(state.readStateByActor))}</pre></details><details open><summary>最新row debug</summary><pre class="chat-read-front-debug-json">${escapeDebugJson(latestInfo || {})}</pre></details><details><summary>選択row debug</summary><pre class="chat-read-front-debug-json">${escapeDebugJson(selectedInfo || {})}</pre></details><div class="chat-read-front-debug-log">${latestLogs.map((log) => `<div><strong>#${log.seq}</strong> ${escapeHtml(log.type)} <small>${escapeHtml(log.at)}</small><div class="chat-read-front-debug-json">${escapeDebugJson(log.payload)}</div></div>`).join('')}</div>`;
-    panel.querySelector('[data-debug-close="1"]')?.addEventListener('click', () => setReadFrontDebugEnabled(false), { once: true });
-  }
-
-  function initializeReadFrontDebug() {
-    if (!canReadFrontDebug) {
-      state.readFrontDebugEnabled = false;
-      if (readFrontDebugToggleBtn) readFrontDebugToggleBtn.classList.add('d-none');
-      return;
-    }
-    if (readFrontDebugToggleBtn) {
-      readFrontDebugToggleBtn.classList.remove('d-none');
-      readFrontDebugToggleBtn.addEventListener('click', () => setReadFrontDebugEnabled(!state.readFrontDebugEnabled));
-    }
-    let initial = false;
-    try { initial = localStorage.getItem(readFrontDebugStorageKey) === '1'; } catch (_) {}
-    setReadFrontDebugEnabled(initial);
-  }
-
   function applyReadUpdate(readState) {
     const legacyKey = `${readState?.actor_type || ''}:${readState?.actor_id || ''}`;
     const key = canonicalReadActorKeyFromKey(readState?.actor_key || legacyKey);
-    pushReadFrontDebugLog('apply_read_update', { room: getDebugRoomContext(), actor_key: key || readState?.actor_key || legacyKey, message_id: Number(readState?.last_read_message_id || 0), readStateSize: state.readStateByActor.size });
     if (!key) return;
     const prev = state.readStateByActor.get(key);
     const nextReadId = Number(readState?.last_read_message_id || 0);
     const prevReadId = Number(prev?.last_read_message_id || 0);
     state.readStateByActor.set(key, {
       actor_key: key,
       display_name: readState?.display_name || prev?.display_name || readDisplayNameFallback(key),
       last_read_message_id: Math.max(prevReadId, nextReadId),
     });
   }
 
   function emitInitialChatJoin() {
     if (!socket) return;
     if (state.initialReadSnapshotTimer) {
       clearTimeout(state.initialReadSnapshotTimer);
       state.initialReadSnapshotTimer = null;
     }
     state.initialJoinEmitted = true;
     const payload = dmMode
       ? { dm_uuid: dmUuid, room_id: effectiveDmRoomId }
       : { event_id: eventId, room_id: roomId };
-    pushReadFrontDebugLog('initial_join_emit', {
-      dmMode,
-      eventId,
-      roomId,
-      dmUuid,
-      payload_event_id: payload?.event_id ?? null,
-      payload_room_id: payload?.room_id ?? null,
-      payload_dm_uuid: payload?.dm_uuid ?? null,
-      read_states_length: 0,
-      readStateSize: state.readStateByActor.size,
-    });
     socket.emit('chat_join', payload);
     state.initialReadSnapshotTimer = window.setTimeout(() => {
       if (state.initialReadSnapshotApplied) return;
-      pushReadFrontDebugLog('initial_snapshot_timeout_warning', {
-        dmMode,
-        eventId,
-        roomId,
-        dmUuid,
-        payload_event_id: null,
-        payload_room_id: null,
-        payload_dm_uuid: null,
-        read_states_length: 0,
-        readStateSize: state.readStateByActor.size,
-        initialJoinEmitted: state.initialJoinEmitted,
-      });
     }, 1200);
   }
 
   function isReadSnapshotForCurrentRoom(payload) {
     const payloadDmUuid = String(payload?.dm_uuid || '');
     const payloadRoomId = String(payload?.room_id || '');
     const payloadEventId = Number(payload?.event_id || 0);
     const hasReadStates = Array.isArray(payload?.read_states) && payload.read_states.length >= 0;
 
     if (dmMode) {
       if (payloadDmUuid && payloadDmUuid === String(dmUuid || '')) return true;
       if (payloadRoomId && payloadRoomId === `dm:${dmUuid}`) return true;
       if (payloadRoomId && payloadRoomId === String(effectiveDmRoomId || '')) return true;
       if (!payloadDmUuid && !payloadRoomId && hasReadStates) return true;
       return false;
     }
 
     const eventMatches = payloadEventId > 0 && payloadEventId === Number(eventId || 0);
     if (!eventMatches) return false;
     if (!payloadRoomId) return true;
     return payloadRoomId === String(roomId || '');
   }
 
   function applyReadSnapshot(states) {
-    const beforeSize = state.readStateByActor.size;
-    const incomingCount = Array.isArray(states) ? states.length : 0;
-    pushReadFrontDebugLog('apply_read_snapshot', {
-      room: getDebugRoomContext(),
-      before_size: beforeSize,
-      incoming_count: incomingCount,
-    });
     state.readStateByActor.clear();
     for (const readState of states || []) {
       applyReadUpdate(readState);
     }
-    pushReadFrontDebugLog('apply_read_snapshot_result', {
-      room: getDebugRoomContext(),
-      before_size: beforeSize,
-      incoming_count: incomingCount,
-      after_size: state.readStateByActor.size,
-    });
   }
 
   function refreshReadIndicatorForRow(row) {
     const c = computeReadIndicatorState(row);
     const { indicator, list, readers, hiddenReason } = c;
-    pushReadFrontDebugLog('refresh_read_row', { message_id: c.messageId, hiddenReason, readers: readers.length });
     if (hiddenReason !== 'visible') {
       if (indicator) {
         indicator.classList.add('d-none');
         indicator.textContent = '';
       }
       if (list) {
         list.classList.add('d-none');
         list.innerHTML = '';
       }
-      renderReadFrontDebugForRow(row);
       return;
     }
     const readLabel = dmMode ? '既読' : `既読 ${readers.length}`;
     indicator.classList.remove('d-none');
     indicator.textContent = readLabel;
     list.innerHTML = `<ul>${readers.map((name) => `<li>${escapeHtml(name)}</li>`).join('')}</ul>`;
-    renderReadFrontDebugForRow(row);
   }
 
   function refreshReadIndicators() {
     const rows = msgBox.querySelectorAll('.chat-row.me');
     rows.forEach((row) => refreshReadIndicatorForRow(row));
-    pushReadFrontDebugLog('refresh_read_indicators', {
-      dmMode,
-      eventId,
-      roomId,
-      dmUuid,
-      payload_event_id: null,
-      payload_room_id: null,
-      payload_dm_uuid: null,
-      read_states_length: state.latestReadSnapshotCount,
-      rowCount: rows.length,
-      readStateSize: state.readStateByActor.size,
-    });
-    refreshAllReadFrontDebug();
-    renderReadFrontDebugPanel();
   }
 
   function toggleReadList(chatMain) {
     const list = chatMain?.querySelector('.chat-read-list');
     if (!list || list.classList.contains('d-none') && !list.innerHTML) return;
     list.classList.toggle('d-none');
   }
 
   function sendSeenForLatestMessage() {
     const rows = msgBox.querySelectorAll('.chat-row[data-message-id]');
     const lastRow = rows.length ? rows[rows.length - 1] : null;
     const messageId = Number(lastRow?.dataset?.messageId || 0);
-    pushReadFrontDebugLog('send_seen_latest', { room: getDebugRoomContext(), message_id: messageId, rowCount: rows.length });
     if (!lastRow) return;
     sendSeenForMessageId(messageId);
   }
 
   function sendSeenForMessageId(messageId) {
     state.latestSendSeenMessageId = Number(messageId || 0) || null;
-    pushReadFrontDebugLog('send_seen_message', { room: getDebugRoomContext(), message_id: Number(messageId || 0), readStateSize: state.readStateByActor.size });
     if (!Number.isFinite(messageId) || messageId <= 0) return;
     if (dmMode) {
       socket.emit('chat_seen', { dm_uuid: dmUuid, room_id: effectiveDmRoomId, last_seen_message_id: messageId });
     } else {
       socket.emit('chat_seen', { event_id: eventId, room_id: roomId, last_seen_message_id: messageId });
     }
   }
 
   function escapeHtml(value) {
     return String(value || '')
       .replace(/&/g, '&amp;')
       .replace(/</g, '&lt;')
       .replace(/>/g, '&gt;')
       .replace(/"/g, '&quot;')
       .replace(/'/g, '&#39;');
   }
 
   function urlBase64ToUint8Array(base64String) {
     const padding = '='.repeat((4 - (base64String.length % 4)) % 4);
     const base64 = (base64String + padding).replace(/-/g, '+').replace(/_/g, '/');
     const rawData = window.atob(base64);
     return Uint8Array.from([...rawData].map((c) => c.charCodeAt(0)));
   }
 
   function scrollToLatestMessage() {
