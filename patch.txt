diff --git a/external_login_user/notifications.py b/external_login_user/notifications.py
index 2588d32510f3bb7d27810161c3d8dbcfea94e827..b6ceeb82a7e5ed3efe141f674831eaa274154ea2 100644
--- a/external_login_user/notifications.py
+++ b/external_login_user/notifications.py
@@ -1,28 +1,29 @@
 from __future__ import annotations
 
 from datetime import datetime
+from time import perf_counter
 from typing import Any
 
 from flask import abort, current_app, jsonify, render_template, request, session
 
 from . import bp
 from .utils import _require_ext_login
 from app.utils.db import get_db
 
 
 _NOTIFICATION_DDL = """
 CREATE TABLE IF NOT EXISTS mfu_notifications (
   id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
   user_kind VARCHAR(16) NOT NULL,
   user_id BIGINT UNSIGNED NOT NULL,
   kind VARCHAR(64) NOT NULL,
   title VARCHAR(255) NOT NULL,
   body TEXT NULL,
   target_url VARCHAR(512) NOT NULL,
   event_id BIGINT UNSIGNED NULL,
   created_at DATETIME NOT NULL,
   read_at DATETIME NULL,
   dedup_key VARCHAR(191) NOT NULL,
   PRIMARY KEY (id),
   UNIQUE KEY uq_mfu_notifications_dedup (user_kind, user_id, dedup_key),
   KEY idx_mfu_notifications_unread (user_kind, user_id, read_at, created_at),
@@ -215,50 +216,136 @@ def api_notifications_list():
             len(items),
             total,
             latest_id,
             latest_created_at,
         )
 
         resp = jsonify(
             {
                 "items": items,
                 "pagination": {
                     "page": page,
                     "per_page": per_page,
                     "total": total,
                     "has_next": page * per_page < total,
                 },
             }
         )
         resp.headers["Cache-Control"] = "no-store, no-cache, must-revalidate, max-age=0"
         resp.headers["Pragma"] = "no-cache"
         return resp
     finally:
         cur.close()
         db.close()
 
 
+@bp.get("/api/notifications/updates")
+def api_notifications_updates():
+    guard = _require_ext_login()
+    if guard:
+        return guard
+
+    uid = int(session.get("ext_user_id") or 0)
+    since_id = max(int(request.args.get("since_id") or 0), 0)
+    limit = min(max(int(request.args.get("limit") or 20), 1), 100)
+
+    started = perf_counter()
+    db = get_db()
+    cur = db.cursor(dictionary=True)
+    try:
+        db.start_transaction()
+
+        cur.execute(
+            """
+            SELECT id, title, body, target_url, created_at, read_at
+              FROM mfu_notifications
+             WHERE user_kind='external'
+               AND user_id=%s
+               AND id > %s
+             ORDER BY id ASC
+             LIMIT %s
+            """,
+            (uid, since_id, limit),
+        )
+        rows = cur.fetchall() or []
+
+        cur.execute(
+            """
+            SELECT COUNT(*) AS cnt
+              FROM mfu_notifications
+             WHERE user_kind='external'
+               AND user_id=%s
+               AND read_at IS NULL
+            """,
+            (uid,),
+        )
+        unread_count = int((cur.fetchone() or {}).get("cnt") or 0)
+        db.commit()
+
+        items = []
+        latest_id = since_id
+        for row in rows:
+            item_id = int(row["id"])
+            latest_id = max(latest_id, item_id)
+            items.append(
+                {
+                    "id": item_id,
+                    "title": row.get("title"),
+                    "body": row.get("body") or "",
+                    "target_url": row.get("target_url") or "/external-login/",
+                    "created_at": row.get("created_at").isoformat() if row.get("created_at") else None,
+                    "read_at": row.get("read_at").isoformat() if row.get("read_at") else None,
+                }
+            )
+
+        elapsed_ms = int((perf_counter() - started) * 1000)
+        current_app.logger.info(
+            "notifications updates user_id=%s since_id=%s limit=%s returned=%s latest_id=%s unread_count=%s elapsed_ms=%s",
+            uid,
+            since_id,
+            limit,
+            len(items),
+            latest_id,
+            unread_count,
+            elapsed_ms,
+        )
+
+        resp = jsonify(
+            {
+                "latest_id": latest_id,
+                "unread_count": unread_count,
+                "items": items,
+            }
+        )
+        resp.headers["Cache-Control"] = "no-store, no-cache, must-revalidate, max-age=0"
+        resp.headers["Pragma"] = "no-cache"
+        return resp
+    finally:
+        cur.close()
+        db.close()
+
+
 @bp.post("/api/notifications/<int:notification_id>/read")
 def api_notifications_mark_read(notification_id: int):
     guard = _require_ext_login()
     if guard:
         return guard
 
     uid = int(session.get("ext_user_id") or 0)
     db = get_db()
     cur = db.cursor()
     try:
         cur.execute(
             """
             UPDATE mfu_notifications
                SET read_at=COALESCE(read_at, %s)
              WHERE id=%s
                AND user_kind='external'
                AND user_id=%s
             """,
             (datetime.utcnow(), notification_id, uid),
         )
         db.commit()
         if cur.rowcount == 0:
             abort(404)
         return jsonify({"ok": True})
     finally:
diff --git a/external_login_user/template/notifications.html b/external_login_user/template/notifications.html
index 611c8dfec004e962fb9d2444b8f24e63ec041984..524872fd6fc1eff6f8957111800cc6aa4ef8810d 100644
--- a/external_login_user/template/notifications.html
+++ b/external_login_user/template/notifications.html
@@ -10,139 +10,227 @@
       <label class="form-check-label" for="onlyUnread">未読のみ</label>
     </div>
     <button id="refreshBtn" class="btn btn-outline-primary btn-sm">更新</button>
     <button id="markAllReadBtn" class="btn btn-outline-secondary btn-sm">すべて既読にする</button>
   </div>
 </div>
 
 <div id="notificationList" class="list-group"></div>
 <div id="emptyState" class="text-muted py-5 text-center d-none">通知はありません。</div>
 <div class="d-flex justify-content-center mt-3">
   <button id="loadMoreBtn" class="btn btn-outline-primary d-none">もっと見る</button>
 </div>
 
 <script>
   (() => {
     const listEl = document.getElementById('notificationList');
     const emptyEl = document.getElementById('emptyState');
     const loadMoreBtn = document.getElementById('loadMoreBtn');
     const markAllReadBtn = document.getElementById('markAllReadBtn');
     const refreshBtn = document.getElementById('refreshBtn');
     const onlyUnread = document.getElementById('onlyUnread');
 
     let page = 1;
     let hasNext = false;
     let loading = false;
+    let pollingTimer = null;
+    let lastId = 0;
+    const knownIds = new Set();
 
     const escapeHtml = (value) => {
       const div = document.createElement('div');
       div.textContent = value || '';
       return div.innerHTML;
     };
 
     const formatDate = (iso) => {
       if (!iso) return '';
       const d = new Date(iso);
       if (Number.isNaN(d.getTime())) return '';
       return d.toLocaleString('ja-JP');
     };
 
     const createItem = (item) => {
       const a = document.createElement('a');
       a.href = '#';
       a.className = `list-group-item list-group-item-action ${item.read_at ? '' : 'list-group-item-warning'}`;
       a.innerHTML = `
         <div class="d-flex w-100 justify-content-between align-items-start">
           <div class="me-3">
             <h2 class="h6 mb-1">${escapeHtml(item.title || 'お知らせ')}</h2>
             <p class="mb-1">${escapeHtml(item.body || '')}</p>
             <small class="text-muted">${formatDate(item.created_at)}</small>
           </div>
           <span class="badge ${item.read_at ? 'bg-secondary' : 'bg-danger'}">${item.read_at ? '既読' : '未読'}</span>
         </div>
       `;
       a.addEventListener('click', async (e) => {
         e.preventDefault();
         try {
           await fetch(`/external-login/api/notifications/${item.id}/read`, {
             method: 'POST',
             credentials: 'same-origin',
           });
         } catch (_e) {
           // noop
         }
         window.location.assign(item.target_url || '/external-login/');
       });
       return a;
     };
 
+    const updateNavBadge = (count) => {
+      const badge = document.getElementById('navNotifBadge');
+      if (!badge) return;
+      const safeCount = Number.isFinite(Number(count)) ? Math.max(0, Number(count)) : 0;
+      if (safeCount > 0) {
+        badge.textContent = String(Math.min(Math.floor(safeCount), 99));
+        badge.classList.remove('d-none');
+        return;
+      }
+      badge.textContent = '0';
+      badge.classList.add('d-none');
+    };
+
+    const prependNewItems = (items) => {
+      if (!Array.isArray(items) || items.length === 0) return;
+      for (const item of items) {
+        if (!item || !item.id || knownIds.has(item.id)) continue;
+        knownIds.add(item.id);
+        listEl.insertBefore(createItem(item), listEl.firstChild);
+      }
+      emptyEl.classList.toggle('d-none', listEl.children.length > 0);
+    };
+
     const renderItems = (items, append = false) => {
-      if (!append) listEl.innerHTML = '';
-      items.forEach((item) => listEl.appendChild(createItem(item)));
+      if (!append) {
+        listEl.innerHTML = '';
+        knownIds.clear();
+      }
+      items.forEach((item) => {
+        if (!item || !item.id || knownIds.has(item.id)) return;
+        knownIds.add(item.id);
+        listEl.appendChild(createItem(item));
+      });
       const hasItems = listEl.children.length > 0;
       emptyEl.classList.toggle('d-none', hasItems);
       loadMoreBtn.classList.toggle('d-none', !hasNext);
+
+      if (!append) {
+        const latest = items.reduce((max, item) => Math.max(max, Number(item?.id || 0)), 0);
+        lastId = Math.max(lastId, latest);
+      }
     };
 
     const load = async ({ append = false } = {}) => {
       if (loading) return;
       loading = true;
       try {
         const unread = onlyUnread.checked ? '1' : '0';
         const res = await fetch(`/external-login/api/notifications?unread=${unread}&page=${page}`, {
           credentials: 'same-origin',
           cache: 'no-store',
         });
         if (!res.ok) throw new Error('failed to load notifications');
         const data = await res.json();
         hasNext = !!data?.pagination?.has_next;
         renderItems(data.items || [], append);
+        if (!append) {
+          const unreadCount = (data.items || []).filter((item) => !item.read_at).length;
+          updateNavBadge(unreadCount);
+        }
       } finally {
         loading = false;
       }
     };
 
+    const pollUpdatesOnce = async () => {
+      if (document.hidden) return;
+      if (onlyUnread.checked) return;
+      try {
+        const res = await fetch(`/external-login/api/notifications/updates?since_id=${lastId}&limit=50`, {
+          credentials: 'same-origin',
+          cache: 'no-store',
+        });
+        if (!res.ok) return;
+        const data = await res.json();
+        prependNewItems(data.items || []);
+        lastId = Math.max(lastId, Number(data?.latest_id || 0));
+        updateNavBadge(data?.unread_count ?? 0);
+      } catch (_e) {
+        // noop
+      }
+    };
+
+    const stopPolling = () => {
+      if (pollingTimer) {
+        window.clearInterval(pollingTimer);
+        pollingTimer = null;
+      }
+    };
+
+    const startPolling = () => {
+      stopPolling();
+      if (document.hidden || onlyUnread.checked) return;
+      pollingTimer = window.setInterval(pollUpdatesOnce, 2000);
+    };
+
     onlyUnread.addEventListener('change', async () => {
       page = 1;
       await load({ append: false });
+      startPolling();
     });
 
     loadMoreBtn.addEventListener('click', async () => {
       if (!hasNext) return;
       page += 1;
       await load({ append: true });
     });
 
     refreshBtn.addEventListener('click', async () => {
       page = 1;
       await load({ append: false });
+      await pollUpdatesOnce();
     });
 
     markAllReadBtn.addEventListener('click', async () => {
       markAllReadBtn.disabled = true;
       try {
         await fetch('/external-login/api/notifications/read-all', {
           method: 'POST',
           credentials: 'same-origin',
         });
         page = 1;
         await load({ append: false });
+        await pollUpdatesOnce();
       } finally {
         markAllReadBtn.disabled = false;
       }
     });
 
     const refreshCurrentHead = async () => {
-      page = 1;
-      await load({ append: false });
+      await pollUpdatesOnce();
     };
 
     document.addEventListener('visibilitychange', () => {
-      if (!document.hidden) refreshCurrentHead();
+      if (document.hidden) {
+        stopPolling();
+        return;
+      }
+      refreshCurrentHead();
+      startPolling();
+    });
+    window.addEventListener('pageshow', async () => {
+      await refreshCurrentHead();
+      startPolling();
+    });
+    window.addEventListener('message', (event) => {
+      if (event?.data?.type === 'mfu-notification-updated') pollUpdatesOnce();
     });
-    window.addEventListener('pageshow', refreshCurrentHead);
-    window.setInterval(refreshCurrentHead, 15000);
 
-    load({ append: false });
+    load({ append: false }).then(() => {
+      startPolling();
+      pollUpdatesOnce();
+    });
   })();
 </script>
 {% endblock %}
