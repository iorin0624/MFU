diff --git a/chat/templates/chat/_chat_nav.html b/chat/templates/chat/_chat_nav.html
index 38bca96b041134d54eb0712c97a20be8a6af3d05..d8aac3b0187d4870d3e4e2cddbe803e683abd8cd 100644
--- a/chat/templates/chat/_chat_nav.html
+++ b/chat/templates/chat/_chat_nav.html
@@ -1,33 +1,32 @@
 <div class="chat-nav">
   <a class="chat-nav-link chat-nav-link-top" href="{{ chat_top_url|default('/external-login/') }}">
     <span class="chat-nav-icon" aria-hidden="true">←</span>
     <span class="chat-nav-label">イベントTOP</span>
   </a>
   <div class="chat-nav-actions" role="navigation" aria-label="チャットナビ">
     <button type="button" id="chat-room-picker-toggle" class="chat-nav-link chat-nav-button" aria-label="サブルーム選択" title="サブルーム">
       <span class="chat-nav-icon" aria-hidden="true">💬</span>
       <span class="chat-nav-label">ルーム</span>
     </button>
     <a class="chat-nav-link chat-notification-link" href="{{ chat_notifications_url|default('/external-login/notifications') }}" aria-label="通知履歴">
-      <span class="chat-nav-icon" aria-hidden="true">🔔</span>
-      <span id="chat-notification-badge" class="chat-notification-badge d-none" aria-live="polite"></span>
+      <span id="chat-notification-badge" class="chat-notification-badge no-unread" aria-live="polite">🔔</span>
       <span class="chat-nav-label">通知履歴</span>
     </a>
     <div class="chat-mute-wrap">
       <button type="button" id="chat-mute-btn" class="chat-nav-link chat-nav-button" aria-label="ミュート">
         <span class="chat-nav-icon" aria-hidden="true">🔕</span>
         <span class="chat-nav-label">ミュート</span>
       </button>
       <div id="chat-mute-menu" class="chat-mute-menu d-none">
         <button type="button" data-mute-hours="1">1時間</button>
         <button type="button" data-mute-hours="8">8時間</button>
         <button type="button" data-mute-hours="24">24時間</button>
         <button type="button" data-mute-clear="1">解除</button>
       </div>
     </div>
     <button type="button" id="chat-search-toggle" class="chat-nav-link chat-nav-button" aria-label="検索" title="検索">
       <span class="chat-nav-icon" aria-hidden="true">🔎</span>
       <span class="chat-nav-label">検索</span>
     </button>
   </div>
 </div>

diff --git a/chat/templates/chat/room.html b/chat/templates/chat/room.html
index e39cb51b3146047a1d2d95b72b0076d79d1a965c..d1c1a3aa76965c4a4c5f72ff70517c404770d324 100644
--- a/chat/templates/chat/room.html
+++ b/chat/templates/chat/room.html
@@ -905,59 +905,69 @@
   align-items: center;
   gap: .25rem;
   border: 1px solid #ced5ec;
   border-radius: 999px;
   padding: .2rem .55rem;
   background: #fff;
   color: #2f3f75;
   text-decoration: none;
   font-size: .85rem;
   white-space: nowrap;
 }
 
 .chat-nav-button {
   cursor: pointer;
 }
 
 
 .chat-notification-link {
   position: relative;
 }
 
 .chat-notification-badge {
   min-width: 1.1rem;
   height: 1.1rem;
   border-radius: 999px;
-  background: #dc3545;
-  color: #fff;
   font-size: .72rem;
   display: inline-flex;
   align-items: center;
   justify-content: center;
   padding: 0 .3rem;
 }
 
+.chat-notification-badge.has-unread {
+  background: #dc3545;
+  color: #fff;
+}
+
+.chat-notification-badge.no-unread {
+  background: transparent;
+  color: inherit;
+  padding: 0;
+  min-width: auto;
+}
+
 .chat-room-picker-modal {
   position: fixed;
   inset: 0;
   z-index: 1200;
 }
 
 .chat-room-picker-modal .backdrop {
   position: absolute;
   inset: 0;
   background: rgba(0, 0, 0, .35);
 }
 
 .chat-room-picker-modal .panel {
   position: relative;
   width: min(92vw, 420px);
   margin: 12vh auto 0;
   background: #fff;
   border-radius: 12px;
   padding: .75rem;
   box-shadow: 0 16px 36px rgba(0,0,0,.2);
 }
 
 .chat-room-picker-list {
   display: flex;
   flex-direction: column;
@@ -3784,54 +3794,56 @@
     });
     updateRoomOptionUnreadBadges();
     updateUnreadJumpButton();
   }
 
   function updateRoomOptionUnreadBadges() {
     if (!roomSwitch) return;
     Array.from(roomSwitch.options).forEach((opt) => {
       const baseLabel = String(opt.textContent || '').replace(/\s*\(未読\d+\)$/u, '');
       const unread = Number((state.unreadByRoomId.get(opt.value || '') || {}).unread_count || 0);
       opt.textContent = unread > 0 ? `${baseLabel} (未読${unread})` : baseLabel;
     });
   }
 
 
 
   async function refreshNotificationBadge() {
     try {
       const res = await fetch('/external-login/api/notifications/unread-count', { credentials: 'same-origin' });
       const data = await res.json().catch(() => ({}));
       if (!res.ok || !data.ok) return;
       const unread = Number(data.unread_count || 0);
       if (!notificationBadge) return;
       if (unread > 0) {
         notificationBadge.textContent = unread > 99 ? '99+' : String(unread);
-        notificationBadge.classList.remove('d-none');
+        notificationBadge.classList.remove('no-unread');
+        notificationBadge.classList.add('has-unread');
       } else {
-        notificationBadge.textContent = '';
-        notificationBadge.classList.add('d-none');
+        notificationBadge.textContent = '🔔';
+        notificationBadge.classList.remove('has-unread');
+        notificationBadge.classList.add('no-unread');
       }
     } catch (_e) {
       // noop
     }
   }
 
   function startNotificationBadgePolling() {
     if (state.notificationPollTimer) return;
     state.notificationPollTimer = setInterval(() => {
       refreshNotificationBadge();
     }, 15000);
   }
 
   function stopNotificationBadgePolling() {
     if (!state.notificationPollTimer) return;
     clearInterval(state.notificationPollTimer);
     state.notificationPollTimer = null;
   }
 
   function updateUnreadJumpButton() {
     const info = state.unreadByRoomId.get(roomId) || {};
     const firstUnreadId = Number(info.first_unread_id || 0);
     if (!unreadJumpBtn) return;
     if (!firstUnreadId) {
       unreadJumpBtn.classList.add('d-none');
