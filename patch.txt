diff --git a/chat/static/chat/js/chat.js b/chat/static/chat/js/chat.js
index 6b3a3f050803e53b4bba98fe7120f956cdff35bd..ba7c88a378ade50ec6673f196ea7bda2c2bcbb35 100644
--- a/chat/static/chat/js/chat.js
+++ b/chat/static/chat/js/chat.js
@@ -5,51 +5,58 @@
   const eventId = Number(root.dataset.eventId);
   const csrfToken = root.dataset.csrf;
   const vapidKey = root.dataset.vapid;
   const meId = root.dataset.me || '';
   const myDisplayName = root.dataset.displayName || '';
   const reactionEmojis = ['💕', '👍', '😆', '😭', '😢', '🫶'];
   const msgBox = document.getElementById('messages');
   const form = document.getElementById('chat-form');
   const bodyInput = document.getElementById('chat-body');
   const pushBtn = document.getElementById('enable-push');
 
   const fmtDate = new Intl.DateTimeFormat('ja-JP', {
     timeZone: 'Asia/Tokyo',
     year: 'numeric',
     month: 'numeric',
     day: 'numeric',
     weekday: 'short',
   });
   const fmtTime = new Intl.DateTimeFormat('ja-JP', {
     timeZone: 'Asia/Tokyo',
     hour: 'numeric',
     minute: '2-digit',
     hour12: false,
   });
 
-  const socket = io({ transports: ['websocket', 'polling'] });
+  const socket = io({
+    path: '/socket.io',
+    transports: ['websocket', 'polling'],
+    withCredentials: true,
+  });
+  socket.on('connect_error', (err) => {
+    console.warn('[socket.io] chat.js connect_error', err?.message || err);
+  });
   socket.emit('chat_join', { event_id: eventId });
 
   socket.on('chat_message', (msg) => {
     renderMessage(msg);
     msgBox.scrollTop = msgBox.scrollHeight;
   });
   socket.on('chat_error', (d) => alert(d.error || '送信失敗'));
   socket.on('chat_reaction_update', (payload) => applyReactionUpdate(payload));
 
 
   msgBox?.addEventListener('click', (e) => {
     const reactionBtn = e.target.closest('.chat-reaction-emoji');
     if (!reactionBtn) return;
     const row = reactionBtn.closest('.chat-row');
     const messageId = Number(row?.dataset.messageId || 0);
     const emoji = reactionBtn.dataset.emoji || '';
     if (!messageId || !reactionEmojis.includes(emoji)) return;
     socket.emit('chat_react', { event_id: eventId, message_id: messageId, emoji });
   });
 
   form?.addEventListener('submit', (e) => {
     e.preventDefault();
     socket.emit('chat_send', { event_id: eventId, body: bodyInput.value });
     bodyInput.value = '';
   });
diff --git a/chat/templates/chat/_chat_nav.html b/chat/templates/chat/_chat_nav.html
index e61aa6e4c254e9bfb9713f18cfadbc53581096b1..2a30a22131839742044519d7e9d3ef4bf5395b95 100644
--- a/chat/templates/chat/_chat_nav.html
+++ b/chat/templates/chat/_chat_nav.html
@@ -17,92 +17,109 @@
       <span id="chatNavNotifBadge"
             class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger d-none">0</span>
       <span class="chat-nav-label">通知履歴</span>
     </a>
 
     <div class="chat-mute-wrap">
       <button type="button" id="chat-mute-btn" class="chat-nav-link chat-nav-button" aria-label="ミュート">
         <span class="chat-nav-icon" aria-hidden="true">🔕</span>
         <span class="chat-nav-label">ミュート</span>
       </button>
       <div id="chat-mute-menu" class="chat-mute-menu d-none">
         <button type="button" data-mute-hours="1">1時間</button>
         <button type="button" data-mute-hours="8">8時間</button>
         <button type="button" data-mute-hours="24">24時間</button>
         <button type="button" data-mute-clear="1">解除</button>
       </div>
     </div>
 
     <button type="button" id="chat-search-toggle" class="chat-nav-link chat-nav-button" aria-label="検索" title="検索">
       <span class="chat-nav-icon" aria-hidden="true">🔎</span>
       <span class="chat-nav-label">検索</span>
     </button>
   </div>
 </div>
 
-<script src="/socket.io/socket.io.js"></script>
+<script src="https://cdn.socket.io/4.7.5/socket.io.min.js"></script>
 <script>
   document.addEventListener('DOMContentLoaded', () => {
     const badge = document.getElementById('chatNavNotifBadge');
     if (!badge) return;
 
     let pollTimer = null;
 
     const applyCount = (rawCount) => {
       const count = Number(rawCount ?? 0);
       if (Number.isFinite(count) && count > 0) {
         badge.textContent = String(Math.min(Math.floor(count), 99));
         badge.classList.remove('d-none');
         return;
       }
       badge.textContent = '0';
       badge.classList.add('d-none');
     };
 
     const updateBadge = async () => {
       try {
         const res = await fetch('/external-login/api/notifications/unread-count', { credentials: 'same-origin', cache: 'no-store' });
+        if (res.status === 401) {
+          const data = await res.json().catch(() => ({}));
+          if (data?.login_url) {
+            window.location.assign(data.login_url);
+            return;
+          }
+          console.warn('[notifications] login required but login_url is missing');
+          return;
+        }
         if (!res.ok) {
+          console.warn('[notifications] unread-count failed', res.status);
           applyCount(0);
           return;
         }
         const data = await res.json();
         applyCount(data?.count ?? 0);
       } catch (_e) {
         applyCount(0);
       }
     };
 
     const startPolling = () => {
       if (pollTimer !== null) return;
       pollTimer = window.setInterval(updateBadge, 30000);
     };
 
     const stopPolling = () => {
       if (pollTimer === null) return;
       window.clearInterval(pollTimer);
       pollTimer = null;
     };
 
     if (window.io) {
-      const socket = window.io();
+      const socket = window.io({
+        path: '/socket.io',
+        transports: ['websocket', 'polling'],
+        withCredentials: true,
+      });
+      socket.on('connect_error', (err) => {
+        console.warn('[socket.io] chat nav connect_error', err?.message || err);
+      });
       socket.on('connect', () => {
         stopPolling();
         updateBadge();
       });
       socket.on('disconnect', () => {
         startPolling();
       });
       socket.on('notif_unread', (data) => {
         applyCount(data?.count ?? 0);
       });
     } else {
       startPolling();
     }
 
     updateBadge();
     document.addEventListener('visibilitychange', () => {
       if (!document.hidden) updateBadge();
     });
     window.addEventListener('pageshow', updateBadge);
   });
 </script>
diff --git a/chat/templates/chat/room.html b/chat/templates/chat/room.html
index 7c8d8b6ca95a5f7374a74bc57aa9d7045ef2cc5c..76e8dadde12475e612ad8050496e40f7030fa1ad 100644
--- a/chat/templates/chat/room.html
+++ b/chat/templates/chat/room.html
@@ -1,37 +1,37 @@
 {% extends "chat/base_chat.html" %}
 {% block content %}
 {% if dm_mode %}
 <div class="container py-3">
   <h4>管理者DM</h4>
   <div id="dmMessages" class="border rounded p-2 mb-2" style="height:60vh;overflow:auto;background:#f8f9fa"></div>
   <div class="input-group">
     <input id="dmInput" class="form-control" placeholder="メッセージ">
     <button id="dmSend" class="btn btn-primary">送信</button>
   </div>
 </div>
-<script src="/socket.io/socket.io.js"></script>
+<script src="https://cdn.socket.io/4.7.5/socket.io.min.js"></script>
 <script>
 (() => {
   const dmUuid = {{ dm_uuid|tojson }};
   const csrfToken = {{ csrf_token|tojson }};
   const actorKey = `${({{ actor.actor_type|tojson }})}:${({{ actor.actor_id|string|tojson }})}`;
   const wrapInfo = new TextEncoder().encode('DM-WRAP-KEY');
   const listEl = document.getElementById('dmMessages');
   const inputEl = document.getElementById('dmInput');
   let identity = null;
 
   const b64ToBytes = (b64) => Uint8Array.from(atob(b64), c => c.charCodeAt(0));
   const bytesToB64 = (bytes) => btoa(String.fromCharCode(...new Uint8Array(bytes)));
   const deviceId = localStorage.getItem('dm_device_id') || crypto.randomUUID();
   localStorage.setItem('dm_device_id', deviceId);
 
   const derivePassKey = async (passphrase, saltBytes, usages=['encrypt','decrypt']) => {
     const keyMat = await crypto.subtle.importKey('raw', new TextEncoder().encode(passphrase), 'PBKDF2', false, ['deriveKey']);
     return crypto.subtle.deriveKey({name:'PBKDF2', hash:'SHA-256', salt:saltBytes, iterations:250000}, keyMat, {name:'AES-GCM', length:256}, false, usages);
   };
 
   const ensureIdentity = async () => {
     const res = await fetch(`/chat/api/dm/identity?device_id=${encodeURIComponent(deviceId)}`, {credentials:'same-origin'});
     const data = await res.json();
     const passphrase = prompt('DMパスフレーズを入力してください');
     if (!passphrase) throw new Error('passphrase required');
@@ -78,51 +78,54 @@
     const text = inputEl.value.trim(); if (!text) return;
     const peerType = actorKey==='admin:admin'?'line':'admin';
     const peerId = actorKey==='admin:admin'?prompt('送信先 actor_id'):'admin';
     if (!peerId) return;
     const pubRes = await fetch(`/chat/api/dm/identity/public?actor_type=${encodeURIComponent(peerType)}&actor_id=${encodeURIComponent(peerId)}`, {credentials:'same-origin'});
     const pubData = await pubRes.json(); if (!pubData.public_key) return alert('相手鍵が未登録です');
     const peerPub = await crypto.subtle.importKey('spki', b64ToBytes(pubData.public_key), {name:'ECDH', namedCurve:'P-256'}, false, []);
     const msgKeyRaw = crypto.getRandomValues(new Uint8Array(32));
     const msgKey = await crypto.subtle.importKey('raw', msgKeyRaw, {name:'AES-GCM'}, false, ['encrypt']);
     const msgNonce = crypto.getRandomValues(new Uint8Array(12));
     const cipher = await crypto.subtle.encrypt({name:'AES-GCM', iv:msgNonce}, msgKey, new TextEncoder().encode(text));
     const wrapFor = async (targetKey, targetPub) => {
       const bits = await crypto.subtle.deriveBits({name:'ECDH', public:targetPub}, identity.privateKey, 256);
       const hk = await crypto.subtle.importKey('raw', bits, 'HKDF', false, ['deriveKey']);
       const wk = await crypto.subtle.deriveKey({name:'HKDF', hash:'SHA-256', salt:new Uint8Array(), info:wrapInfo}, hk, {name:'AES-GCM', length:256}, false, ['encrypt']);
       const nonce = crypto.getRandomValues(new Uint8Array(12));
       const w = await crypto.subtle.encrypt({name:'AES-GCM', iv:nonce}, wk, msgKeyRaw);
       return [targetKey,{from_actor_key:actorKey,ciphertext:bytesToB64(w),nonce:bytesToB64(nonce),alg:'AES-GCM',hkdf:'HKDF-SHA256',info:'DM-WRAP-KEY'}];
     };
     const selfPub = identity.publicKey;
     const wrapped = Object.fromEntries([await wrapFor(`${peerType}:${peerId}`, peerPub), await wrapFor(actorKey, selfPub)]);
     socket.emit('dm_send_message', {uuid: dmUuid, csrf_token: csrfToken, ciphertext: bytesToB64(cipher), nonce: bytesToB64(msgNonce), alg:'AES-GCM', wrapped_keys_json: wrapped});
     inputEl.value='';
   };
 
-  const socket = io();
+  const socket = io({ path: '/socket.io', transports: ['websocket', 'polling'], withCredentials: true });
+  socket.on('connect_error', (err) => {
+    console.warn('[socket.io] dm connect_error', err?.message || err);
+  });
   socket.on('connect', () => socket.emit('join_dm', {uuid: dmUuid, csrf_token: csrfToken}));
   socket.on('dm_message', async (m) => renderMsg(await decryptMessage(m), m.sender_actor_id===String({{ actor.actor_id|string|tojson }})));
   document.getElementById('dmSend').addEventListener('click', sendMessage);
   inputEl.addEventListener('keydown', (e)=>{ if (e.key==='Enter') sendMessage(); });
 
   (async () => {
     await ensureIdentity();
     const res = await fetch(`/chat/api/dm/${encodeURIComponent(dmUuid)}/messages`, {credentials:'same-origin'});
     const data = await res.json();
     for (const m of (data.messages||[])) renderMsg(await decryptMessage(m), m.sender_actor_id===String({{ actor.actor_id|string|tojson }}));
   })().catch((e)=>alert(e.message||'DM初期化に失敗しました'));
 })();
 </script>
 {% else %}
 
 <style>
 .chat-messages {
   height: min(70vh, 780px);
   min-height: 520px;
   overflow: auto;
   overflow-x: hidden;
   background: #e8f3ff;
   touch-action: pan-y;
 }
 
@@ -1648,51 +1651,58 @@
     threadRootId: null,
     loadingOlder: false,
     hasMoreOlder: true,
     messageIds: new Set(),
     unreadByRoomId: new Map(),
     unreadRefreshTimer: null,
     notificationPollTimer: null,
     muteUntilByRoomId: new Map(),
     mentionCandidates: [],
     mentionActiveIndex: 0,
     mentionRange: null,
     mentionDebounceTimer: null,
   };
 
   if (ctxReactions && ctxReactions.childElementCount === 0) {
     reactionEmojis.forEach((emoji) => {
       const btn = document.createElement('button');
       btn.type = 'button';
       btn.className = 'chat-reaction-emoji';
       btn.dataset.emoji = emoji;
       btn.textContent = emoji;
       ctxReactions.appendChild(btn);
     });
   }
 
-  const socket = io({ transports: ['websocket', 'polling'] });
+  const socket = io({
+    path: '/socket.io',
+    transports: ['websocket', 'polling'],
+    withCredentials: true,
+  });
+  socket.on('connect_error', (err) => {
+    console.warn('[socket.io] room connect_error', err?.message || err);
+  });
   socket.emit('chat_join', { event_id: eventId, room_id: roomId });
 
   initReactionUis();
   syncExistingMessageIds();
   scheduleUnreadRefresh();
   refreshNotificationBadge();
   startNotificationBadgePolling();
   refreshMuteButton();
   scrollToLatestMessage();
   refreshReadIndicators();
   window.addEventListener('load', () => {
     scrollToLatestMessage();
     sendSeenForLatestMessage();
   }, { once: true });
 
   updateAutoGrow();
   bodyInput?.addEventListener('input', () => {
     updateAutoGrow();
     queueTypingStart();
     handleMentionInput();
   });
   bodyInput?.addEventListener('keydown', handleMentionKeydown);
 
   socket.on('chat_message', (msg) => {
     const wasNearBottom = isNearBottom(msgBox);
diff --git a/external_login_user/notifications.py b/external_login_user/notifications.py
index 022e213a8f4469cb6f6c90db333bc90b55d9cbc6..933be98925241e06da4cb6b01ee43be79a6b6e30 100644
--- a/external_login_user/notifications.py
+++ b/external_login_user/notifications.py
@@ -1,47 +1,65 @@
 from __future__ import annotations
 
 from datetime import datetime, timezone
 from time import perf_counter
 from typing import Any
-from urllib.parse import parse_qs, urlparse
+from urllib.parse import parse_qs, quote, urlparse
 
 from flask import abort, current_app, jsonify, render_template, request, session
 
 from . import bp
 from .utils import _require_ext_login
 from app.utils.db import get_db
 
 try:
     from app.chat.socketio_ext import socketio
 except Exception:  # pragma: no cover
     socketio = None
 
 
 _READ_NOTIFICATIONS_KEEP_LIMIT = 30
 
 
+def _api_login_required_response():
+    next_path = request.full_path if request.query_string else request.path
+    login_url = bp.url_prefix + "/line/login" if bp.url_prefix else "/external-login/line/login"
+    if next_path:
+        login_url = f"{login_url}?next={quote(next_path, safe='/?:=&')}"
+    resp = jsonify({"ok": False, "error": "login_required", "login_url": login_url})
+    resp.status_code = 401
+    resp.headers["Cache-Control"] = "no-store, no-cache, must-revalidate, max-age=0"
+    resp.headers["Pragma"] = "no-cache"
+    return resp
+
+
+def _require_ext_login_json():
+    if session.get("ext_user_id"):
+        return None
+    return _api_login_required_response()
+
+
 def _parse_chat_room_context(row: dict[str, Any]) -> tuple[int | None, str | None]:
     target_url = str(row.get("target_url") or "").strip()
     event_id = int(row.get("event_id") or 0) or None
     if not target_url:
         return event_id, None
     try:
         parsed = urlparse(target_url)
         if not parsed.path.startswith("/chat/events/"):
             return event_id, None
         qs = parse_qs(parsed.query or "")
         room_id = (qs.get("room_id") or [None])[0]
         if room_id:
             room_id = str(room_id)
         if not event_id:
             ev = (qs.get("event_id") or [None])[0]
             if ev:
                 event_id = int(ev)
         return event_id, room_id
     except Exception:
         return event_id, None
 
 
 def _is_notification_visible_for_external(cur, uid: int, row: dict[str, Any], room_cache: dict[str, tuple[int, str]]) -> tuple[bool, str | None]:
     event_id, room_id = _parse_chat_room_context(row)
     if not room_id:
@@ -253,70 +271,70 @@ def create_notification_external(
             (kind or "").strip() or "general",
             dedup,
             inserted,
             existing["id"] if existing else None,
             deleted_old_read,
         )
         return inserted
     except Exception:
         current_app.logger.warning("create_notification_external failed user_id=%s", user_id, exc_info=True)
         return False
     finally:
         cur.close()
         db.close()
 
 
 @bp.get("/notifications")
 def notifications_page():
     guard = _require_ext_login()
     if guard:
         return guard
     return render_template("notifications.html")
 
 
 @bp.get("/api/notifications/unread-count")
 def api_notifications_unread_count():
-    guard = _require_ext_login()
+    guard = _require_ext_login_json()
     if guard:
         return guard
 
     uid = int(session.get("ext_user_id") or 0)
     try:
         count = _compute_unread_count_external(uid)
         current_app.logger.info("notifications unread-count user_id=%s read_at_is_null=true count=%s", uid, count)
         resp = jsonify({"count": count})
         resp.headers["Cache-Control"] = "no-store, no-cache, must-revalidate, max-age=0"
         resp.headers["Pragma"] = "no-cache"
         return resp
     except Exception:
         current_app.logger.warning("notifications unread-count failed user_id=%s", uid, exc_info=True)
         raise
 
 
 @bp.get("/api/notifications")
 def api_notifications_list():
-    guard = _require_ext_login()
+    guard = _require_ext_login_json()
     if guard:
         return guard
 
     uid = int(session.get("ext_user_id") or 0)
     unread_arg = (request.args.get("unread") or "").strip().lower()
     unread_only = unread_arg not in {"0", "false", "no", "off"}
     page = max(int(request.args.get("page") or 1), 1)
     per_page = 20
     offset = (page - 1) * per_page
 
     where = ["user_kind='external'", "user_id=%s"]
     params: list[Any] = [uid]
     if unread_only:
         where.append("read_at IS NULL")
 
     where_sql = " AND ".join(where)
     db = get_db()
     cur = db.cursor(dictionary=True)
     try:
         room_cache: dict[str, tuple[int, str]] = {}
         cur.execute(
             f"""
             SELECT id, kind, title, body, target_url, event_id, created_at, read_at
               FROM mfu_notifications
              WHERE {where_sql}
@@ -375,51 +393,51 @@ def api_notifications_list():
             latest_id,
             latest_created_at,
         )
 
         resp = jsonify(
             {
                 "items": items,
                 "pagination": {
                     "page": page,
                     "per_page": per_page,
                     "total": total,
                     "has_next": page * per_page < total,
                 },
             }
         )
         resp.headers["Cache-Control"] = "no-store, no-cache, must-revalidate, max-age=0"
         resp.headers["Pragma"] = "no-cache"
         return resp
     finally:
         cur.close()
         db.close()
 
 
 @bp.get("/api/notifications/updates")
 def api_notifications_updates():
-    guard = _require_ext_login()
+    guard = _require_ext_login_json()
     if guard:
         return guard
 
     uid = int(session.get("ext_user_id") or 0)
     since_id = max(int(request.args.get("since_id") or 0), 0)
     limit = min(max(int(request.args.get("limit") or 20), 1), 100)
 
     started = perf_counter()
     db = get_db()
     cur = db.cursor(dictionary=True)
     try:
         db.start_transaction()
         room_cache: dict[str, tuple[int, str]] = {}
 
         cur.execute(
             """
             SELECT id, title, body, target_url, event_id, created_at, read_at
               FROM mfu_notifications
              WHERE user_kind='external'
                AND user_id=%s
                AND id > %s
              ORDER BY id ASC
              LIMIT %s
             """,
             (uid, since_id, limit),
@@ -471,80 +489,80 @@ def api_notifications_updates():
             since_id,
             limit,
             len(items),
             latest_id,
             unread_count,
             elapsed_ms,
         )
 
         resp = jsonify(
             {
                 "latest_id": latest_id,
                 "unread_count": unread_count,
                 "items": items,
             }
         )
         resp.headers["Cache-Control"] = "no-store, no-cache, must-revalidate, max-age=0"
         resp.headers["Pragma"] = "no-cache"
         return resp
     finally:
         cur.close()
         db.close()
 
 
 @bp.post("/api/notifications/<int:notification_id>/read")
 def api_notifications_mark_read(notification_id: int):
-    guard = _require_ext_login()
+    guard = _require_ext_login_json()
     if guard:
         return guard
 
     uid = int(session.get("ext_user_id") or 0)
     db = get_db()
     cur = db.cursor()
     try:
         cur.execute(
             """
             UPDATE mfu_notifications
                SET read_at=COALESCE(read_at, %s)
              WHERE id=%s
                AND user_kind='external'
                AND user_id=%s
             """,
             (datetime.utcnow(), notification_id, uid),
         )
         db.commit()
         if cur.rowcount == 0:
             abort(404)
         _emit_notif_unread(uid, reason="read")
         return jsonify({"ok": True})
     finally:
         cur.close()
         db.close()
 
 
 @bp.post("/api/notifications/read-all")
 def api_notifications_mark_all_read():
-    guard = _require_ext_login()
+    guard = _require_ext_login_json()
     if guard:
         return guard
 
     uid = int(session.get("ext_user_id") or 0)
     db = get_db()
     cur = db.cursor()
     try:
         cur.execute(
             """
             UPDATE mfu_notifications
                SET read_at=%s
              WHERE user_kind='external'
                AND user_id=%s
                AND read_at IS NULL
             """,
             (datetime.utcnow(), uid),
         )
         updated = int(cur.rowcount or 0)
         db.commit()
         _emit_notif_unread(uid, reason="read_all")
         return jsonify({"ok": True, "updated": updated})
     finally:
         cur.close()
         db.close()
diff --git a/external_login_user/template/_extlogin_nav.html b/external_login_user/template/_extlogin_nav.html
index 721525c5bd55bdc8bf91d67a4c8614ca27340e6d..321d4ce3b1e9ace567832dd07e8ec218ee876d23 100644
--- a/external_login_user/template/_extlogin_nav.html
+++ b/external_login_user/template/_extlogin_nav.html
@@ -3,92 +3,109 @@
     <a class="navbar-brand" href="/external-login/">MFU</a>
 
     <div class="d-flex align-items-center gap-2 ms-auto">
       <a class="nav-link position-relative p-2" href="/external-login/notifications" aria-label="通知" id="navNotifBell">
         <span class="fs-5">🔔</span>
         <span id="navNotifBadge"
               class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger d-none">0</span>
       </a>
 
       <button class="navbar-toggler" type="button"
               data-bs-toggle="collapse" data-bs-target="#extNavCollapse"
               aria-controls="extNavCollapse" aria-expanded="false" aria-label="メニュー">
         <span class="navbar-toggler-icon"></span>
       </button>
     </div>
 
     <div class="collapse navbar-collapse" id="extNavCollapse">
       <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
         <li class="nav-item"><a class="nav-link" href="/external-login/">ホーム</a></li>
         <li class="nav-item"><a class="nav-link" href="/chat/dm/admin">管理者DM</a></li>
       </ul>
     </div>
   </div>
 </nav>
 
-<script src="/socket.io/socket.io.js"></script>
+<script src="https://cdn.socket.io/4.7.5/socket.io.min.js"></script>
 <script>
   document.addEventListener('DOMContentLoaded', () => {
     const badge = document.getElementById('navNotifBadge');
     if (!badge) return;
 
     let pollTimer = null;
 
     const applyCount = (rawCount) => {
       const count = Number(rawCount ?? 0);
       if (Number.isFinite(count) && count > 0) {
         badge.textContent = String(Math.min(Math.floor(count), 99));
         badge.classList.remove('d-none');
         return;
       }
       badge.textContent = '0';
       badge.classList.add('d-none');
     };
 
     const updateBadge = async () => {
       try {
         const res = await fetch('/external-login/api/notifications/unread-count', { credentials: 'same-origin', cache: 'no-store' });
+        if (res.status === 401) {
+          const data = await res.json().catch(() => ({}));
+          if (data?.login_url) {
+            window.location.assign(data.login_url);
+            return;
+          }
+          console.warn('[notifications] login required but login_url is missing');
+          return;
+        }
         if (!res.ok) {
+          console.warn('[notifications] unread-count failed', res.status);
           applyCount(0);
           return;
         }
         const data = await res.json();
         applyCount(data?.count ?? 0);
       } catch (_e) {
         applyCount(0);
       }
     };
 
     const startPolling = () => {
       if (pollTimer !== null) return;
       pollTimer = window.setInterval(updateBadge, 30000);
     };
 
     const stopPolling = () => {
       if (pollTimer === null) return;
       window.clearInterval(pollTimer);
       pollTimer = null;
     };
 
     if (window.io) {
-      const socket = window.io();
+      const socket = window.io({
+        path: '/socket.io',
+        transports: ['websocket', 'polling'],
+        withCredentials: true,
+      });
+      socket.on('connect_error', (err) => {
+        console.warn('[socket.io] ext nav connect_error', err?.message || err);
+      });
       socket.on('connect', () => {
         stopPolling();
         updateBadge();
       });
       socket.on('disconnect', () => {
         startPolling();
       });
       socket.on('notif_unread', (data) => {
         applyCount(data?.count ?? 0);
       });
     } else {
       startPolling();
     }
 
     updateBadge();
     document.addEventListener('visibilitychange', () => {
       if (!document.hidden) updateBadge();
     });
     window.addEventListener('pageshow', updateBadge);
   });
 </script>
diff --git a/external_login_user/template/notifications.html b/external_login_user/template/notifications.html
index 4561c9b2bf81bb474fd23fd5f8c5aea698c0d5c1..4f966c59247be29a7b7cb355a5c70e3140ceb98d 100644
--- a/external_login_user/template/notifications.html
+++ b/external_login_user/template/notifications.html
@@ -80,158 +80,173 @@
           await fetch(`/external-login/api/notifications/${item.id}/read`, {
             method: 'POST',
             credentials: 'same-origin',
           });
         } catch (_e) {
           // noop
         }
         window.location.assign(item.target_url || '/external-login/');
       });
       return a;
     };
 
     const updateNavBadge = (count) => {
       const badge = document.getElementById('navNotifBadge');
       if (!badge) return;
       const safeCount = Number.isFinite(Number(count)) ? Math.max(0, Number(count)) : 0;
       if (safeCount > 0) {
         badge.textContent = String(Math.min(Math.floor(safeCount), 99));
         badge.classList.remove('d-none');
         return;
       }
       badge.textContent = '0';
       badge.classList.add('d-none');
     };
 
+
+    const handleLoginRequired = async (res) => {
+      if (res?.status !== 401) return false;
+      const data = await res.json().catch(() => ({}));
+      if (data?.login_url) {
+        window.location.assign(data.login_url);
+        return true;
+      }
+      console.warn('[notifications] login required but login_url is missing');
+      return true;
+    };
+
     const prependNewItems = (items) => {
       if (!Array.isArray(items) || items.length === 0) return;
       for (const item of items) {
         if (!item || !item.id || knownIds.has(item.id)) continue;
         knownIds.add(item.id);
         listEl.insertBefore(createItem(item), listEl.firstChild);
       }
       emptyEl.classList.toggle('d-none', listEl.children.length > 0);
     };
 
     const renderItems = (items, append = false) => {
       if (!append) {
         listEl.innerHTML = '';
         knownIds.clear();
       }
       items.forEach((item) => {
         if (!item || !item.id || knownIds.has(item.id)) return;
         knownIds.add(item.id);
         listEl.appendChild(createItem(item));
       });
       const hasItems = listEl.children.length > 0;
       emptyEl.classList.toggle('d-none', hasItems);
       loadMoreBtn.classList.toggle('d-none', !hasNext);
 
       if (!append) {
         const latest = items.reduce((max, item) => Math.max(max, Number(item?.id || 0)), 0);
         lastId = Math.max(lastId, latest);
       }
     };
 
     const load = async ({ append = false } = {}) => {
       if (loading) return;
       loading = true;
       try {
         const unread = onlyUnread.checked ? '1' : '0';
         const res = await fetch(`/external-login/api/notifications?unread=${unread}&page=${page}`, {
           credentials: 'same-origin',
           cache: 'no-store',
         });
+        if (await handleLoginRequired(res)) return;
         if (!res.ok) throw new Error('failed to load notifications');
         const data = await res.json();
         hasNext = !!data?.pagination?.has_next;
         renderItems(data.items || [], append);
         if (!append) {
           const unreadCount = (data.items || []).filter((item) => !item.read_at).length;
           updateNavBadge(unreadCount);
         }
       } finally {
         loading = false;
       }
     };
 
     const pollUpdatesOnce = async () => {
       if (document.hidden) return;
       if (onlyUnread.checked) return;
       try {
         const res = await fetch(`/external-login/api/notifications/updates?since_id=${lastId}&limit=50`, {
           credentials: 'same-origin',
           cache: 'no-store',
         });
+        if (await handleLoginRequired(res)) return;
         if (!res.ok) return;
         const data = await res.json();
         prependNewItems(data.items || []);
         lastId = Math.max(lastId, Number(data?.latest_id || 0));
         updateNavBadge(data?.unread_count ?? 0);
       } catch (_e) {
         // noop
       }
     };
 
     const stopPolling = () => {
       if (pollingTimer) {
         window.clearInterval(pollingTimer);
         pollingTimer = null;
       }
     };
 
     const startPolling = () => {
       stopPolling();
       if (document.hidden || onlyUnread.checked) return;
       pollingTimer = window.setInterval(pollUpdatesOnce, 2000);
     };
 
     onlyUnread.addEventListener('change', async () => {
       page = 1;
       await load({ append: false });
       startPolling();
     });
 
     loadMoreBtn.addEventListener('click', async () => {
       if (!hasNext) return;
       page += 1;
       await load({ append: true });
     });
 
     refreshBtn.addEventListener('click', async () => {
       page = 1;
       await load({ append: false });
       await pollUpdatesOnce();
     });
 
     markAllReadBtn.addEventListener('click', async () => {
       markAllReadBtn.disabled = true;
       try {
-        await fetch('/external-login/api/notifications/read-all', {
+        const res = await fetch('/external-login/api/notifications/read-all', {
           method: 'POST',
           credentials: 'same-origin',
         });
+        if (await handleLoginRequired(res)) return;
         page = 1;
         await load({ append: false });
         await pollUpdatesOnce();
       } finally {
         markAllReadBtn.disabled = false;
       }
     });
 
     const refreshCurrentHead = async () => {
       await pollUpdatesOnce();
     };
 
     document.addEventListener('visibilitychange', () => {
       if (document.hidden) {
         stopPolling();
         return;
       }
       refreshCurrentHead();
       startPolling();
     });
     window.addEventListener('pageshow', async () => {
       await refreshCurrentHead();
       startPolling();
     });
     window.addEventListener('message', (event) => {
