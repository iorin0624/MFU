diff --git a/chat/templates/chat/room.html b/chat/templates/chat/room.html
index fadb25f17a115d77eb1afee902ccf10230007114..a5be77ab7921cebe4be345dd55ace2d2f4a5a4eb 100644
--- a/chat/templates/chat/room.html
+++ b/chat/templates/chat/room.html
@@ -1804,172 +1804,208 @@ window.addEventListener('DOMContentLoaded', async () => {
     threadRootId: null,
     loadingOlder: false,
     hasMoreOlder: true,
     messageIds: new Set(),
     unreadByRoomId: new Map(),
     unreadRefreshTimer: null,
     notificationPollTimer: null,
     presenceHeartbeatTimer: null,
     roomPresenceEntered: false,
     muteUntilByRoomId: new Map(),
     mentionCandidates: [],
     mentionActiveIndex: 0,
     mentionRange: null,
     mentionDebounceTimer: null,
     uploadPreviewFiles: [],
     uploadPreviewUrls: [],
     dragOverlayDepth: 0,
     readFrontDebugEnabled: false,
     readFrontDebugLog: [],
     readFrontDebugSeq: 0,
     latestReadSnapshotCount: 0,
     latestReadUpdate: null,
     latestSendSeenMessageId: null,
     latestMyMessageChecks: [],
     readFrontDebugSelectedMessageId: null,
+    initialReadSnapshotApplied: false,
+    initialJoinEmitted: false,
+    initialReadSnapshotTimer: null,
   };
 
   if (ctxReactions && ctxReactions.childElementCount === 0) {
     reactionEmojis.forEach((emoji) => {
       const btn = document.createElement('button');
       btn.type = 'button';
       btn.className = 'chat-reaction-emoji';
       btn.dataset.emoji = emoji;
       btn.textContent = emoji;
       ctxReactions.appendChild(btn);
     });
   }
 
   const socket = window.io({ path: '/socket.io', transports: ['websocket', 'polling'] });
-  if (dmMode) {
-    socket.emit('chat_join', { dm_uuid: dmUuid, room_id: effectiveDmRoomId });
-  } else {
-    socket.emit('chat_join', { event_id: eventId, room_id: roomId });
-  }
-
-  initReactionUis();
-  syncExistingMessageIds();
-  await initializeNotificationState();
-  startPresenceHeartbeat();
-  scheduleUnreadRefresh();
-  startNotificationBadgePolling();
-  refreshMuteButton();
-  scrollToLatestMessage();
-  initializeReadFrontDebug();
-  refreshReadIndicators();
-  window.addEventListener('load', () => {
-    scrollToLatestMessage();
-    sendSeenForLatestMessage();
-  }, { once: true });
-
-  updateAutoGrow();
-  bodyInput?.addEventListener('input', () => {
-    updateAutoGrow();
-    queueTypingStart();
-    handleMentionInput();
-  });
-  bodyInput?.addEventListener('keydown', handleMentionKeydown);
-
   socket.on('chat_message', (msg) => {
     const wasNearBottom = isNearBottom(msgBox);
     const messageId = Number(msg?.id || 0);
     const threadRootId = Number(msg?.thread_root_id || 0);
     const isThreadReply = threadRootId > 0;
 
     if (isThreadReply) {
       bumpThreadReplyCount(threadRootId);
       if (state.threadRootId && threadRootId === Number(state.threadRootId)) {
         appendThreadMessage(msg);
         sendSeenForMessageId(messageId);
       }
     } else {
       renderMessage(msg);
       if (state.threadRootId && messageId === Number(state.threadRootId)) {
         appendThreadMessage(msg);
       }
       if (composerFocused) {
         scrollForComposerFocus();
         hideJumpBtn();
       } else if (wasNearBottom) {
         scrollToBottom();
         hideJumpBtn();
       } else {
         showJumpBtn();
       }
       sendSeenForMessageId(messageId);
     }
     setSendingState(false);
     scheduleUnreadRefresh();
   });
   socket.on('chat_error', (d) => {
     setSendingState(false);
     alert(d.error || '送信失敗');
   });
   socket.on('chat_read_snapshot', (payload) => {
+    const readStates = Array.isArray(payload?.read_states) ? payload.read_states : [];
+    const debugPayload = {
+      dmMode,
+      eventId,
+      roomId,
+      dmUuid,
+      payload_event_id: payload?.event_id ?? null,
+      payload_room_id: payload?.room_id ?? null,
+      payload_dm_uuid: payload?.dm_uuid ?? null,
+      read_states_length: readStates.length,
+      readStateSize: state.readStateByActor.size,
+    };
+    pushReadFrontDebugLog('socket_snapshot_received', debugPayload);
+    if (!isReadSnapshotForCurrentRoom(payload)) {
+      pushReadFrontDebugLog('socket_snapshot_ignored_room_mismatch', debugPayload);
+      return;
+    }
     state.latestReadSnapshotCount = Array.isArray(payload?.read_states) ? payload.read_states.length : 0;
-    pushReadFrontDebugLog('socket_snapshot', { room: getDebugRoomContext(), count: state.latestReadSnapshotCount });
-    applyReadSnapshot(payload?.read_states || []);
+    applyReadSnapshot(readStates);
+    state.initialReadSnapshotApplied = true;
+    if (state.initialReadSnapshotTimer) {
+      clearTimeout(state.initialReadSnapshotTimer);
+      state.initialReadSnapshotTimer = null;
+    }
     refreshReadIndicators();
+    refreshAllReadFrontDebug?.();
+    pushReadFrontDebugLog('socket_snapshot_applied', {
+      ...debugPayload,
+      readStateSize: state.readStateByActor.size,
+    });
     sendSeenForLatestMessage();
   });
   socket.on('chat_read_update', (payload) => {
     state.latestReadUpdate = payload || null;
-    pushReadFrontDebugLog('socket_update', { room: getDebugRoomContext(), actor_key: payload?.actor_key || `${payload?.actor_type || ''}:${payload?.actor_id || ''}`, message_id: Number(payload?.last_read_message_id || 0) });
+    pushReadFrontDebugLog('socket_update_received', {
+      dmMode,
+      eventId,
+      roomId,
+      dmUuid,
+      payload_event_id: payload?.event_id ?? null,
+      payload_room_id: payload?.room_id ?? null,
+      payload_dm_uuid: payload?.dm_uuid ?? null,
+      read_states_length: 0,
+      readStateSize: state.readStateByActor.size,
+      actor_key: payload?.actor_key || `${payload?.actor_type || ''}:${payload?.actor_id || ''}`,
+      message_id: Number(payload?.last_read_message_id || 0),
+    });
     applyReadUpdate(payload);
     refreshReadIndicators();
   });
   socket.on('chat_reaction_update', (payload) => {
     const messageId = Number(payload?.message_id || 0);
     if (!messageId) return;
     const row = document.querySelector(`.chat-row[data-message-id="${messageId}"]`);
     if (row && Number(row.dataset.deletedFlag || 0) === 1) return;
     const prev = state.reactionByMessage.get(String(messageId)) || { reactionsSummary: [], myReaction: null };
     let myReaction = prev.myReaction || null;
     const changed = payload?.changed || {};
     if (canonicalReadActorKeyFromKey(changed.actor_key || `${changed.actor_type || ""}:${changed.actor_id || ""}`) === canonicalMeId) {
       myReaction = changed.emoji || null;
     }
     setReactionState(messageId, payload?.reactions || [], myReaction);
     if (isMessageInOpenThread(messageId)) refreshOpenThread();
   });
   socket.on('chat_delete_update', (payload) => {
     const messageId = Number(payload?.message_id || 0);
     if (!messageId) return;
     applyDeletedState(messageId, payload?.deleted_by_actor_type || '');
     if (isMessageInOpenThread(messageId)) refreshOpenThread();
   });
   socket.on('chat_edit_update', (payload) => {
     const messageId = Number(payload?.message_id || payload?.id || 0);
     if (!messageId) return;
     applyEditedState(messageId, payload);
     if (isMessageInOpenThread(messageId)) refreshOpenThread();
   });
   socket.on('chat_typing_update', (payload) => {
     if (dmMode) return;
     applyTypingUpdate(payload || {});
   });
 
+  initializeReadFrontDebug();
+  initReactionUis();
+  syncExistingMessageIds();
+  refreshReadIndicators();
+  emitInitialChatJoin();
+  await initializeNotificationState();
+  startPresenceHeartbeat();
+  scheduleUnreadRefresh();
+  startNotificationBadgePolling();
+  refreshMuteButton();
+  scrollToLatestMessage();
+  window.addEventListener('load', () => {
+    scrollToLatestMessage();
+    sendSeenForLatestMessage();
+  }, { once: true });
+
+  updateAutoGrow();
+  bodyInput?.addEventListener('input', () => {
+    updateAutoGrow();
+    queueTypingStart();
+    handleMentionInput();
+  });
+  bodyInput?.addEventListener('keydown', handleMentionKeydown);
+
 
 
   msgBox.addEventListener('click', (e) => {
     const selectedRow = e.target.closest('.chat-row[data-message-id]');
     if (selectedRow && isReadFrontDebugEnabled()) {
       state.readFrontDebugSelectedMessageId = Number(selectedRow.dataset.messageId || 0) || null;
       renderReadFrontDebugPanel();
     }
     const rowForAction = e.target.closest('.chat-row');
     if (rowForAction && Number(rowForAction.dataset.deletedFlag || 0) === 1) {
       if (e.target.closest('.chat-reply-quote')) {
         jumpToMessage(e.target.closest('.chat-reply-quote').dataset.jumpToId);
       }
       return;
     }
 
     const imageLink = e.target.closest('.chat-image-link');
     if (imageLink) {
       e.preventDefault();
       let imageList = [];
       try {
         imageList = JSON.parse(imageLink.dataset.imageList || '[]');
       } catch (_) {
         imageList = [];
       }
@@ -4312,85 +4348,167 @@ function openFulltextModal(row) {
     }
     if (readFrontDebugToggleBtn) {
       readFrontDebugToggleBtn.classList.remove('d-none');
       readFrontDebugToggleBtn.addEventListener('click', () => setReadFrontDebugEnabled(!state.readFrontDebugEnabled));
     }
     let initial = false;
     try { initial = localStorage.getItem(readFrontDebugStorageKey) === '1'; } catch (_) {}
     setReadFrontDebugEnabled(initial);
   }
 
   function applyReadUpdate(readState) {
     const legacyKey = `${readState?.actor_type || ''}:${readState?.actor_id || ''}`;
     const key = canonicalReadActorKeyFromKey(readState?.actor_key || legacyKey);
     pushReadFrontDebugLog('apply_read_update', { room: getDebugRoomContext(), actor_key: key || readState?.actor_key || legacyKey, message_id: Number(readState?.last_read_message_id || 0), readStateSize: state.readStateByActor.size });
     if (!key) return;
     const prev = state.readStateByActor.get(key);
     const nextReadId = Number(readState?.last_read_message_id || 0);
     const prevReadId = Number(prev?.last_read_message_id || 0);
     state.readStateByActor.set(key, {
       actor_key: key,
       display_name: readState?.display_name || prev?.display_name || readDisplayNameFallback(key),
       last_read_message_id: Math.max(prevReadId, nextReadId),
     });
   }
 
+  function emitInitialChatJoin() {
+    if (!socket) return;
+    if (state.initialReadSnapshotTimer) {
+      clearTimeout(state.initialReadSnapshotTimer);
+      state.initialReadSnapshotTimer = null;
+    }
+    state.initialJoinEmitted = true;
+    const payload = dmMode
+      ? { dm_uuid: dmUuid, room_id: effectiveDmRoomId }
+      : { event_id: eventId, room_id: roomId };
+    pushReadFrontDebugLog('initial_join_emit', {
+      dmMode,
+      eventId,
+      roomId,
+      dmUuid,
+      payload_event_id: payload?.event_id ?? null,
+      payload_room_id: payload?.room_id ?? null,
+      payload_dm_uuid: payload?.dm_uuid ?? null,
+      read_states_length: 0,
+      readStateSize: state.readStateByActor.size,
+    });
+    socket.emit('chat_join', payload);
+    state.initialReadSnapshotTimer = window.setTimeout(() => {
+      if (state.initialReadSnapshotApplied) return;
+      pushReadFrontDebugLog('initial_snapshot_timeout_warning', {
+        dmMode,
+        eventId,
+        roomId,
+        dmUuid,
+        payload_event_id: null,
+        payload_room_id: null,
+        payload_dm_uuid: null,
+        read_states_length: 0,
+        readStateSize: state.readStateByActor.size,
+        initialJoinEmitted: state.initialJoinEmitted,
+      });
+    }, 1200);
+  }
+
+  function isReadSnapshotForCurrentRoom(payload) {
+    const payloadDmUuid = String(payload?.dm_uuid || '');
+    const payloadRoomId = String(payload?.room_id || '');
+    const payloadEventId = Number(payload?.event_id || 0);
+    const hasReadStates = Array.isArray(payload?.read_states) && payload.read_states.length >= 0;
+
+    if (dmMode) {
+      if (payloadDmUuid && payloadDmUuid === String(dmUuid || '')) return true;
+      if (payloadRoomId && payloadRoomId === `dm:${dmUuid}`) return true;
+      if (payloadRoomId && payloadRoomId === String(effectiveDmRoomId || '')) return true;
+      if (!payloadDmUuid && !payloadRoomId && hasReadStates) return true;
+      return false;
+    }
+
+    const eventMatches = payloadEventId > 0 && payloadEventId === Number(eventId || 0);
+    if (!eventMatches) return false;
+    if (!payloadRoomId) return true;
+    return payloadRoomId === String(roomId || '');
+  }
+
   function applyReadSnapshot(states) {
-    pushReadFrontDebugLog('apply_read_snapshot', { room: getDebugRoomContext(), count: Array.isArray(states) ? states.length : 0 });
+    const beforeSize = state.readStateByActor.size;
+    const incomingCount = Array.isArray(states) ? states.length : 0;
+    pushReadFrontDebugLog('apply_read_snapshot', {
+      room: getDebugRoomContext(),
+      before_size: beforeSize,
+      incoming_count: incomingCount,
+    });
     state.readStateByActor.clear();
     for (const readState of states || []) {
       applyReadUpdate(readState);
     }
+    pushReadFrontDebugLog('apply_read_snapshot_result', {
+      room: getDebugRoomContext(),
+      before_size: beforeSize,
+      incoming_count: incomingCount,
+      after_size: state.readStateByActor.size,
+    });
   }
 
   function refreshReadIndicatorForRow(row) {
     const c = computeReadIndicatorState(row);
     const { indicator, list, readers, hiddenReason } = c;
     pushReadFrontDebugLog('refresh_read_row', { message_id: c.messageId, hiddenReason, readers: readers.length });
     if (hiddenReason !== 'visible') {
       if (indicator) {
         indicator.classList.add('d-none');
         indicator.textContent = '';
       }
       if (list) {
         list.classList.add('d-none');
         list.innerHTML = '';
       }
       renderReadFrontDebugForRow(row);
       return;
     }
     const readLabel = dmMode ? '既読' : `既読 ${readers.length}`;
     indicator.classList.remove('d-none');
     indicator.textContent = readLabel;
     list.innerHTML = `<ul>${readers.map((name) => `<li>${escapeHtml(name)}</li>`).join('')}</ul>`;
     renderReadFrontDebugForRow(row);
   }
 
   function refreshReadIndicators() {
     const rows = msgBox.querySelectorAll('.chat-row.me');
     rows.forEach((row) => refreshReadIndicatorForRow(row));
-    pushReadFrontDebugLog('refresh_read_indicators', { room: getDebugRoomContext(), rowCount: rows.length, readStateSize: state.readStateByActor.size });
+    pushReadFrontDebugLog('refresh_read_indicators', {
+      dmMode,
+      eventId,
+      roomId,
+      dmUuid,
+      payload_event_id: null,
+      payload_room_id: null,
+      payload_dm_uuid: null,
+      read_states_length: state.latestReadSnapshotCount,
+      rowCount: rows.length,
+      readStateSize: state.readStateByActor.size,
+    });
     refreshAllReadFrontDebug();
     renderReadFrontDebugPanel();
   }
 
   function toggleReadList(chatMain) {
     const list = chatMain?.querySelector('.chat-read-list');
     if (!list || list.classList.contains('d-none') && !list.innerHTML) return;
     list.classList.toggle('d-none');
   }
 
   function sendSeenForLatestMessage() {
     const rows = msgBox.querySelectorAll('.chat-row[data-message-id]');
     const lastRow = rows.length ? rows[rows.length - 1] : null;
     const messageId = Number(lastRow?.dataset?.messageId || 0);
     pushReadFrontDebugLog('send_seen_latest', { room: getDebugRoomContext(), message_id: messageId, rowCount: rows.length });
     if (!lastRow) return;
     sendSeenForMessageId(messageId);
   }
 
   function sendSeenForMessageId(messageId) {
     state.latestSendSeenMessageId = Number(messageId || 0) || null;
     pushReadFrontDebugLog('send_seen_message', { room: getDebugRoomContext(), message_id: Number(messageId || 0), readStateSize: state.readStateByActor.size });
     if (!Number.isFinite(messageId) || messageId <= 0) return;
     if (dmMode) {
       socket.emit('chat_seen', { dm_uuid: dmUuid, room_id: effectiveDmRoomId, last_seen_message_id: messageId });
