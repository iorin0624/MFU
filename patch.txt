diff --git a/utils/totp_util.py b/utils/totp_util.py
index e0d7d0bf8f2c72c7a267a7042e4d55c42ceae62e..951be5dbc9d6323a85a0e071d5d9ac6c6506c51f 100644
--- a/utils/totp_util.py
+++ b/utils/totp_util.py
@@ -1,27 +1,77 @@
 import pyotp
 from app.utils.db import get_db as get_db_connection
 
-def generate_otp_secret():
+
+def generate_totp_secret():
     return pyotp.random_base32()
 
+
 def assign_otp_secret_to_user(username):
     conn = get_db_connection()
     cur = conn.cursor(dictionary=True)
 
-    cur.execute("SELECT otp_secret FROM users WHERE username = %s", (username,))
+    cur.execute(
+        "SELECT totp_secret, totp_enabled FROM users WHERE username = %s", (username,)
+    )
     row = cur.fetchone()
-    if row and row['otp_secret']:
-        return row['otp_secret']
+    if row and row.get("totp_secret"):
+        return row["totp_secret"]
 
-    new_secret = generate_otp_secret()
-    cur.execute("UPDATE users SET otp_secret = %s WHERE username = %s", (new_secret, username))
+    new_secret = generate_totp_secret()
+    cur.execute(
+        "UPDATE users SET totp_secret = %s, totp_enabled = 1 WHERE username = %s",
+        (new_secret, username),
+    )
     conn.commit()
     return new_secret
 
+
+def reset_totp_secret(username):
+    conn = get_db_connection()
+    cur = conn.cursor(dictionary=True)
+    new_secret = generate_totp_secret()
+    cur.execute(
+        "UPDATE users SET totp_secret = %s, totp_enabled = 1 WHERE username = %s",
+        (new_secret, username),
+    )
+    conn.commit()
+    return new_secret
+
+
+def disable_totp_for_user(username):
+    conn = get_db_connection()
+    cur = conn.cursor(dictionary=True)
+    cur.execute(
+        "UPDATE users SET totp_secret = NULL, totp_enabled = 0 WHERE username = %s",
+        (username,),
+    )
+    conn.commit()
+
+
 def get_user_otp_secret(username):
     conn = get_db_connection()
     cur = conn.cursor(dictionary=True)
+    cur.execute(
+        "SELECT totp_secret, totp_enabled FROM users WHERE username = %s", (username,)
+    )
+    row = cur.fetchone()
+    if not row:
+        return None
+    if row.get("totp_enabled") and row.get("totp_secret"):
+        return row["totp_secret"]
+    return None
+
 
-    cur.execute("SELECT otp_secret FROM users WHERE username = %s", (username,))
+def get_totp_status(username):
+    conn = get_db_connection()
+    cur = conn.cursor(dictionary=True)
+    cur.execute(
+        "SELECT totp_secret, totp_enabled FROM users WHERE username = %s", (username,)
+    )
     row = cur.fetchone()
-    return row['otp_secret'] if row else None
+    if not row:
+        return {"enabled": False, "has_secret": False}
+    return {
+        "enabled": bool(row.get("totp_enabled")),
+        "has_secret": bool(row.get("totp_secret")),
+    }
