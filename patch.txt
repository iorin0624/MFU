diff --git a/external_login_user/template/_extlogin_nav.html b/external_login_user/template/_extlogin_nav.html
index 60abc5ad817c15f65b08a16d6fa2189032ff516f..ca707b60a60cf7c470a1451cdce19e1dae1ba203 100644
--- a/external_login_user/template/_extlogin_nav.html
+++ b/external_login_user/template/_extlogin_nav.html
@@ -1,25 +1,28 @@
 <nav class="navbar navbar-expand-lg bg-body-tertiary">
   <div class="container">
-    <a class="navbar-brand" href="{{ url_for('external_login_user.me') }}">MFU</a>
-    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#nv">
+    <a class="navbar-brand" href="/external-login/">MFU</a>
+    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#nv" aria-controls="nv" aria-expanded="false" aria-label="Toggle navigation">
       <span class="navbar-toggler-icon"></span>
     </button>
     <div id="nv" class="collapse navbar-collapse">
       <ul class="navbar-nav me-auto">
-        <li class="nav-item"><a class="nav-link" href="{{ url_for('external_login_user.me') }}">ホーム</a></li>
-        <li class="nav-item"><a class="nav-link" href="{{ url_for('external_login_user.index') }}">イベント</a></li>
-        <li class="nav-item"><a class="nav-link" href="/albums/">アルバム</a></li>
+        <li class="nav-item"><a class="nav-link" href="/external-login/">ホーム</a></li>
       </ul>
-      <ul class="navbar-nav">
+      <ul class="navbar-nav align-items-lg-center">
         <li class="nav-item">
-          <a class="nav-link position-relative" href="{{ url_for('external_login_user.notifications_page') }}" aria-label="通知">
-            🔔 通知
-            <span id="ext-notification-badge" class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger d-none">0</span>
+          <a class="nav-link position-relative" href="/external-login/notifications" id="navNotifBell" aria-label="通知">
+            🔔
+            <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger d-none" id="navNotifBadge">0</span>
           </a>
         </li>
-        <li class="nav-item"><a class="nav-link" href="{{ url_for('external_login_user.profile') }}">プロフィール</a></li>
-        <li class="nav-item"><a class="nav-link" href="{{ url_for('external_login_user.logout') }}">ログアウト</a></li>
+        <li class="nav-item dropdown">
+          <button class="nav-link btn btn-link text-decoration-none" id="extNavMenuButton" data-bs-toggle="dropdown" aria-expanded="false" aria-label="メニュー" type="button">⋮</button>
+          <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="extNavMenuButton">
+            <li><a class="dropdown-item" href="/external-login/profile">プロフィール</a></li>
+            <li><a class="dropdown-item" href="/external-login/logout">ログアウト</a></li>
+          </ul>
+        </li>
       </ul>
     </div>
   </div>
 </nav>
diff --git a/external_login_user/template/base_extlogin.html b/external_login_user/template/base_extlogin.html
index 3efdecb73e645350f6af25d3874cff6f8d825eae..bcab8da4397d67aaca6363b3e856ecff4f4167ff 100644
--- a/external_login_user/template/base_extlogin.html
+++ b/external_login_user/template/base_extlogin.html
@@ -95,53 +95,60 @@
 
     if (navigator.serviceWorker) {
       navigator.serviceWorker.addEventListener('message', (event) => {
         const data = event?.data || {};
         if (data.type !== 'MFU_PUSH_NAV' || !data.url) return;
         try {
           localStorage.setItem(
             PENDING_KEY,
             JSON.stringify({ url: normalizePath(data.url), ts: Number(data.ts) || Date.now() })
           );
         } catch (_e) {
           // noop
         }
       });
     }
 
     document.addEventListener('visibilitychange', () => {
       if (document.visibilityState === 'visible') {
         handlePendingNavigation();
       }
     });
     window.addEventListener('pageshow', handlePendingNavigation);
     window.addEventListener('focus', handlePendingNavigation);
   })();
 
-  (() => {
-    const badge = document.getElementById('ext-notification-badge');
+  document.addEventListener('DOMContentLoaded', () => {
+    const badge = document.getElementById('navNotifBadge');
     if (!badge) return;
 
+    const hideBadge = () => {
+      badge.textContent = '0';
+      badge.classList.add('d-none');
+    };
+
     const updateBadge = async () => {
       try {
-        const res = await fetch('{{ url_for("external_login_user.api_notifications_unread_count") }}', { credentials: 'same-origin' });
-        if (!res.ok) return;
+        const res = await fetch('/external-login/api/notifications/unread-count', { credentials: 'same-origin' });
+        if (!res.ok) {
+          hideBadge();
+          return;
+        }
         const data = await res.json();
-        const count = Number(data?.count || 0);
-        if (count > 0) {
-          badge.textContent = String(Math.min(count, 99));
+        const count = Number(data?.count ?? 0);
+        if (Number.isFinite(count) && count > 0) {
+          badge.textContent = String(Math.min(Math.floor(count), 99));
           badge.classList.remove('d-none');
-        } else {
-          badge.textContent = '0';
-          badge.classList.add('d-none');
+          return;
         }
       } catch (_e) {
         // noop
       }
+      hideBadge();
     };
 
     updateBadge();
     window.setInterval(updateBadge, 15000);
-  })();
+  });
 </script>
 </body>
 </html>
