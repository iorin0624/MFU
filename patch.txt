diff --git a/chat/templates/chat/room.html b/chat/templates/chat/room.html
index 38faa0d3831fbb6f05079d7050f735eb3e021395..e4d45469337b8b79f08191b94777370d55bf7eb4 100644
--- a/chat/templates/chat/room.html
+++ b/chat/templates/chat/room.html
@@ -18,51 +18,51 @@
 }
 
 .chat-date-divider span {
   display: inline-block;
   font-size: 0.78rem;
   color: #556;
   background: rgba(255, 255, 255, 0.95);
   border-radius: 999px;
   padding: 3px 12px;
 }
 
 .chat-row {
   position: relative;
   margin-bottom: 10px;
   touch-action: pan-y;
 }
 
 
 /* ===== Swipe-to-reply animation ===== */
 .chat-row .chat-bubble {
   will-change: transform;
 }
 
 .chat-swipe-underlay {
   position: absolute;
-  right: 18px;
+  left: 18px;
   top: 50%;
   transform: translateY(-50%);
   display: flex;
   align-items: center;
   justify-content: center;
   font-size: 18px;
   opacity: 0;
   pointer-events: none;
   transition: opacity 120ms ease;
 }
 
 .chat-row.swiping .chat-bubble {
   transition: none;
 }
 
 .chat-row.swiping .chat-swipe-underlay {
   opacity: 1;
 }
 
 .chat-row.snapback .chat-bubble {
   transition: transform 160ms ease;
 }
 
 .chat-row.commit .chat-bubble {
   transition: transform 120ms ease;
@@ -1535,80 +1535,80 @@
 
   msgBox.addEventListener('pointermove', (e) => {
     if (!isMobile) return;
     if (e.pointerType === 'mouse') return;
     if (!state.swipe.active || !state.swipe.row || !state.swipe.bubble) return;
     if (state.touchPointerId !== e.pointerId) return;
 
     const dx = e.clientX - state.swipe.startX;
     const dy = e.clientY - state.swipe.startY;
     state.swipe.dx = dx;
     state.swipe.dy = dy;
 
     if (!state.swipe.locked) {
       if (Math.abs(dx) > SWIPE_LOCK && Math.abs(dx) > Math.abs(dy) * 1.2) {
         state.swipe.locked = true;
         clearLongPressTimer();
       } else if (Math.abs(dy) > SWIPE_LOCK && Math.abs(dy) > Math.abs(dx) * 1.2) {
         state.swipe.active = false;
         clearLongPressTimer();
         return;
       } else {
         return;
       }
     }
 
-    if (dx >= 0) {
+    if (dx <= 0) {
       setSwipeTransform(state.swipe.row, state.swipe.bubble, 0);
       return;
     }
 
-    const x = clamp(dx, -SWIPE_MAX, 0);
+    const x = clamp(dx, 0, SWIPE_MAX);
     setSwipeTransform(state.swipe.row, state.swipe.bubble, x);
   });
 
   msgBox.addEventListener('pointerup', () => {
     if (!isMobile) return;
 
     const row = state.swipe.row;
     const bubble = state.swipe.bubble;
     const dx = state.swipe.dx;
     const locked = state.swipe.locked;
 
     state.swipe.active = false;
     state.swipe.locked = false;
     state.swipe.row = null;
     state.swipe.bubble = null;
 
     clearLongPressTimer();
 
     if (!row || !bubble || !locked) return;
 
     const moved = Math.abs(dx);
-    if (dx < 0 && moved >= SWIPE_COMMIT) {
+    if (dx > 0 && moved >= SWIPE_COMMIT) {
       row.classList.add('commit');
-      bubble.style.transform = `translateX(${-SWIPE_MAX}px)`;
+      bubble.style.transform = `translateX(${SWIPE_MAX}px)`;
 
       if (navigator?.vibrate) navigator.vibrate(10);
 
       window.setTimeout(() => {
         state.swipeIgnoreClickUntil = Date.now() + 500;
         const messageId = row.dataset.messageId;
         const senderName = row.dataset.senderName || 'Unknown';
         const bodyPlain = row.dataset.bodyPlain || '';
         setReplyState(messageId, senderName, bodyPlain, bubble);
         resetSwipe(row, bubble);
       }, 90);
       return;
     }
 
     resetSwipe(row, bubble);
   });
 
   msgBox.addEventListener('pointercancel', () => {
     if (!isMobile) return;
     const row = state.swipe.row;
     const bubble = state.swipe.bubble;
     state.swipe.active = false;
     state.swipe.locked = false;
     state.swipe.row = null;
     state.swipe.bubble = null;
