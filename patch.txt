diff --git a/chat/templates/chat/room.html b/chat/templates/chat/room.html
index 1234567..89abcde 100644
--- a/chat/templates/chat/room.html
+++ b/chat/templates/chat/room.html
@@ -1710,6 +1710,27 @@
     notificationPollTimer: null,
   };
 
+  // Service Workerへの状態通知
+  function notifyActiveToServiceWorker(active) {
+    if ('serviceWorker' in navigator && navigator.serviceWorker.controller) {
+      navigator.serviceWorker.controller.postMessage({
+        type: 'CHAT_ROOM_ACTIVE',
+        active: active,
+        eventId: eventId,
+        roomId: roomId
+      });
+    }
+  }
+
+  window.addEventListener('focus', () => {
+    notifyActiveToServiceWorker(true);
+    refreshNotificationBadge(); // フォーカス時にバッジも更新
+  });
+  window.addEventListener('blur', () => notifyActiveToServiceWorker(false));
+  window.addEventListener('beforeunload', () => notifyActiveToServiceWorker(false));
+  // 初期状態の通知
+  if (document.hasFocus()) notifyActiveToServiceWorker(true);
+
   function escapeHtml(str) {
     const div = document.createElement('div');
     div.textContent = str;
diff --git a/chat/templates/chat/sw.js b/chat/templates/chat/sw.js
index 2345678..9fedcba 100644
--- a/chat/templates/chat/sw.js
+++ b/chat/templates/chat/sw.js
@@ -8,6 +8,21 @@ self.addEventListener('activate', (event) => {
   event.waitUntil(clients.claim());
 });
 
+let activeRooms = new Map(); // clientId -> { eventId, roomId }
+
+self.addEventListener('message', (event) => {
+  if (event.data && event.data.type === 'CHAT_ROOM_ACTIVE') {
+    if (event.data.active) {
+      activeRooms.set(event.source.id, {
+        eventId: String(event.data.eventId),
+        roomId: String(event.data.roomId)
+      });
+    } else {
+      activeRooms.delete(event.source.id);
+    }
+  }
+});
+
 self.addEventListener('push', (event) => {
   let payload = {};
   try {
@@ -20,6 +35,23 @@ self.addEventListener('push', (event) => {
   const body = payload.body || '新着メッセージがあります';
   const url = payload.url || (payload.event_id ? `/chat/events/${payload.event_id}` : '/chat/');
 
+  const incomingEventId = String(payload.event_id || '');
+  const incomingRoomId = String(payload.room_id || '');
+
+  // アクティブなルームの通知を抑制
+  if (incomingEventId && incomingRoomId) {
+    let isRoomOpenAndFocused = false;
+    for (const room of activeRooms.values()) {
+      if (room.eventId === incomingEventId && room.roomId === incomingRoomId) {
+        isRoomOpenAndFocused = true;
+        break;
+      }
+    }
+    if (isRoomOpenAndFocused) {
+      return; // 通知を表示しない
+    }
+  }
+
   event.waitUntil(
     self.registration.showNotification(title, {
       body,
