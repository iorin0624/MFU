diff --git a/external_login_user/admin_users.py b/external_login_user/admin_users.py
index a6fd2b8280defab6b7f6913acc96cbe237808940..771f37c6929f9281fe190a79b6c928f17dd84440 100644
--- a/external_login_user/admin_users.py
+++ b/external_login_user/admin_users.py
@@ -75,89 +75,100 @@ def _format_datetime_jst(value) -> str | None:
         return str(value)
 
     if dt.tzinfo is None:
         dt = dt.replace(tzinfo=timezone.utc)
 
     jst = timezone(timedelta(hours=9))
     return dt.astimezone(jst).strftime("%Y年%m月%d日 %H:%M:%S")
 
 
 def _format_email_verified_at_rows(rows: list[dict]) -> None:
     for r in rows:
         r["email_verified_at"] = _format_datetime_jst(r.get("email_verified_at"))
 
 def _normalize_sort(sort_key: str | None, sort_order: str | None) -> tuple[str, str]:
     """一覧API向けのソートキーを正規化する。"""
     key = (sort_key or "id").strip().lower()
     if key not in ("id", "nickname"):
         key = "id"
 
     order = (sort_order or "asc").strip().lower()
     if order not in ("asc", "desc"):
         order = "asc"
     return key, order
 
 
+_PUSH_SUBSCRIPTION_COUNT_SQL = """
+    (
+      SELECT COUNT(*)
+        FROM chat_push_subscriptions cps
+       WHERE cps.actor_type = 'line'
+         AND cps.actor_id = CAST(external_login_user.id AS CHAR)
+    ) AS push_subscription_count
+"""
+
+
 # ============= 画面（一覧） =============
 @bp.route("/admin/ext-users")
 def admin_ext_users_index():
     guard = _require_mfu_login_redirect()
     if guard:
         return guard
 
     q = (request.args.get("q") or "").strip()
     sort_key, sort_order = _normalize_sort(request.args.get("sort"), request.args.get("order"))
 
     params: list = []
     where = []
     if q:
         like = f"%{q}%"
         where.append(
             "(CAST(id AS CHAR) LIKE %s OR nickname LIKE %s OR x_id LIKE %s OR instagram_id LIKE %s OR email LIKE %s OR social_id LIKE %s)"
         )
         params += [like, like, like, like, like, like]
     sql_where = ("WHERE " + " AND ".join(where)) if where else ""
 
     db = get_db()
     cur = db.cursor(dictionary=True)
     try:
         has_verified = _column_exists("external_login_user", "email_verified_at")
-        cols = """
+        cols = f"""
             id,
             nickname,
             x_id,
             instagram_id,
             email,
             social_id,
             avatar_file,
             avatar_url,
             created_at,
             updated_at,
             admin_note,
             COALESCE(chat_admin_alias, 0) AS chat_admin_alias,
             COALESCE(notify_album_upload, 1)  AS notify_album_upload,
             COALESCE(notify_album_process, 1) AS notify_album_process,
+            {_PUSH_SUBSCRIPTION_COUNT_SQL},
             (
               SELECT COUNT(*)
                 FROM external_login_user_card_data c
                WHERE c.user_id = external_login_user.id
                  AND c.deleted_at IS NULL
             ) AS card_count
         """
         if has_verified:
             cols += ", email_verified_at"
 
         order_by = f"id {sort_order}" if sort_key == "id" else f"nickname {sort_order}, id {sort_order}"
         cur.execute(f"""
             SELECT {cols}
               FROM external_login_user
               {sql_where}
              ORDER BY {order_by}
         """, params)
         initial_items = cur.fetchall() or []
         if not has_verified:
             for r in initial_items:
                 r["email_verified_at"] = None
         _format_email_verified_at_rows(initial_items)
 
         cur.execute(f"SELECT COUNT(*) AS c FROM external_login_user {sql_where}", params)
         total = int((cur.fetchone() or {}).get("c", 0))
@@ -188,57 +199,58 @@ def admin_ext_users_data():
     if guard:
         return guard
 
     q = (request.args.get("q") or "").strip()
     sort_key, sort_order = _normalize_sort(request.args.get("sort"), request.args.get("order"))
     try:
         page = max(int(request.args.get("page") or 1), 1)
     except Exception:
         page = 1
     try:
         per_page = min(max(int(request.args.get("per_page") or 1000), 1), 10000)
     except Exception:
         per_page = 1000
 
     params: list = []
     where = []
     if q:
         like = f"%{q}%"
         where.append(
             "(CAST(id AS CHAR) LIKE %s OR nickname LIKE %s OR x_id LIKE %s OR instagram_id LIKE %s OR email LIKE %s OR social_id LIKE %s)"
         )
         params += [like, like, like, like, like, like]
     sql_where = ("WHERE " + " AND ".join(where)) if where else ""
 
     has_verified = _column_exists("external_login_user", "email_verified_at")
-    cols = """
+    cols = f"""
         id, nickname, x_id, instagram_id, email, social_id,
         avatar_file, avatar_url, created_at, updated_at,
         admin_note,
         COALESCE(chat_admin_alias, 0) AS chat_admin_alias,
         COALESCE(notify_album_upload, 1)  AS notify_album_upload,
         COALESCE(notify_album_process, 1) AS notify_album_process,
+        {_PUSH_SUBSCRIPTION_COUNT_SQL},
         (
           SELECT COUNT(*)
             FROM external_login_user_card_data c
            WHERE c.user_id = external_login_user.id
              AND c.deleted_at IS NULL
         ) AS card_count
     """
     if has_verified:
         cols += ", email_verified_at"
 
     db = get_db(); cur = db.cursor(dictionary=True)
     try:
         cur.execute(f"SELECT COUNT(*) AS c FROM external_login_user {sql_where}", params)
         total = int(cur.fetchone()["c"])
         offset = (page - 1) * per_page
 
         order_by = f"id {sort_order}" if sort_key == "id" else f"nickname {sort_order}, id {sort_order}"
         cur.execute(f"""
             SELECT {cols}
               FROM external_login_user
               {sql_where}
              ORDER BY {order_by}
              LIMIT %s OFFSET %s
         """, params + [per_page, offset])
         rows = cur.fetchall() or []
@@ -249,64 +261,67 @@ def admin_ext_users_data():
     finally:
         try: cur.close(); db.close()
         except Exception: pass
 
     return jsonify({
         "ok": True,
         "items": rows,
         "total": total,
         "page": page,
         "per_page": per_page,
         "sort": sort_key,
         "order": sort_order,
         "q": q,
     })
 
 # ============= API（単票：旧モーダル用） =============
 @bp.get("/admin/ext-users/<int:user_id>/detail")
 def admin_ext_users_detail(user_id: int):
     """（旧モーダル用）単票取得。"""
     guard = _require_mfu_login_redirect()
     if guard:
         return guard
 
     db = get_db(); cur = db.cursor(dictionary=True)
     try:
-        cur.execute("""
+        cur.execute(f"""
             SELECT
               id,
               nickname,
               x_id,
               instagram_id,
               email,
               social_id,
               avatar_file,
               avatar_url,
               created_at,
               updated_at,
               admin_note,
-              COALESCE(chat_admin_alias, 0) AS chat_admin_alias
+              COALESCE(chat_admin_alias, 0) AS chat_admin_alias,
+              COALESCE(notify_album_upload, 1)  AS notify_album_upload,
+              COALESCE(notify_album_process, 1) AS notify_album_process,
+              {_PUSH_SUBSCRIPTION_COUNT_SQL}
             FROM external_login_user
             WHERE id=%s
             LIMIT 1
         """, (user_id,))
         row = cur.fetchone()
         if not row:
             abort(404)
     finally:
         try:
             cur.close()
             db.close()
         except Exception:
             pass
 
     return jsonify({"ok": True, "item": row})
 
 
 # ============= 更新 =============
 @bp.post("/admin/ext-users/<int:user_id>/update")
 def admin_ext_users_update(user_id: int):
     """項目更新（nickname / x_id / instagram_id / email / admin_note）
        フォームPOST: 成功時は一覧へリダイレクト（?saved=1）
        JSON(AJAX): 互換のため JSON を返す
     """
     guard = _require_mfu_login_redirect()
@@ -472,57 +487,58 @@ def admin_ext_users_resend_confirm(user_id: int):
             abort(404)
         if not u.get("email"):
             return redirect(url_for("external_login_user.admin_ext_users_edit_page", user_id=user_id, error="no_email"))
     finally:
         try:
             cur.close()
             db.close()
         except Exception:
             pass
 
     return render_template("admin_ext_users_resend_confirm.html", u=u, admin_csrf=_admin_csrf_token())
 
 
 # ============= 別ページ編集（GET） =============
 @bp.get("/admin/ext-users/<int:user_id>/edit", endpoint="admin_ext_users_edit_page")
 def admin_ext_users_edit_page(user_id: int):
     """外部ログインユーザー 編集ページ（別ページ実装）"""
     guard = _require_mfu_login_redirect()
     if guard:
         return guard
 
     db = get_db()
     cur = db.cursor(dictionary=True)
     try:
         has_verified = _column_exists("external_login_user", "email_verified_at")
-        cols = """
+        cols = f"""
             id, nickname, x_id, instagram_id, email, social_id,
             avatar_file, avatar_url, created_at, updated_at,
             admin_note,
             COALESCE(chat_admin_alias, 0) AS chat_admin_alias,
             COALESCE(notify_album_upload, 1)  AS notify_album_upload,
-            COALESCE(notify_album_process, 1) AS notify_album_process
+            COALESCE(notify_album_process, 1) AS notify_album_process,
+            {_PUSH_SUBSCRIPTION_COUNT_SQL}
         """
         if has_verified:
             cols += ", email_verified_at"
         cur.execute(f"SELECT {cols} FROM external_login_user WHERE id=%s LIMIT 1", (user_id,))
         user = cur.fetchone()
         if not user:
             abort(404)
         if not has_verified:
             user["email_verified_at"] = None
 
         # 参加中イベント一覧
         cur.execute(
             """
             SELECT
               m.id AS member_id,
               e.id AS event_id,
               e.title,
               e.starts_at,
               m.status,
               m.payment_status
             FROM mfu_event_member AS m
             JOIN mfu_event AS e ON e.id = m.event_id
             WHERE m.user_id=%s
             ORDER BY COALESCE(e.starts_at, '9999-12-31') DESC, e.id DESC
             """,
diff --git a/external_login_user/template/admin_ext_users.html b/external_login_user/template/admin_ext_users.html
index 92d13c5e8c936e7a258a0c68a2391ceae3e339bf..8158367dc4bffda4cd7451aa59c8d04a2c1e1593 100644
--- a/external_login_user/template/admin_ext_users.html
+++ b/external_login_user/template/admin_ext_users.html
@@ -68,50 +68,58 @@
             <div>{{ it.email }}</div>
             <div class="small mt-1">
               {% if it.email_verified_at %}
                 <span class="badge bg-success">確認済</span>
                 <span class="text-muted ms-1">{{ it.email_verified_at }}</span>
               {% else %}
                 <span class="badge bg-secondary">未確認</span>
               {% endif %}
             </div>
           {% else %}
             <span class="text-muted">-</span>
           {% endif %}
         </td>
         <td>
           {% if it.x_id %}
             <a href="https://x.com/{{ it.x_id|e }}" target="_blank" rel="noopener">X: @{{ it.x_id }}</a>{% if it.instagram_id %}<br>{% endif %}
           {% endif %}
           {% if it.instagram_id %}
             <a href="https://www.instagram.com/{{ it.instagram_id|e }}/" target="_blank" rel="noopener">IG: @{{ it.instagram_id }}</a>
           {% endif %}
           {% if not it.x_id and not it.instagram_id %}
             <span class="text-muted">-</span>
           {% endif %}
         </td>
         <td>
+          <div>
+            Push通知
+            {% if (it.push_subscription_count or 0)|int > 0 %}
+              🔵
+            {% else %}
+              🔴
+            {% endif %}
+          </div>
           <div>
             アップ
             {% if it.notify_album_upload %}
               🔵
             {% else %}
               🔴
             {% endif %}
           </div>
           <div>
             加工
             {% if it.notify_album_process %}
               🔵
             {% else %}
               🔴
             {% endif %}
           </div>
         </td>
         <td>
           {% if it.card_count and it.card_count|int > 0 %}
             登録あり
           {% else %}
             未登録
           {% endif %}
         </td>
         <td class="text-end">
@@ -176,60 +184,61 @@
       return `<img src="${escapeHtml(avatar)}" alt="avatar" width="40" height="40" style="object-fit:cover;border-radius:50%;border:1px solid #ddd;">`;
     }
 
     function renderRows(items) {
       if (!items || !items.length) {
         tbody.innerHTML = '<tr><td colspan="8" class="text-center text-muted py-4">データがありません。</td></tr>';
         return;
       }
       tbody.innerHTML = items.map((it) => {
         const isEnabled = (value) => {
           if (value === true) return true;
           if (value === false || value == null) return false;
           const n = Number(value);
           return Number.isFinite(n) ? n === 1 : String(value).toLowerCase() === 'true';
         };
         const nick = escapeHtml(it.nickname || '');
         const socialId = escapeHtml(it.social_id || '-');
         const emailBlock = it.email
           ? `<div>${escapeHtml(it.email)}</div>
              <div class="small mt-1">${it.email_verified_at ? '<span class="badge bg-success">確認済</span><span class="text-muted ms-1">' + escapeHtml(it.email_verified_at) + '</span>' : '<span class="badge bg-secondary">未確認</span>'}</div>`
           : '<span class="text-muted">-</span>';
         let sns = '<span class="text-muted">-</span>';
         if (it.x_id || it.instagram_id) {
           sns = `${it.x_id ? `<a href="https://x.com/${encodeURIComponent(it.x_id)}" target="_blank" rel="noopener">X: @${escapeHtml(it.x_id)}</a>` : ''}${(it.x_id && it.instagram_id) ? '<br>' : ''}${it.instagram_id ? `<a href="https://www.instagram.com/${encodeURIComponent(it.instagram_id)}/" target="_blank" rel="noopener">IG: @${escapeHtml(it.instagram_id)}</a>` : ''}`;
         }
+        const notifyPush = Number(it.push_subscription_count || 0) > 0 ? '🔵' : '🔴';
         const notifyUpload = isEnabled(it.notify_album_upload) ? '🔵' : '🔴';
         const notifyProcess = isEnabled(it.notify_album_process) ? '🔵' : '🔴';
         const cardText = (it.card_count && Number(it.card_count) > 0) ? '登録あり' : '未登録';
         return `<tr data-id="${Number(it.id || 0)}">
           <td class="text-muted">#${Number(it.id || 0)}</td>
           <td>${makeAvatar(it)}</td>
           <td><div class="fw-semibold">${nick}</div><div class="small text-muted">social_id: ${socialId}</div></td>
           <td>${emailBlock}</td>
           <td>${sns}</td>
-          <td><div>アップ ${notifyUpload}</div><div>加工 ${notifyProcess}</div></td>
+          <td><div>Push通知 ${notifyPush}</div><div>アップ ${notifyUpload}</div><div>加工 ${notifyProcess}</div></td>
           <td>${cardText}</td>
           <td class="text-end"><a class="btn btn-sm btn-outline-primary" href="/external-login/admin/ext-users/${Number(it.id || 0)}/edit">編集</a></td>
         </tr>`;
       }).join('');
     }
 
     function updateSortButtonStyles() {
       const isId = state.sort === 'id';
       sortIdBtn.classList.toggle('btn-primary', isId);
       sortIdBtn.classList.toggle('btn-outline-secondary', !isId);
       sortNickBtn.classList.toggle('btn-primary', !isId);
       sortNickBtn.classList.toggle('btn-outline-secondary', isId);
       sortIdBtn.textContent = `ID順${isId ? (state.order === 'asc' ? ' ▲' : ' ▼') : ''}`;
       sortNickBtn.textContent = `ニックネーム順${!isId ? (state.order === 'asc' ? ' ▲' : ' ▼') : ''}`;
     }
 
     async function fetchAndRender() {
       const url = new URL(config.dataUrl, window.location.origin);
       url.searchParams.set('q', state.q);
       url.searchParams.set('sort', state.sort);
       url.searchParams.set('order', state.order);
       url.searchParams.set('page', '1');
       url.searchParams.set('per_page', String(state.perPage));
 
       const res = await fetch(url.toString(), { credentials: 'same-origin' });
diff --git a/external_login_user/template/admin_ext_users_edit.html b/external_login_user/template/admin_ext_users_edit.html
index 617f00c65af4f462cec8b62206620ffa4e11fd6d..882318cc5789ecbe3b54a407f565a83425857b8a 100644
--- a/external_login_user/template/admin_ext_users_edit.html
+++ b/external_login_user/template/admin_ext_users_edit.html
@@ -42,74 +42,78 @@
             <input type="text" class="form-control" name="nickname" value="{{ u.nickname or '' }}" required>
           </div>
           <div class="row g-2">
             <div class="col-md-6">
               <label class="form-label">X（@不要）</label>
               <input type="text" class="form-control" name="x_id" value="{{ u.x_id or '' }}" placeholder="example">
             </div>
             <div class="col-md-6">
               <label class="form-label">Instagram（@不要）</label>
               <input type="text" class="form-control" name="instagram_id" value="{{ u.instagram_id or '' }}" placeholder="example">
             </div>
           </div>
           <div class="mt-3">
             <label class="form-label">メール</label>
             <input type="email" class="form-control" name="email" value="{{ u.email or '' }}" placeholder="name@example.com">
             <div class="form-text mt-1">
               {% if u.email_verified_at %}
                 <span class="badge bg-success">確認済</span>
                 <small class="text-muted ms-2">{{ u.email_verified_at }}</small>
               {% else %}
                 <span class="badge bg-secondary">未確認</span>
               {% endif %}
             </div>
           </div>
 
-          {# ★ 共有アルバム通知有無の確認 #}
           <div class="mt-3">
-            <label class="form-label">共有アルバム通知</label>
-            <div>
-              <span class="badge bg-outline-secondary text-dark me-2">
-                アップロード通知:
-                {% if u.notify_album_upload %}
-                  ON
-                {% else %}
-                  OFF
-                {% endif %}
-              </span>
-              <span class="badge bg-outline-secondary text-dark">
-                加工完了通知:
-                {% if u.notify_album_process %}
-                  ON
-                {% else %}
-                  OFF
-                {% endif %}
-              </span>
+            <div class="form-label">通知状態</div>
+            <div class="fw-semibold">共有アルバム通知</div>
+            <div class="mt-1">
+              アップロード通知:
+              {% if u.notify_album_upload %}
+                ON
+              {% else %}
+                OFF
+              {% endif %}
+              加工完了通知:
+              {% if u.notify_album_process %}
+                ON
+              {% else %}
+                OFF
+              {% endif %}
             </div>
             <div class="form-text mt-1 text-muted">
               外部ユーザー本人のプロフィール画面で設定された通知フラグの現在値です（管理画面からは変更できません）。
             </div>
+            <div class="mt-1">
+              Push通知:
+              {% if (u.push_subscription_count or 0)|int > 0 %}
+                ON
+              {% else %}
+                OFF
+              {% endif %}
+            </div>
           </div>
 
           <div class="mt-3">
             <label class="form-label d-block">チャット管理者 alias</label>
             <div class="form-check form-switch">
               <input class="form-check-input" type="checkbox" id="chat_admin_alias" name="chat_admin_alias" value="1" {% if u.chat_admin_alias %}checked{% endif %}>
               <label class="form-check-label" for="chat_admin_alias">LINEログイン時にチャット/通知のみ admin alias として扱う</label>
             </div>
             <div class="form-text mt-1 text-muted">※ /chat と /mfu-notifications のみ対象。管理画面全体の権限は付与されません。</div>
           </div>
 
           {# ★ 内部メモ（管理者用） #}
           <div class="mt-3">
             <label class="form-label">内部メモ（管理者用）</label>
             <textarea
               class="form-control"
               name="admin_note"
               rows="4"
               placeholder="このユーザーに関する注意点やメモを記入">{{ u.admin_note or '' }}</textarea>
             <div class="form-text mt-1 text-muted">
               ※このメモは管理画面のみで使用され、ユーザー本人には表示されません。
             </div>
           </div>
         </div>
 
