diff --git a/utils/mail_delivery.py b/utils/mail_delivery.py
index 03e7f3162154701e5f8babc0ac395594f5756603..72330c5fbbba28c2e7b5e0168be454c67e100b30 100644
--- a/utils/mail_delivery.py
+++ b/utils/mail_delivery.py
@@ -1,40 +1,40 @@
 from __future__ import annotations
 
 import os
 import uuid
 from datetime import datetime, timedelta
 
 import requests
 from flask import current_app
 
 from app.utils.db import get_db
 
 DEFAULT_POSTFIX_STATUS_API_BASE_URL = "http://192.168.103.15:18080"
 DEFAULT_MESSAGE_ID_DOMAIN = "mfu.iori0624.jp"
 DEFAULT_POLL_LIMIT_HOURS = 24
-DEFAULT_HTTP_TIMEOUT_SEC = 5
+DEFAULT_HTTP_TIMEOUT_SEC = 15
 
 
 def _get_config_value(name: str, default):
     try:
         return current_app.config.get(name, default)
     except Exception:
         return os.environ.get(name, default)
 
 
 def _clamp(value: str | None, max_len: int) -> str | None:
     if value is None:
         return None
     text = str(value)
     return text if len(text) <= max_len else text[:max_len]
 
 
 def ensure_mail_delivery_schema() -> None:
     db = get_db()
     cur = db.cursor()
     cur.execute(
         """
         CREATE TABLE IF NOT EXISTS mfu_mail_delivery_log (
             id INT AUTO_INCREMENT PRIMARY KEY,
             mfu_mail_uuid CHAR(36) NOT NULL,
             message_id VARCHAR(255) NOT NULL,
@@ -168,85 +168,104 @@ def _update_delivery_row(
 def _touch_delivery_row(
     *,
     row_id: int,
     detail: str | None,
     checked_at: datetime,
 ) -> None:
     db = get_db()
     cur = db.cursor()
     cur.execute(
         """
         UPDATE mfu_mail_delivery_log
            SET last_delivery_detail = %s,
                last_delivery_checked_at = %s
          WHERE id = %s
         """,
         (
             _clamp(detail, 1000),
             checked_at,
             row_id,
         ),
     )
     db.commit()
     db.close()
 
 
-def poll_mail_delivery_statuses(max_rows: int = 200) -> dict:
+def poll_mail_delivery_statuses(max_rows: int = 200, timeout_sec: int | None = None) -> dict:
     ensure_mail_delivery_schema()
     base_url = _get_config_value(
         "MFU_POSTFIX_STATUS_API_BASE_URL", DEFAULT_POSTFIX_STATUS_API_BASE_URL
     )
     api_key = _get_config_value("MFU_POSTFIX_STATUS_API_KEY", "")
     limit_hours = int(
         _get_config_value("MFU_MAIL_STATUS_POLL_LIMIT_HOURS", DEFAULT_POLL_LIMIT_HOURS)
     )
-    timeout_sec = int(
-        _get_config_value("MFU_MAIL_STATUS_HTTP_TIMEOUT_SEC", DEFAULT_HTTP_TIMEOUT_SEC)
-    )
+    if timeout_sec is None:
+        timeout_sec = int(
+            _get_config_value("MFU_MAIL_STATUS_HTTP_TIMEOUT_SEC", DEFAULT_HTTP_TIMEOUT_SEC)
+        )
+    timeout_sec = max(1, min(60, int(timeout_sec)))
 
     base_url = (base_url or DEFAULT_POSTFIX_STATUS_API_BASE_URL).rstrip("/")
     headers = {"X-API-Key": api_key} if api_key else {}
 
     rows = _fetch_poll_targets(limit_hours=limit_hours, max_rows=max_rows)
     now = datetime.now()
     summary = {"checked": 0, "updated": 0, "errors": 0}
 
     for row in rows:
         summary["checked"] += 1
         message_id = row.get("message_id")
         row_id = row.get("id")
         if not message_id or not row_id:
             continue
 
         url = f"{base_url}/api/mail/status"
         try:
             resp = requests.get(
                 url,
                 params={"message_id": message_id},
                 headers=headers,
                 timeout=timeout_sec,
             )
+        except requests.Timeout:
+            retry_timeout = min(timeout_sec * 2, 60)
+            try:
+                resp = requests.get(
+                    url,
+                    params={"message_id": message_id},
+                    headers=headers,
+                    timeout=retry_timeout,
+                )
+            except requests.RequestException as exc:
+                summary["errors"] += 1
+                _touch_delivery_row(
+                    row_id=row_id,
+                    detail=f"poll_error: {exc.__class__.__name__}",
+                    checked_at=now,
+                )
+                continue
         except requests.RequestException as exc:
             summary["errors"] += 1
             _touch_delivery_row(
                 row_id=row_id,
                 detail=f"poll_error: {exc.__class__.__name__}",
                 checked_at=now,
             )
             continue
 
         if resp.status_code != 200:
             summary["errors"] += 1
             _touch_delivery_row(
                 row_id=row_id,
                 detail=f"poll_http_status: {resp.status_code}",
                 checked_at=now,
             )
             continue
 
         try:
             payload = resp.json()
         except ValueError:
             summary["errors"] += 1
             _touch_delivery_row(
                 row_id=row_id,
                 detail="poll_error: invalid_json",
